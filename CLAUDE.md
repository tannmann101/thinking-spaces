# CLAUDE.md — Thinking Spaces

## What this is
A personal, single-user thinking workshop app. Each train of thought (a concept, theory, plan, assessment) becomes a "Space" — a revisitable, evolving place, not a static note. Spaces can hold different kinds of content depending on what they're for, can reference each other, and the whole system should feel alive and buildable-on-itself over time. This is being built for one person's own use, not as a product.

## Non-negotiable working method
This project has a specific failure mode to avoid: past attempts sent one big prompt, got a rough MVP back, then spent a long time fighting it in frustrating, unreviewable tinkering. That is not how this build happens. Instead:

- **Work in small, reviewed slices.** Finish one coherent piece, then stop and explain in plain language what was built and why, before continuing.
- **Confirm scope before starting any new pass or major piece of work.** Restate your understanding of what's being asked; don't assume and run.
- **No black boxes.** The person directing this project is not writing the code themselves. Every feature needs to be legible to a non-implementer: centralize things that would otherwise be scattered (a single registry file for Tools, a single registry file for Views — see below), comment intent not just mechanics, and avoid clever abstractions that hide what's actually happening.
- **Favor boring, readable code over clever code.** This is a solo-maintained personal project, not a codebase optimized for a team.
- **Don't build ahead of the current priority** (see Roadmap) without explicit go-ahead, even if the next piece seems obvious or easy.
- **Use the person's own vocabulary** (Dashboard, Creation Mode, Spaces, Tools, Resources, Categories, Workspaces, Dev Mode, Relational Map, Work, Synthesis, Provenance — see below), not implementation jargon, when discussing the app with them. If a term drifts from how they actually think about it, that's a signal to realign the language, not just push forward.

## Tech stack
React + Vite (frontend), Node/Express + SQLite (backend). This matches the person's other personal apps (a household finance tracker, a household-management app called Secretary) — consistency across their own tools matters more than any particular technical advantage. This remains the primary stack for local development (see the launcher/README.md and the root README.md) -- `worker/` is a second, deploy-only backend (see "Hosting" below), not a replacement for `backend/`.

## Hosting
The app now has a real hosting path, matching how the person's other domain (`thegardners.xyz`, home to their "gardners-hub" household site) is already set up: static frontend on GitHub Pages, a Cloudflare Worker for anything needing a backend, gated by the same Cloudflare Access PIN login the rest of that domain already uses. Confirmed via direct question over the alternative (keep the Express/better-sqlite3 backend as-is, deploy it unchanged to a host like Render/Fly with a persistent disk, and have a Cloudflare Worker just proxy to it) -- the person picked the fuller Cloudflare-native port once shown the real cost picture (Render/Fly both dropped their free persistent-disk tiers in 2024-2026, so the proxy option would run ~$7-8/mo forever; the full port is free indefinitely, matching gardners-hub's own economics, at the cost of a real porting project rather than a quick deploy).

`worker/` is that port: a Cloudflare Worker + D1 (D1 is Cloudflare's serverless, SQLite-compatible database) reimplementation of every route and query function `backend/` has, kept as a parallel, independently-runnable backend rather than a replacement -- `backend/`'s Express + better-sqlite3 code is untouched and stays the one used for local development (the desktop launcher, `npm run dev`). Structure mirrors `backend/src/db/queries/` file-for-file (`worker/src/db/*.js`, one module per concern), and `worker/src/index.js` is a single hand-rolled router (no Express inside a Worker) matching every route in `backend/src/routes/*.js` exactly -- same paths, same validation, same status codes. Every function takes `env` as an explicit first parameter (for `env.DB`, the D1 binding) rather than reading a module-level `db` singleton the way the Node backend does, since a Worker has no safe place to stash a per-request value at module scope -- the same pattern gardners-hub's own Worker already uses. D1 confirmed to support the JSON1 SQLite extension (`json_extract`/`json_each`/`json_array_length`) before porting began, since dozens of queries here depend on it.

Two of the Node backend's one-time startup migrations were deliberately *not* ported: `migrateTextBlockLines`/`migrateWorkItemSupport` upgrade rows written before their respective content shapes existed, but a D1 database starts fresh on the *current* shapes, so there's never a legacy-shaped row to migrate. `ensureTestSpaceExists` and `seedTemplates` were also pulled out of runtime code -- a Worker has no boot hook to run them at, and running idempotent existence-checks on every request would mean real extra D1 queries on every single request forever, for something that only ever needs to happen once. `worker/schema.sql` declares the finished current schema directly (no incremental `ALTER TABLE` history to replay), and `worker/templates-seed.sql` (generated programmatically from the real `TEMPLATES` array in `backend/src/db/seedTemplates.js`, not hand-transcribed, to rule out transcription drift) seeds the 6 built-in Templates -- both applied once via `wrangler d1 execute` during deployment setup, the same one-time-setup pattern gardners-hub's own `worker/DEPLOY.md` already established for its own schema/seed. The Test Space's actual demo content (`seedTestSpaceBlocks`) is deliberately *not* ported as generic seed data at all -- this is a migration of the person's own real, already-accumulated Thinking Spaces database, not a fresh install, so the real Test Space and every other real Space need to come from an export of their actual `backend/data/thinking-spaces.sqlite` file (not yet done -- see Open).

Verified live end-to-end via `wrangler dev` against a local D1 (Miniflare) emulation, exercising every single route by hand with curl -- not just the query modules in isolation: creating a Space from a Template (confirms `applyTemplate`/Skeleton-lane seeding), the full `=`/`?`/`!` shorthand-promotion path (the skeleton.js/trail.js circular pair, the most delicate part of the port), filing a line into a lane, pairing a Tension, Workspace create/assemble/report/delete, Work items and the cross-Space picker, all three Report levels, Insights' five facets, the Week calendar, the Graph, Reviews (draft + create, including the `sinceDate` ISO-normalization fix), Creation Mode's full `extraBlocks`/`workspaceNames` resolution path, every validation/404 error path, and the Test Space's delete-protection -- confirmed the local D1 database returned to just the Test Space after every test Space created during verification was cleaned up.

Step 2 -- the frontend, the data migration, and the deployment playbook -- followed once Step 1's PR was up, confirming the subdomain (`thinking.thegardners.xyz`) via direct question and picking a Cloudflare API token over an interactive `wrangler login` (this session runs headless -- no browser to complete an OAuth flow in -- so a token is what actually works here). Four pieces:
- **The static build.** `frontend/`'s `vite build` output needed nothing new to work as a static site (it already fetches `/api/*` on its own origin, which the Worker route intercepts before GitHub Pages would ever see it) except one real gap: GitHub Pages has no server-side rewrites, so a direct load of a client-routed path like `/spaces/abc123` has no real file to serve. Fixed with the standard SPA-on-GitHub-Pages trick -- `frontend/public/404.html` encodes the real path into a query string and redirects to `/`, and a small classic (non-module) `<script>` added to `frontend/index.html`, which runs before the deferred module script that boots React, decodes it back via `history.replaceState` so React Router sees the real route on its first render. Verified live, not just read: built the site, served the static `dist/` output with a plain static file server (not Vite's own dev server, which has its own unrelated SPA handling that wouldn't have caught a real gap), confirmed a direct hit on `/spaces/abc123?foo=bar` returns a real 404 status carrying `404.html`'s content (matching what GitHub Pages actually does), then drove it with Playwright and confirmed the browser ends up back on that exact URL -- path, query string, and all -- with `SpacePage` actually rendered.
- **`frontend/public/CNAME`** (`thinking.thegardners.xyz`) and **`.github/workflows/deploy-pages.yml`** -- builds `frontend/` and publishes it via GitHub's own Pages actions (`upload-pages-artifact`/`deploy-pages`, no `gh-pages` branch to maintain) on every push to `main` touching `frontend/**`.
- **`backend/export-to-d1.mjs`** -- the actual data migration. Reads the real `backend/data/thinking-spaces.sqlite` (better-sqlite3, already a `backend/` dependency) and writes a plain SQL script of `INSERT` statements for every table, in foreign-key dependency order (templates, then spaces, then everything that references a space) -- Templates use `INSERT OR IGNORE` (defensive, in case `templates-seed.sql` already seeded the 6 built-ins first), everything else uses a plain `INSERT`, since a real primary-key collision on the person's own actual data is a genuine problem worth stopping on, not something to paper over. Verified against this session's own real, live `backend/data/thinking-spaces.sqlite` (not a synthetic fixture) -- ran it, got the exact known 4-Space baseline back out, then applied the result to a fresh local D1 (schema, then `templates-seed.sql`, then this export, the identical order the real deployment will use) and confirmed via curl that every real Space, its blocks, its backlinks, and a full Space Report all came back byte-for-byte the same through the D1-backed Worker as they do through the real Express backend. The generated `data-export.sql` is `.gitignore`d at the repo root (a bare filename pattern, so it's excluded from both `backend/` and `worker/`) -- it's real personal data and must never be committed, by accident or otherwise.
- **`worker/DEPLOY.md`** -- the actual runbook, adapted from gardners-hub's own `worker/DEPLOY.md` (same shape: log in, create the D1 database, apply schema, seed Templates, migrate data, deploy, wire up DNS/Access/Pages, test) but for Thinking Spaces' specifics -- the new `thinking.thegardners.xyz` subdomain needs its own DNS record added (gardners-hub's own root domain already had one), and the data-migration step points at `export-to-d1.mjs` instead of a hand-written `seed.sql`, since this is a migration of a personal database already full of real content, not a small app with no prior state.

This closes Step 2 of the plan as far as this session can take it without live Cloudflare access -- see Open for what's still needed (a Cloudflare API token, scoped narrowly, to actually run the deployment commands from here) before the app is live at the real URL.

## The shape of the app, in plain language

This is the vocabulary to use — it's the person's own mental model, and it should stay the primary way of talking about this project:

- **Dashboard** — where you land: create Spaces, see Spaces, see trends/metrics/insights across them.
- **Creation Mode** — the flow for starting a new Space: pulling in Tools and Resources for what this Space is for, with real room for customization/personalization so the Space feels like its own thing, not a generic form.
- **Spaces** — one Space per train of thought. Every Space loosely falls into one of 5 starting clusters (see below), plus "Other" for anything that doesn't. A Space is never *just* its cluster, though — it gets tailored the way every train of thought is different.
- **Tools** — the functions/features a Space can use to work toward its purpose (write a paragraph, keep a checklist, compare two things, chart progress, etc.). Tools are internal to the app, modular and general-purpose, expanded in dev sessions like this one rather than built by the person from inside the running app. They should be visible as a catalog (with a demo of what each one offers) and some should be markable as "groupable" with others they work well alongside.
- **Resources** — anything that exists outside of (or alongside) the app that gets surfaced *within* it, in relation to a Space, a Relational Space, the Dashboard, or a Tool: a book, a computer, a phone, an account, a YouTuber or podcaster, a social media post, a discussion, a set of notes, an interpretive **lens** (etymology, phenomenology, anthropology, history, epistemology, philosophy, or any other reading discipline worth having on hand across many different phenomena, not just one) — anything worth having on hand while thinking something through. Creating one is a deliberately different flow from creating a Space (its own "+ New Resource" entry point on the Dashboard, not a cluster card inside ordinary Creation Mode): it homes in on what the Resource actually *is*, what it *affords*, what it *touches / is touched by* (real References to existing Spaces, picked at creation), and what it *offers* — those four facets become the new Resource's starting Categories, so it never starts as an undifferentiated blank page the way a generic Space's Text/List dropdown used to feel. A **Resource Template** (see the Roadmap entry below) can replace the first three of those four with a set tailored to a specific type of thing — a Book's own questions are different from a Riddle's — while the fourth (Touches / Touched By) stays the same for every type, since it's a mechanical capability, not a description. Under the hood it's still an ordinary Space (tagged `"resource"` plus any freely-chosen type tags like `"book"`/`"person"`/`"account"`), which is what gives it full, unconditional access to every other Tool/View/Category/Reference/Trail/Graph/Log capability for free.
- **Categories** — freely-named facets *of a single Space's own topic*, defined per-Space (e.g. a Space about a big purchase might have "Financial Impact," "Risk Tolerance," "Timing"). Distinct from tags, which categorize a Space itself among every other Space (e.g. "resource"). A block can belong to zero, one, or several Categories at once — a Space is trying to catch the pattern of something with several angles at play simultaneously, not sort its content into single, exclusive bins. Categories are managed on the Space page and assigned to blocks there (including right when a block is added); a filter strip lets you focus on one Category at a time without hiding a block's other memberships.
- **Workspaces** — a deliberately assembled, named environment *inside one Space* that bundles whichever Tools belong together for focused, hands-on work — distinct from a Category (a free-standing facet name with no page of its own; a lens on the existing feed) and distinct from a plain Tool/block (one single thing). A Space can have several Workspaces; a Tool can belong to any number of them, or none, and stays visible in the ordinary flat feed either way — joining a Workspace is additive, never a move. Opening one is a real "pop into a tool-specific environment" moment: its own dedicated page, not an inline row, where its assembled Tools get room to work together. Creating/naming/deleting a Workspace, and adding or removing a Tool from one, are all ordinary, always-available actions, same as anything else in this app — no separate mode to switch into, which is what keeps this consistent with the Dev Mode decision below despite Workspaces being reached by navigating to their own page (the same kind of break the Template editor or the Graph page already are, not a gate on ordinary editing).
- **Workspace Kinds** — the eight specialized thinking environments a Workspace can be: **Analyst**, **Critical Thinking**, **Problem Formulation**, **Conceptualize**, **Worldview Assessment**, **Modeling**, **Etymology**, and **Word-Concept Mapping**. A Kind is deliberately *not* a Template: a Space Template seeds an arrangement and then lets go, whereas a Kind keeps shaping its Workspace for as long as it exists — it names the page's own sections, frames each with the question that section exists to answer, leads the add-a-Tool picker with the Tools that kind is built around (and starts the picker on the first of them), and gives the whole page its own look. Without that, an Etymology Workspace would look identical to an Analyst one the moment you finished creating it, which is the exact problem Kinds were built to fix. A Kind *leads and arranges, it never restricts*: any Tool can go in any Workspace, and a Tool that matches none of a kind's sections lands in a final "Also here" group rather than disappearing. Defined in one readable file, `frontend/src/registry/workspaceKinds.js`, in the same spirit as the Tool registries — the database stores only *which* kind a Workspace is (`workspaces.kind`), never what a kind contains. An unkinded Workspace keeps the plain flat feed every Workspace had before Kinds existed.
- **Mapping** — a fourth Tool family alongside General/Work/Time, for Tools about *structure and relation* rather than a single claim: **Word Evolution** (how a term's sense moved over time), **Concept Map** (a referent and every rendering of it in circulation, each marked by how far it sits from the thing itself) and **Model** (the parts something is built from and how they hold each other up). Its own family specifically because none of the three shares Work's `{statement, support, confidence}` shape — `NewBlockForm`'s Work optgroup is built off `family === 'work'`, so filing them there would have created them with the wrong content shape.
- **Dev Mode** — **resolved, no longer a distinct mode.** Earlier brainstorming described a separate mode you'd enter to make structural changes. The person overrode that directly: everything should be addable/removable/editable in place, on every page, all the time — a toggle you have to switch into runs against that. So "Dev Mode" now means *ordinary, ubiquitous editability*, not a gate: structural controls (add/remove/reorder Tools, edit any property) are always visible, everywhere, never hidden behind a switch. Workspaces (above) introduced a genuine dedicated page you navigate to for focused work with a Tool — deliberately reconciled with this decision, not a quiet reversal of it: nothing on the ordinary Space page became less editable in place to make room for it.
- **Relational Map** — where Spaces get connected: simply (checkboxes/text references) and visually, as an interactive node/link thought-map in the spirit of Obsidian's graph view, but livelier. This is also where Relational Spaces (a Space seeded from several others) get managed. A Relational Space is tagged `"relational"` at creation (see `createRelationalSpace` in `backend/src/db/queries/spaces.js`) — an ordinary, freely-removable tag, the same pattern a Resource's `"resource"` tag and a Synthesis's `"synthesis"` tag already use, added by the category-hygiene audit below once it surfaced that a Relational Space previously had no persistent signal at all once created (no origin, no tag) and so was indistinguishable from a Space built by hand the moment you left the Graph page.
- **Log** — a global, cross-Space activity feed: every Space/Tool/Template created or removed, every status change, every entry edited, merged with each Space's own Trail history, in one chronological place — meant as the foundation for trends and data insights over time, not just a record for its own sake. Its per-Space counterpart is **Trail** (see below), which reads the same recorded activity back for one Space alongside that Space's own narrative entries.
- **Work** — the individual acts of thinking a Space is for (assess, question, and more to come) each become their own real Tool, not a generic block with a label. An Assessment is a judgment with supporting rationale and a confidence marker; a Question is an open inquiry with why it matters and how central it feels. Every kind of Work shares that same underlying shape, which is what lets Synthesis (below) pull from them uniformly — but each still reads as its own distinct Tool in the catalog, not a labeled Text block.
- **Synthesis** — compiling a handful of existing Work items, possibly from different Spaces, into a new, more polished piece: an essay, a definition, a story, a plain writing entry. Its own guided flow, reached from its own "+ New Synthesis" entry point on the Dashboard (next to "+ New Resource") — under the hood it's still an ordinary Space (tagged "synthesis" plus an optional freely-chosen kind), pre-seeded with the selected items' own text copied into a starting block to draft from, plus a Reference back to each source Space.
- **Provenance** — whether a Space was brought in from *outside* the app (a Resource — a book, a person, an account) or *produced by the app itself* through thinking done here (a Synthesis, or a Space later judged mature enough to cite elsewhere). Shown as a small badge (External/Internal) on the Space page and in the Dashboard's digests. A mature Synthesis can be explicitly **promoted** to also carry Resource status once it's settled enough to be referenced the way an external Resource is — promoting never changes what a Space *is* (its provenance stays "internal" forever), only that it's now also discoverable as a Resource. This is the app's answer to "Spaces can produce things which turn into tools/resources down the line": Work produces claims, Synthesis compiles them into a real piece, and promotion is the last, optional step that lets a finished piece take its place alongside everything sourced from outside.
- **Insights** — the fuller realization of the Dashboard's founding promise to "see trends/metrics/insights across [Spaces]." Its own dedicated page, made deliberately prominent (a banner on the Dashboard, not just a nav link), reading four facets across every Space at once: what kind of thinking is happening and how settled it feels (Work Type and confidence mix), what themes keep recurring and which conflicts in your own reasoning are still open (recurring Categories, open Tensions), whether thinking is actually moving and which Spaces have gone quiet (weekly activity, staleness), and how much of what exists was brought in versus produced, and how much of that raw thinking has actually been distilled (Provenance split, the Work → Synthesis → Resource funnel). Purely computed from existing data -- no new schema, no new stored fields.
- **Reports** — the single-thing complement to Insights' cross-Space trends: any Space, any Workspace, or any one Tool/Work item (a Hypothesis, a Text block, whatever) can produce a structured-plus-prose snapshot of its own current state, on demand, via an ordinary "Report" button right where that thing already lives -- no separate mode or page. Meant as raw material for two different audiences: Insights already covers the aggregate/trend reading, while a single report is meant to be copied out of the app entirely and handed to an external Claude conversation that can give one Space/Tool/thought closer attention than an in-app view ever could. A Relational Space and a "connection" between two Spaces don't get their own report kind -- a Relational Space is just an ordinary Space (so the Space report already covers it), and a connection is just a Reference block (so the block report already covers that). Purely computed on demand, nothing stored or versioned.
- **Time** — the practical-utility side of the app, threading together everything to do with when: a due date, a checkpoint, actually timing work, looking back on it, and finally seeing all of it in one place. A whole arc, all four layers now built: a real due date on a Space, a Milestone as its own real Tool (a checkpoint with a target date and a reached/not-yet-reached state, distinct from a List item's `reviewBy`), a Session as another real Tool (a timed sitting of work you start and stop, with the elapsed time logged), a Review -- a deliberate, structured look-back at what changed since the last one, reusing Reports' "diff what changed" idea but scoped to "since last Review" instead of "right now," and reusing Trail's own storage rather than inventing a parallel history -- and a fifth Insights facet pulling all three together with due dates: what's coming up, what's overdue, how much time has actually been logged, and which Spaces have gone quiet on reflection specifically, not just on activity in general.
- **Projects** — a real, named piece of work you decided to take on, that a Milestone or a Session belongs to. *Personally initiated*, which is what distinguishes it from a Goal (below): a Goal is a pursuit you notice you're already heading toward. Named "Project," not "Goal," to avoid colliding with a Space's own then-existing `goal` field ("Working toward") — a collision now resolved the other way too, since that field has been replaced by real Goals. A Milestone or Session belongs to at most one Project at a time (unlike a Tool's many-to-many Workspace membership), since a checkpoint or a timed sitting most naturally serves one project. **A Project does not belong to a Space.** Its member entries live in whatever Spaces they were created in, so a Project's Spaces are simply whichever those turn out to be — derived at read time, never stored, which is what lets one Project hold work happening in several Spaces at once with nothing extra to keep in sync. Reached from its own top-level page (`/projects`, a Sidebar entry): the index shows every Project, the Goal it serves, how far it has got, and every Space its work actually happens in; a Project's own page (`/projects/:id`) shows its assigned Milestones/Sessions grouped under the Space each one lives in, a computed progress line (Milestones reached, minutes logged across Sessions), and — since it has no Space of its own to add into — a Space picker governing both "add a new Milestone/Session" and "pull in one already there." A Space page shows, read-only, which Projects its own work feeds; an entry joins one through the picker on that entry itself, which is the only action there is. Starting a Session has its own one-click "▶ Start a Session" affordance on both the Space page and a Project's own page, instead of only the generic "+ Add Entry" dropdown, since starting a timer is more time-sensitive than adding an ordinary entry. A Project card carries a gold `--accent` left-edge stripe, the same "General neutral / Work maroon / Time gold" family-color convention `.block-row[data-family]` already uses.
- **Goals** — a pursuit several Spaces can be working toward at once. In the person's own framing, "projects are personally initiated, goals are revealed as relevant pursuits" — so a Goal deliberately has **no Milestones or Sessions of its own**; give it those and it just becomes a Project with a different name. What it has instead is *reach*: which Spaces are working toward it (`spaces.goal_ids`, a JSON array, the same many-to-many shape `tags` already uses) and which Projects are serving it (`projects.goal_id`). This replaced the single free-text "Working toward" line a Space used to carry, which could only ever hold one thing and could never be shared with another Space. Its own top-level page (`/goals`, a Sidebar entry) lists every Goal with its note, its Spaces and its Projects — and deliberately gives a Goal no dedicated page of its own, unlike a Project: a Project has real content (its assigned entries) that needs somewhere to live, whereas everything a Goal has fits on its card, so a page per Goal would show nothing the index doesn't.

- **Personalization** — the ability to make each thing in the app *look and feel like its own thing*, not just be titled differently. Two halves, deliberately: a **computed default per kind** (every one of the 18 Tool types gets its own distinct look, and a Space gets one from its kind — Synthesis, Resource, Relational, or ordinary) and a **hand override** on top of it, per individual Space or entry. Four dimensions: **Color** (10 accents), **Shape** (the silhouette an entry is drawn with — plain, slab, bracket, rule, notch, inset, underline, tab), **Density** (compact/normal/roomy) and **Typeface** (body/display/mono). Only the override is ever stored, never the resolved value — so an item nobody has touched keeps tracking its kind's default forever, while an item deliberately set to a value that *happens* to match its default is a genuinely different thing (it has stopped tracking). Defaults live in exactly one file, `frontend/src/theme/itemTheme.js`, in the same spirit as the Tool registries: open one file and see every default the app assigns. This replaced the earlier `accent` (`star`/`underline`/`triangle`/`dot`) marker, which the person named directly as unclear — "it's not clear what these do or why they're here" — and the Creation Mode step misleadingly called "Personalize," which only ever set a goal and tags.

- **Durability** — the safety net under everything else: an **Export** of the whole database (JSON, the complete restorable backup; and Markdown, the readable archive for when this app isn't around — both rendered from one payload so they can't disagree), a **Search** across everything actually written rather than just Space titles, and a **Trash** that makes a delete undoable. Reached from the Sidebar, so all three are available on every page. Export is deliberately export-only for now: a restore path is real work (id collisions, merge-versus-replace) and is only needed the day something breaks, by which point the file is what matters.

### Where the build actually stands against that vocabulary

Passes 1–5 of the original roadmap are done, plus a visual-polish pass, an editability pass, a Tools catalog, a Creation Mode overhaul, and the Dev Mode decision above. Mapped onto the language:

- **Tools** = the **Block types** (Text, List, Reference, Media, Comparison) and **View types** (Timeline, Progress, Streak, Ledger, Graph), registered in `frontend/src/registry/blocks.js` and `frontend/src/registry/views.js`. Browsable at `/tools` (`frontend/src/pages/ToolsPage.jsx`) — every Tool's description, a live demo (via each registry entry's `demoBlock`/`demoProps`), and `worksWith` for what it pairs with. (As of the vocabulary-audit pass below, "Block" was never actually chosen from the person's own vocabulary — it was implementation language typed during construction. What the UI now shows for one is **Entry**, and the Text Tool's UI label is now **Writing**, a Skeleton "lane" is now a Skeleton "section" in visible copy. This was confirmed a copy-and-labels-only rename: the registry key `text`, every `*Block.jsx` component/file name, `blockRegistry`, the `blocks` table, `block.type`, and every JSON field name — `skeletonLane` included — all keep their original names, since nothing about the code itself was wrong, only what a person reads on screen. The rest of this document keeps describing the code using its original names, since those are what's actually in the source.) A Block's registry entry also carries `family` (`'general'` or `'work'`, added once the registry grew to 15 Block types and both the catalog and the "+ Add Block" dropdown had become one flat, hard-to-scan list) — the catalog groups by it under "Blocks" (General/Work sub-headings) and `NewBlockForm.jsx` groups by it via native `<optgroup>`s, so a future Work Type needs only its registry entry, no edit to either UI. A Block's registry entry can also carry an optional `workshopComponent` — a bespoke, more spacious version of the Tool that a Workspace page (see the Workspaces vocabulary entry) renders instead of the ordinary `component`, when one exists; most Tools don't have one yet, and fall back to `component` until they get their own redesign pass. **Text** is the first: `frontend/src/blocks/TextWorkshop.jsx` (registered as `text.workshopComponent`) is a real writing environment in place of the small click-to-edit paragraph — always-open, an auto-growing serif writing surface, a live word count, the `quote`/`paraphrase`/`reflection`/`inference` attribution tag made actually settable for the first time (it existed in the data model with no UI anywhere before this), and a rendered "as it reads" preview with wiki-links resolved underneath the raw draft. It deliberately still saves on blur rather than continuously while typing — the `=`/`?`/`!` Skeleton-promotion path (see `saveTextBlockWithPromotion`) rewrites a block's own text out from under it when a line gets promoted, and a background autosave racing that or an in-progress keystroke risked either lost input or a resurrected already-promoted line; one discrete commit per editing session avoids that. The `[[Space]]` link-rendering logic (`renderTextWithLinks`) was pulled out to `frontend/src/blocks/textLinks.jsx` so the ordinary inline Text block and the Workshop version share one implementation instead of two copies drifting apart. **List** is second: `frontend/src/blocks/ListWorkshop.jsx` (registered as `list.workshopComponent`) turns the compact, run-on-line-per-item feed row into a card per item, with real drag-and-drop reordering (native HTML5 DnD, not a hand-rolled pointer tracker -- a plain linear reorder doesn't need the Graph's freeform physics) in place of up/down buttons, and each item's secondary fields (confidence, date, review date, number) as their own small labeled chips instead of run-on inline dashes. It also closes a real gap: a list that starts empty could never gain a checkbox/confidence/date/number field, since a new item's shape was only ever inferred from an existing item -- the Workshop's empty-list shape picker lets the very first item establish that shape explicitly. The item-shape inference logic (`buildNewItem`, `CONFIDENCE_CYCLE`) was pulled out to `frontend/src/blocks/listItems.js`, same reasoning as `textLinks.jsx`, so the ordinary inline List block and the Workshop version can't drift apart on what shape a new item gets. **Reference, Media, and Comparison** followed together in one pass: `ReferenceWorkshop.jsx` gives the note a real auto-growing surface and the trust rating a clear chip row (`unrated`/`high`/`medium`/`low`), plus a live preview card of the target Space itself (title, status, goal, Categories, tags, fetched fresh via `getSpace`) -- so "what this touches" actually shows something about what it touches, not just a name to click through to. `MediaWorkshop.jsx` adds a larger click-to-zoom lightbox and, closing a real gap, an editable source URL (previously a broken or placeholder image URL could never be fixed without deleting the block). `ComparisonWorkshop.jsx` gives each side its own card in a clearer side-by-side grid, a chip-style contrast toggle with a real auto-growing note surface, and a "swap sides" button; each side still renders through the ordinary `TextBlock`/`ReferenceBlock` (via the same `onSave` override they already supported for embedded, non-standalone content) rather than through `TextWorkshop`/`ReferenceWorkshop`, since those expect a real block id a Comparison side doesn't have. **TextWorkshop was substantially redesigned again in a later pass**, from one shared attribution tag for the whole block into discrete, individually-tagged lines: a Text block's content is now `{lines: [{id, text, tag}]}` rather than `{tag, text}`, so each line -- not just the whole paragraph -- carries its own `quote`/`paraphrase`/`reflection`/`inference` tag, set independently via a small margin glyph per line. Enter commits the line being edited and opens a fresh blank line after it; blur commits without adding one. The ordinary inline `TextBlock.jsx` stays deliberately simple about this: it still edits as one joined multi-line textarea and re-splits the result back into lines on save, matching each line to its old self by exact text (`relineFromText`) so an untouched line keeps its id and tag -- only a changed or new line loses its old attribution, which is the honest default (a changed sentence's old attribution may no longer be right). Comparison's embedded sides keep the older `{tag, text}` shape forever, since they're plain objects embedded in a `comparison` block's own content, never real `type='text'` rows -- `TextBlock.jsx` detects which shape it was handed (`legacy = !block.content.lines`) and renders either. Every Text block is created through one function, `createBlock` in `backend/src/db/queries.js`, which normalizes whichever shape it's handed into `{lines}` via a private `normalizeTextContent` helper -- so no frontend call site that creates a Text block (a Template, `ensureSkeletonLanes`, `NewBlockForm`, `CreateResource`, seed data) needed to change for this; a one-time `migrateTextBlockLines()` backfill (run at server startup) upgrades any pre-existing rows still on the old shape. TextWorkshop also gained Focus Mode (see the Workspaces entry below) and a per-line "File" action that copies a line's text into a Skeleton lane without removing it from the Writing Surface -- see the Skeleton entry below, a deliberately different verb and outcome from typed `=`/`?`/`!` shorthand, which still promotes a line out of the block entirely.
- **Spaces' 5 clusters** = originally the 5 built-in Templates (Inquiry/Analytical, Technical/Practical, Life Management, Person-Reflection, Creative), now 6 with **Problem Formulation** (see the Work vocabulary entry above and `backend/src/db/seedTemplates.js`) — each a named starting arrangement of Tools. "Start Blank" in Creation Mode already serves as the "Other" option. Problem Formulation seeds a blank Writing Surface, a "Lenses to Consider" List pre-filled with the person's own habitual reading disciplines (Phenomenology, Philosophy, History, Etymology, Anthropology, Epistemology, checked off as actually applied rather than required), and a blank Formulation block -- added the same way every prior Template addition has been, via `seedTemplates()`'s own idempotent per-id check, so restarting the server inserts just the new row without touching the existing 5 or requiring a fresh database.
- **Resources** = a Space carrying `"resource"` in its `tags` array (a plain JSON array on `spaces` — see Data model). Extra tags (`"book"`, `"person"`, `"account"`, `"lens"`, ...) already work for sub-typing with zero new code. Surfaced in the Dashboard's Resources digest and selectable in ordinary Creation Mode. Creating one goes through its own page, `frontend/src/pages/CreateResource.jsx` (routed at `/resources/new`, its own "+ New Resource" button on the Dashboard next to "+ New Space") — not the cluster-card Creation Mode a Space uses. It asks four guided questions (what it is / what it affords / what it touches or is touched by / what it offers), lets you pick any existing Spaces it relates to (each becomes a real Reference block, with an optional note, right away), and on submit calls the same `createSpaceWithSetup` every Space creation path uses — just pre-seeded with those four question names as the new Space's starting `categories` and one starter block filed under each. `createSpace`/`createSpaceWithSetup`/`POST /spaces` all accept an optional `categories` array now (previously Categories could only be set after creation via `PATCH /spaces/:id`) specifically so a guided flow like this can seed them before any block exists. **Resource Templates** = a new `resource_templates` table (id, type, label, facets -- see Data model), a deliberately separate mechanism from ordinary Templates, confirmed via direct question over reusing the `templates` table -- a Resource Template *replaces* CreateResource.jsx's three generic descriptive facets (What It Is / What It Affords / What It Offers) with a type-tailored set of its own once a chosen type tag matches one (checked in order, first match wins; falls back to the generic three for an unrecognized type). The fourth, structural facet (Touches / Touched By) is never part of a Template's own `facets` -- it stays universal across every type. `backend/src/db/queries/resourceTemplates.js` mirrors `templates.js`'s own CRUD shape exactly; `backend/src/db/seedResourceTemplates.js` seeds all 17 named types (Book, Lecture, Article, Art Piece, Lesson, Parable, Video, Seminar, Website, Song/Musical Piece, Aphorism, Debate, Essay, Film, Poem, Story, Riddle) in one pass, each with its own genuinely distinct three facets written for that type -- confirmed via direct question to build the whole named list at once rather than starting with 2-3 to prove the pattern first, the discipline every other type rollout in this app (Work Types) had used until now. Managed on its own page, `frontend/src/pages/ResourceTemplatesPage.jsx`/`ResourceTemplateEditor.jsx` (routed at `/resource-templates`, `/resource-templates/new`, `/resource-templates/:id/edit`) -- reached via a contextual link from the ordinary Templates page and from CreateResource.jsx itself, not its own top-level Sidebar entry, since it's a configuration page for one specific creation flow, not a top-level destination. `CreateResource.jsx` looks up a matching template fresh, by type, every time -- editing or deleting a Resource Template only ever affects Resources created from that point on, exactly the same "no live-cascading system" property ordinary Templates already have.
- **Creation Mode**, as actually built = visual cluster cards (with a live preview of what each starts with), the ability to add extra Tools and pull in existing Resources as References before the Space exists, an optional "Workspaces" step to name starting Workspaces and file new Tools into them right away, and tags/goal set at creation — see `frontend/src/pages/CreateSpace.jsx` and `createSpaceWithSetup` in `backend/src/db/queries.js`. The Workspaces step names Workspaces as plain draft strings (they don't exist as real rows until the Space itself is created); `NewBlockForm.jsx`'s optional `workspaceNames` prop lets a new Tool be filed into one of those draft names, which `createSpaceWithSetup` resolves into real Workspace ids once the Workspaces are actually created, in the same request. Scoped to ordinary Space creation only, not `CreateResource.jsx` — a Resource's four-facet Category flow is already its own guided structure, and stacking Workspace-assembly onto it risked diluting that; extending it there is a possible, explicitly separate follow-up.
- **Dev Mode** — see the resolved entry above. Every page's edit surfaces should be checked against this whenever new content is added to the app: if something can be created, it needs a way to be removed or changed too, without a mode switch.
- **Categories** = `spaces.categories` (a per-Space JSON array of freely-named strings, managed by a `CategoryManager` component on the Space page) plus `properties.categories` on individual blocks (many-to-many — see Data model). Built in response to the Space page feeling "too modular" and generic (a bare Text/List dropdown, no sense of what belongs together or what follows what): a new block can now be filed under one or more of the Space's own Categories right at creation (in `NewBlockForm.jsx`, when a `categories` prop is passed), an existing block's Category membership is toggled from a chip picker inline in the feed, and a filter strip above the feed ("All" plus one tab per Category) lets you zoom in on a single facet while the unfiltered view still shows every block's full Category chips, so overlapping membership stays visible rather than hidden.
- **Workspaces** = a new `workspaces` table (id, space_id, name — see Data model) plus `properties.workspaces` (an array of workspace ids, many-to-many) on individual blocks. Managed from a "Workspaces" section on the Space page (`WorkspaceList` in `frontend/src/pages/SpacePage.jsx`) — create one by name, open one via its card. Its dedicated page is `frontend/src/pages/WorkspacePage.jsx`, routed at `/spaces/:spaceId/workspaces/:workspaceId`: shows the Tools assembled into it, lets you add a brand-new Tool straight into the Workspace, pull in a Tool that already exists elsewhere on the Space, remove a Tool from the Workspace (without deleting it), or delete the Workspace itself (which only ever removes the Workspace row — its Tools stay on the Space, same as removing a Category never deletes what was filed under it). A block's Workspace chips also show inline in the ordinary feed (`BlockWorkspacePicker`), same visibility principle as Category chips. Backend: `backend/src/routes/workspaces.js` (list/create under `/spaces/:spaceId/workspaces`, get/rename/delete under `/workspaces/:id`), `updateBlockWorkspaces` and the `--- Workspaces ---` section in `backend/src/db/queries.js`. A Tool gets a bespoke in-Workspace redesign one at a time (see the Tools entry above for how that's registered) rather than all at once — the only thing still open here. `WorkspacePage.jsx` also gained Focus Mode: a `focusedBlockId` bit of page state that, when set, hides every sibling block and the page's own secondary chrome (title/subtitle, the applicable-Views grid, the Remove-from-Workspace/Delete-block controls, the Add/Pull-in/Delete-Workspace sections) and shows just the one focused block, enlarged. `focused`/`onFocus` props are passed uniformly to every block's rendered Component regardless of type, though only `TextWorkshop`'s own "⤢ Focus" toggle currently reads them — "nothing but prose to look at" was the specific request behind it.
- **Relational Map** = the Graph page (`/graph`), computed live from Reference blocks, Workspaces, *and* Projects (no separate graph structure is stored). `frontend/src/views/GraphView.jsx` is a real interactive map: a small hand-rolled force simulation (repulsion between nodes, springs along edges, no external library) that runs continuously, drag to reposition a node (it rejoins the simulation on release, positions aren't persisted -- same spirit as Obsidian's own graph view), drag the background to pan, scroll to zoom, click a node to open that Space, Workspace, or Project, hover to highlight its connections. Three node kinds share the map: a Space (circle), a Workspace (small square, drawn in Work's own maroon), and a Project (small diamond, drawn in the Time family's own gold `--accent` instead -- added once an outside-review audit found Projects had no Graph presence at all, unlike Workspaces, with no reason on record; see the Roadmap entry below). Three edge kinds: a `reference` edge between two Spaces (solid, arrowed, same as before), a `contains` edge from a Space to one of its own Workspaces, and a `contains-project` edge from a Space to a Project its own entries feed (one per Space a Project has work in, since a Project belongs to no single Space -- a Project with nothing assigned yet doesn't appear on the map at all, having nothing to attach to; its node is anchored at the first such Space via a computed `primary_space_id`, a placement hint rather than a stored column) (both containment kinds short, dashed, unstyled -- pull the contained node in tight against its parent rather than letting it drift like an ordinary connection). Node ids are namespaced (`space:<id>` / `workspace:<id>` / `project:<id>`) in `getGraphData()`'s consumer, since ids come from three different tables. Only `reference` edges count toward a Space's visual size (its circle grows with reference degree); a Space's own Workspaces/Projects don't inflate that, since containment is a different kind of relationship than being referenced.
- **Log** = `/log` (`frontend/src/pages/LogPage.jsx`), backed by `listGlobalActivity`/`getActivityStats` in `backend/src/db/queries.js`. Merges the `activity_log` table (Space/Tool/Template created or removed, status and due-date changes, entries edited, Milestones reached) with the existing `trail_entries` into one feed, plus a first simple set of stats (total events, last 7 days, most active Space). Content edits were deliberately left out originally, on the reasoning that logging every one would bury the events worth seeing trends in; that turned out to cost more than it saved (see the Trail auto-tracking Roadmap entry) and they're recorded now, but *coalesced* — repeated edits to the same entry inside ten minutes bump one row's `event_count` rather than inserting another, so a long writing session reads as one line saying `×5` rather than five lines. A change with a real, nameable implication (a Milestone reached, a Session completed) is never coalesced: it happened once, and saying so twice would be wrong.
- **Skeleton** (the Premises/Evidence/Open Questions/Tensions lanes seeded by `ensureSkeletonLanes`) gained two capabilities in the same pass as the Writing Surface redesign above, both reachable from inside a Workspace. First, a second way to get a line of prose into a lane: alongside typed `=`/`?`/`!` shorthand (which still *promotes* a line, removing it from the Writing Surface -- `saveTextBlockWithPromotion`), TextWorkshop's per-line "File" button *copies* a line's text into a chosen lane (Premises, Evidence, or Open Questions -- never Tensions itself, which is built by pairing, not filing) as a new tentative item, leaving the Writing Surface untouched (`fileLineInLane` in `backend/src/db/queries.js`, `POST /spaces/:id/skeleton/file`). Confirmed directly with the person which behavior was wanted (copy-and-leave, not cut-and-file) rather than assumed. Second, a Tension item can now name the actual two claims in tension, not just describe the tension in prose: `createTensionPair` (`POST /spaces/:id/skeleton/tensions`) adds a Tensions-lane item carrying `statementA`/`statementB`, each a `{blockId, itemId}` pointer at an item in any of the three claim-bearing lanes. `ListWorkshop.jsx`'s `TensionBuilder` is the picker for this: pick one claim, then a second, confirm a label, done. Resolution is live, not snapshotted -- `ListWorkshop` fetches the Space's other blocks (`getBlocksForSpace`) and looks up each pointer fresh on every render, so editing a source statement's text updates every Tension pairing it's part of automatically, with no separate sync step needed. Both new backend endpoints live in a new `backend/src/routes/skeleton.js`, registered in `backend/src/index.js`. Voice capture (raised in the same design corpus this pass drew from) was deliberately left out -- the most speculative and environment-dependent idea raised -- flagged to the person rather than silently dropped.
- **Trail** = `trail_entries` (kind `auto`/`manual`, `summary`, `note`, a full `skeleton_snapshot` taken at write time), managed by `TrailSpine.jsx` on the Space page. Two gaps closed in a later pass, against a wireframe batch that pictured Trail with two capabilities the built version didn't fully have yet. First: entries used to be write-once. `updateTrailEntry` in `backend/src/db/queries.js` (`PATCH /spaces/:id/trail/:entryId`) lets an auto entry get a manual "why" attached after the fact, or a manual entry's own note edited later -- a manual entry's `summary` (the truncated preview the Log page shows) is recomputed to match whenever its note changes; an auto entry's `summary` is left alone, since a note added there is commentary layered on top of what already wrote itself, not a replacement for it. Second: Rewind (viewing the Skeleton exactly as it stood at an earlier entry) already existed from an early pass, just as a plain inline expand-in-place toggle rather than the side-by-side comparison the wireframe showed -- confirmed with the person that this was worth restyling rather than leaving alone. It's now an explicit **Now vs. As-of** comparison with a "Return to Now" close button, both columns rendering through the same `getSkeletonSnapshot` (now exported from `queries.js`, not private) a stored snapshot was originally built from, via a new `GET /spaces/:id/skeleton/current` route -- so Now and As-of can never drift apart by going through two different readings of the same data. Rewind stays read-only throughout: "Return to Now" only closes the comparison, it never mutates anything. A later pass (see the Roadmap entry) fixed the thing that made all of the above moot in practice: Trail only ever wrote itself on a *Skeleton* edit, so on a Space where the person never used the promotion shorthand it recorded nothing at all -- the real database held 0 Trail entries across 51 Spaces created and 98 entries added. `listSpaceHistory` now returns a Space's `trail_entries` **and** its own `activity_log` rows in one chronological list, each tagged with a `source` (`'trail'` | `'activity'`), which is the mirror of what `listGlobalActivity` already did for the Log. The two kinds stay visibly different because they genuinely are: a Trail row carries a Skeleton snapshot and an editable note, so it expands and offers Rewind; an activity row is a recorded fact with neither, so it stays one flat line rather than offering a comparison it couldn't produce.
- **Work** = eleven Block types so far -- `assessment`, `question`, `analysis`, `deduction`, `definition`, `demonstration`, `insight`, `implication`, (added in a later, "surgical granularity" pass) `hypothesis`, `objection`, and (added at the person's request, alongside the Resources vocabulary entry's `lens` tag and Templates' new Problem Formulation cluster below) `formulation` -- registered in `frontend/src/registry/blocks.js` alongside the original five -- the first Tools built specifically for one thinking-act rather than as a general-purpose block. Every kind shares one underlying shape, `{statement, support, confidence}`, implemented once in `frontend/src/blocks/WorkBlock.jsx` -- each Tool (`AssessmentBlock.jsx`, `QuestionBlock.jsx`, `AnalysisBlock.jsx`, `DeductionBlock.jsx`, `DefinitionBlock.jsx`, `DemonstrationBlock.jsx`, `InsightBlock.jsx`, `ImplicationBlock.jsx`, `HypothesisBlock.jsx`, `ObjectionBlock.jsx`, `FormulationBlock.jsx`) is a thin wrapper that just names the statement field and the support list's heading for its own kind and passes everything else through: Assessment "Rationale", Question "Why this matters", Analysis "Breakdown", Deduction "Reasoning", Demonstration "Walkthrough", Insight "What led to it", Implication "What suggests it", Hypothesis "What would test this", Objection "What this challenges", Formulation "Grounds". Definition is the one exception to "statement = the Tool's own name" -- a definition is inherently asymmetric (a short term, a longer meaning), so its statement field holds the term and the meaning itself lives in what every other Work Type calls its support list, relabeled "Definition". Formulation is a second, different kind of exception: unlike every claim built from direct observation, a Formulation's statement is explicitly *derived through an interpretive lens* (etymology, phenomenology, anthropology, history, epistemology, philosophy, ...) rather than observed directly -- what the person described as their own habitual first move when starting to think about something: naming what a phenomenon or an utterance/behavior/stated value fundamentally *is*, and what worldview would have to be true for it to make sense, before Assessment/Question/Analysis even have something settled enough to work with. A Formulation's Grounds points can link live to a claim surfaced by whichever lens was applied, the same `SupportLinker` every other Work Type already uses. Confirmed via direct question before the first two were built: real distinct Tools sharing one skeleton (not a generic block with a label), and only Assessment and Question to start, establishing the pattern before the rest rolled out. The second four (Analysis, Deduction, Definition, Demonstration) were built together once the pattern had proven out; Insight and Implication followed as a smaller pass once the person asked to keep going through their original list -- most of the rest of it (plan/outline, derive/infer, explain/expound, ...) either already mapped onto an existing Tool or was a near-duplicate of one already built, walked back as a recommendation via direct question rather than built wholesale. `support` was originally a single `rationale` prose blob; redesigned in the same "surgical granularity" pass that added Hypothesis and Objection into a list of discrete points, each either its own short free-text claim or a *live link* to another existing claim (another Work block's statement, or a Skeleton lane item) -- resolved fresh on every render the same way a Tension's `statementA`/`statementB` pointers already were, so editing the linked claim updates every place that links to it automatically. `WorkBlock.jsx`'s `SupportLinker` is the picker for this (candidates: every other Work block in the Space, plus every item in a claim-bearing Skeleton lane); a block created before this redesign is upgraded transparently by `normalizeWorkContent`/`migrateWorkItemSupport` in `backend/src/db/queries.js`, same approach the Text-lines migration already established. Hypothesis (a claim proposed to test, not yet believed) and Objection (a targeted challenge to another claim) were the two genuine gaps confirmed via direct question once support-linking existed to make Objection viable without a dedicated pointer field of its own -- what an Objection challenges is just a linked support point, keeping it consistent with the shared shape. Confidence was widened in the same pass from 3 levels to 5 (`questioned`/`tentative`/`moderate`/`solid`/`certain`, see `CONFIDENCE_LEVELS` in `frontend/src/registry/blocks.js`), shared by List items and Work items alike as it always has been -- the three original words were kept as-is so no stored value needed migrating. Addable through the ordinary "+ Add Block" form (`NewBlockForm.jsx`) alongside Text and List, since none of them need the kind of external input Reference/Media/Comparison do -- `WORK_TYPE_STARTER_PROMPTS` in `NewBlockForm.jsx` is the one place a new Work Type's starting-text placeholder needs adding. That same form's "Compare Work Types" panel (see the Roadmap entry below) lists every Work Type's label and description at once, registry-driven off the same entries -- a future Work Type needs no edit there either. `backend/src/db/queries.js`'s `WORK_TYPES` is the one list of which block types count as Work -- adding a future kind means adding it there and to the frontend registry; `listWorkItems()` and Synthesis's picker pick it up automatically, nothing else needs to change (confirmed live at every rollout so far: each new type shows up in the Tools catalog, the "+ Add Block" form, and Synthesis's picker with zero changes to Synthesis's own code).
- **Synthesis** = its own guided creation flow, `frontend/src/pages/CreateSynthesis.jsx` (routed at `/synthesis/new`, its own "+ New Synthesis" button on the Dashboard next to "+ New Resource"), modeled directly on Resource creation -- confirmed via direct question that the compiled piece should become a new Space (so it gets its own Categories/Trail/References), not a block bolted onto an existing one. Picks from every existing Work item across every Space (`GET /work-items`, backed by `listWorkItems()` in a new `backend/src/routes/work.js`), grouped by source Space, multi-select. On submit, `createSpace` (the same function every creation path already uses) is called with `tags: ['synthesis', ...optional kind]` and `extraBlocks`: one Reference block per distinct source Space (for backlinks/Graph, the same machinery every Reference already gets for free), a "Source Material" Text block whose lines are the selected items' own statement text copied in -- one line per item, labeled by kind -- so there's real content to draft from immediately, and one blank Text block to actually write the piece in. Surfaced on the Dashboard the same way Resources are: `SynthesesDigest`, reading `listSpacesByTag('synthesis')` (already general-purpose, not Resource-specific, so this needed zero new backend query). Every one of those three starter blocks now also carries a real Category (`Drawn From` / `Source Material` / `Draft`), mirroring CreateResource.jsx's own starter blocks -- see the Roadmap entry below for why. `SYNTHESIS_KIND_SUGGESTIONS` widened from 4 to 10 named Kinds, plus a free-text `+ custom kind` input matching CreateResource.jsx's own type-tag entry, so a Kind is never capped by the suggestion list again.
- **Provenance** = `spaces.origin` (`'external'` | `'internal'` | null, see Data model). Set once at creation, never editable afterward: `CreateResource.jsx` passes `'external'`, `CreateSynthesis.jsx` passes `'internal'`, ordinary Creation Mode leaves it null. `OriginBadge` on the Space page (`SpacePage.jsx`) shows it as a small pill next to the status pill, colored with `--accent` when internal (the same "added/produced" color Visual Identity's manual layer already uses). `PromoteToResource`, also on the Space page, appears only when a Space is an as-yet-unpromoted Synthesis (`origin === 'internal' && tags.includes('synthesis') && !tags.includes('resource')`) and, on click, does nothing more than add the `'resource'` tag via the ordinary `updateSpace` -- no new Space, no content change, no new backend endpoint, since the Dashboard's Resources digest already reads tag membership. A promoted Space then shows up in both `ResourcesDigest` and `SynthesesDigest`, each badging it (`Internal` / `↑ Resource` respectively) so a produced-then-promoted piece never gets mistaken for something sourced from outside. Deliberately scoped to promoting a whole Synthesis only, not a raw Work item directly -- Synthesis is already the maturation step from scattered claims into one real piece, so promotion is the second, later step on top of it, not a competing path.
- **Insights** = `/insights` (`frontend/src/pages/InsightsPage.jsx`), backed by one combined `GET /insights` endpoint (`backend/src/routes/insights.js`) bundling four query functions from a new `--- Insights ---` section in `backend/src/db/queries.js` -- `getWorkMixInsights()` (Work item counts by type and by confidence), `getThemeInsights()` (Category names recurring across more than one Space, plus every open Tensions-lane item across every Space), `getActivityTrendInsights()` (a weekly count of every `activity_log`/`trail_entries` event over the last 8 weeks, plus which Spaces haven't been touched in 30+ days regardless of their manually-set status), and `getProvenanceInsights()` (the `spaces.origin` breakdown, plus counts for the Work -> Synthesis -> Resource funnel: total Work items, how many were compiled into a Synthesis, how many of those were promoted to Resource status). Mirrors the same "one combined payload" pattern `GET /activity` already established for the Log page, and reuses the app's existing hand-rolled `.progress-track`/`.progress-fill` bar visual (via a small `BarRow` component in `InsightsPage.jsx`) rather than introducing a charting library, consistent with every other visualization in the app. `getProvenanceInsights()`'s Work -> Synthesis step now reads real item-level lineage (`distilledWorkItemCount`, the count of distinct Work items actually used in a Synthesis) rather than the number of Synthesis pieces that happen to exist -- see the Roadmap entry above for how "which claims fed which Synthesis" went from unanswerable to a real query. Made prominent per the person's explicit request: an ordinary nav-link entry alongside Tools/Log/Graph, plus a visually distinct banner (`.insights-banner`) near the top of the Dashboard, the one deliberately loud element on a page that otherwise reads as calm and utilitarian by design. Every facet (the fifth, Time, included) also returns a `reading` -- one computed sentence naming what's actually notable in that facet's own numbers, not just the numbers themselves (see the Roadmap entry below for why and how).
- **Reports** = one shared frontend component, `frontend/src/components/ReportButton.jsx`, dropped into every level that can produce one: a "Space Report" button on `SpacePage.jsx`, a "Workspace Report" button on `WorkspacePage.jsx`, a "Project Report" button on `ProjectPage.jsx` (added once an audit found Project was the only one of the three grouping concepts with no page-level report at all -- see the Roadmap entry below), and a plain "Report" button next to every block in both pages' feeds (covering every Block type uniformly, a Work item like a Hypothesis included, since they're all just rows in the same `blocks` table). Fetches lazily, only once opened -- same on-demand pattern Trail's own Rewind comparison already established. Backed by four query functions in a new `--- Reports ---` section of `backend/src/db/queries.js` -- `getSpaceReport(spaceId)`, `getWorkspaceReport(workspaceId)`, `getProjectReport(projectId)`, `getBlockReport(blockId)` -- each returning one shared shape, `{ level, id, label, generatedAt, sections: [{ heading, lines }] }`, and four matching routes (`GET /spaces/:id/report`, `GET /workspaces/:id/report`, `GET /projects/:id/report`, `GET /blocks/:id/report`) that each pair the structured report with a prose narrative rendered by one small shared function, `renderReportText()` in a new `backend/src/reportFormat.js` (pure text formatting, no database access, kept separate from queries.js the same way `textLinks.jsx`/`listItems.js` already separate rendering from data). Keeping that shape identical across all four levels is what lets one renderer serve all of them instead of four near-duplicates -- the same "structured data now, prose rendered from it" split Insights' own charts already use. The panel itself shows the prose narrative by default with a raw-data JSON toggle and a Copy-to-clipboard button, since getting the text out of the app cleanly is the entire point.
- **Time** (Due dates, Milestones, Sessions, Review, and Insights surfacing) = `spaces.due_date` (nullable `'YYYY-MM-DD'`, see Data model), editable in place via a `DueDate` component on `SpacePage.jsx` (a native `<input type="date">`, same principle as `WorkingToward`) and shown with an `isOverdue` badge wherever a Space appears with a due date, including the Dashboard's own Space list. `isOverdue` is computed fresh in `withComputedSpaceFields` in `backend/src/db/queries.js` (`due_date` in the past, independent of status -- same reasoning Insights' staleness check already established) rather than stored, so it's always correct without any write-time bookkeeping. **Milestone** is a new Block type -- `frontend/src/blocks/MilestoneBlock.jsx`, content `{label, targetDate, reached, reachedAt, note}` -- registered in `blockRegistry` with a third `family` value, `'time'` (alongside the existing `'general'`/`'work'`), since a checkpoint's own operational state doesn't fit either existing family: it's not a general-purpose container and it's not a claim sharing Work's `{statement, support, confidence}` shape. `family: 'time'` needed the same three touch points `family: 'work'` already established when it was added -- `ToolsPage.jsx` (a third "Time" sub-heading), `NewBlockForm.jsx` (a third `<optgroup>`, registry-driven the same way Work's is) -- plus one thing Work's uniform shape didn't need: `NewBlockForm.jsx`'s `handleSubmit` needed its own explicit branch for Milestone's content shape, since "Time" isn't one shared shape the way Work is (documented directly in that file's own comment, so a future non-uniform Time Type doesn't get missed the same way). **Session**, added right after Milestone in the same pass, is the second Time Type and the first genuinely *live* Tool in the app -- `frontend/src/blocks/SessionBlock.jsx`, content `{label, startedAt, endedAt, durationMinutes, note}`. Start/Stop write `startedAt`/`endedAt` (client clock, same "browser's own clock is good enough for a single-user app" reasoning `reachedAt`/`todayString()` already use); while running, elapsed time is derived fresh from `startedAt` on every render (via a `setInterval` that only ticks the component's own display, never writes anything) rather than kept as a separately-ticking counter, so a session that's been running since before the tab was closed still reads correctly the moment it's reopened -- the persisted `startedAt` is the only source of truth. One Session block is one sitting; logging another sitting means adding another Session block, same granularity Milestone and Work items already use. Needed only Session's own `handleSubmit` branch in `NewBlockForm.jsx` -- `ToolsPage.jsx`'s "Time" section and the "+ Add Block" dropdown's Time `<optgroup>` are both already registry-driven, so neither needed touching. `getSpaceReport`/`getBlockReport` in `backend/src/db/queries.js` (see the Reports vocabulary entry) gained a Session case each -- a Space's report now also surfaces a Sessions section (count + total minutes logged), and a Session's own block report summarizes its running/completed status and timestamps -- continuing the same "thread Time into Reports" approach Milestone established. **Review** is the Time arc's third layer, and deliberately not a new concept alongside Trail -- it's a third `trail_entries.kind` value (`'review'`, alongside the existing `'auto'`/`'manual'`), so it gets Trail's storage, its full skeleton snapshot, Rewind, and the existing note-attaching path (`updateTrailEntry`) entirely for free: a Review's own kind isn't `'manual'`, so its auto-computed summary is left alone when a note gets attached, same as an `'auto'` entry's already was. `getReviewDraft(spaceId)` (`backend/src/db/queries.js`) is a read-only preview of what a Review would say right now -- blocks added, Milestones reached, and Sessions completed (with total minutes) since the last Review (or since the Space was created, for the first one) -- and `createReview(spaceId)` writes that exact same computed text as a new Trail entry via `logTrailEntry`, so the preview and the permanent record can never disagree. `GET /spaces/:id/reviews/draft` and `POST /spaces/:id/reviews` are the two new routes; `ReviewStarter` in `frontend/src/trail/TrailSpine.jsx` is the "Start a Review" -> preview -> "Log this Review" flow, sitting right above the ordinary Trail list since a logged Review then renders through the exact same `TrailEntryRow` as any other entry (its own "Review" label instead of "Auto"/"Note"). `LogPage.jsx`'s `KIND_LABELS` gained a matching `trail_review: 'Review'` entry so it reads correctly on the Log page too, for free, since the Log already merges every Trail entry generically. One accepted precision limit, stated directly in `getReviewDraft`'s own comment: a Milestone's `reachedAt` is date-only, so one reached earlier the same day as the last Review can't be told apart from one reached just after it -- not worth a finer-grained timestamp just for this comparison in a personal, single-user app. **Surfacing** is the Time arc's fourth and final layer: a fifth Insights facet, `getTimeInsights()` in `backend/src/db/queries.js`, added to the same combined `GET /insights` payload the other four facets already share (`insightsRouter` in `backend/src/routes/insights.js`) and rendered by a new `TimeSection` in `InsightsPage.jsx` -- placed last, after Provenance & Synthesis yield. It reads Spaces with a `due_date` set (split into overdue/upcoming), every Milestone across every Space (reached count plus which ones are overdue -- past their `targetDate`, still unreached), every Session (total minutes logged across completed ones, plus how many are currently running), and Review recency per Space (which have never been reviewed at all, and which haven't been reviewed in `reviewStaleThresholdDays` (14) days -- a genuinely different question from Insights' own Activity & staleness facet, which measures general activity, not deliberate reflection specifically). Needed zero new CSS: every visual piece (`insight-columns`, `insight-plain-list`, `insight-funnel`/`insight-funnel-number`, `insight-empty`, `insight-detail`) already existed from the first four facets -- the one addition, `.insight-columns + .insight-columns { margin-top: ... }`, was a small gap fix for Time being the first facet to stack two rows of columns, not new visual language.

If the person mentions older brainstorming names (Skeleton, Trail, Tension Resolver, Argument Map, Citation Tracker, Milestone Tracker, Habit Streak), these are already built as Tools — Skeleton lanes and Trail have their own files (`registry/skeleton.js`, the Trail spine on each Space page); the rest map onto Views over List Tools. Check before assuming a familiar name means new work.

## Visual direction
Done, not a future pass anymore. The shipped look is a matte-black/oxblood dark theme — Fraunces (display serif), Source Serif 4 (body serif), JetBrains Mono (utility mono) — defined in `frontend/src/index.css`. This superseded an earlier "vellum/ink" plan after the person reviewed a mockup and preferred the darker direction. The Visual Identity glyph (an organic branching mark per Space, computed from status/reference count/open tensions — never decorative) is real and themed to match. Widened in a later pass to react to two more already-computed dimensions, overdue status and Milestone-reached ratio — see the Roadmap entry below.

**Retired** (see the personalization/theming Roadmap entry below -- kept here as the record of what the glyph used to carry): a fourth, deliberately different layer was added on top of those three computed dimensions in a later pass -- a manual accent (`star`/`underline`/`triangle`/`dot`), hand-picked per-Space from a small chip-toggle picker on the Space page (`AccentPicker` in `frontend/src/pages/SpacePage.jsx`) and persisted as `spaces.accent`. `SpaceGlyph.jsx` draws it in its own color (`--accent`, a warm gold, distinct from the maroon the computed shape uses) so it always reads as an added mark, never a change to what the glyph is actually reporting about the Space — the manual layer sits *on top of* the computed base, it never replaces it. Since it's drawn inside `SpaceGlyph` itself, it shows up for free at every scale that component already renders at (the Dashboard card, the Space-page header) with no per-page work. The person named this directly as unreadable ("its not clear what these do or why they're here"), and it was replaced by the four-dimension theme system -- which serves the same "make this Space mine" purpose far more legibly, and now also drives the glyph's own stroke color. `AccentPicker` and the glyph's accent-drawing code are gone; the `spaces.accent` column stays in place, unused and annotated, rather than being dropped in a destructive migration.

**Tool/section personality**, added in a later "professionalism pass" (see Roadmap): a grounded audit (screenshotting the real Tools catalog, a real Space page, the Dashboard) found that every Block type, every View type, and every Dashboard digest rendered as a visually identical box — differentiated only by reading its text, not by looking at it. Fixed with two additions to `blockRegistry`/`viewRegistry` (`frontend/src/registry/blocks.js` / `views.js`): `icon`, a single restrained text glyph per Tool (no emoji -- `¶` Text, `⚖` Assessment, `◆` Milestone, and so on for all 22 Tools), and the existing `family` field pressed into double duty as a coarse color grouping. Rather than invent a third hue, family color reuses what the palette already means: General stays neutral (the foundational, unmarked family), Work gets `--maroon` (the app's own primary color, fitting since Work -- the thinking-act Tools -- is literally the app's central activity), and Time gets `--accent` (the gold already reserved for Visual Identity's manual layer and for Time's own reached/running states, so this doesn't introduce a competing meaning for gold, it extends the existing one). Rendered as a left-border stripe on `.tool-card` (`ToolsPage.jsx`) and on every `.block-row` (`SpacePage.jsx`/`WorkspacePage.jsx`, via a `data-family` attribute read from the registry at render time -- no change to any individual Block component), plus a small `.block-type-tag` (icon + label) above each block in the feed. That tag closes a real, separate gap found in the same audit: a Work block's own kind (e.g. "Assessment" vs. "Question") was previously never rendered anywhere at all -- `WorkBlock.jsx`'s `statementLabel` prop was only ever used as placeholder-hint text, not a visible label. The same personality idea extends to the Dashboard's five digests (`Dashboard.jsx`): each gets its own icon and, for the two with an existing color meaning elsewhere, a matching accent -- Overdue for review borrows the same maroon `.overdue-badge` already uses (this *is* the same "overdue" concept), Syntheses borrows the gold Provenance already uses for "produced by the app itself." The other three digests (recent Trail activity, resurface suggestion, Resources) stay neutral, icon-only.

**Dialogs and interaction polish**, the professionalism pass's second slice: the app's only two calls out to native browser chrome (`window.confirm`, `window.prompt` -- 6 call sites in total) are gone, replaced by a themed `ConfirmDialog` (`frontend/src/components/ConfirmDialog.jsx`), mounted once at the app root (`App.jsx`) via a `ConfirmDialogProvider` and reached from any page through a `useConfirmDialog()` hook exposing `confirm(message)` (plain yes/no) and `promptToMatch(message, matchText)` (the same "type the name back exactly" pattern Space deletion already used, now themed and shared instead of duplicated). Both return a Promise, awaited at the call site the same way the native functions returned synchronously -- no page had to restructure its own delete-handler logic, just swap the call. Alongside it: the stylesheet had zero CSS transitions anywhere despite 27 `:hover` rules, so every state change snapped instantly -- one shared rule (`transition: border-color, background-color, color, opacity`) applied to every interactive element fixes that app-wide, respecting `prefers-reduced-motion`. A `:focus-visible` outline ring was also added site-wide (keyboard/assistive-only, invisible on a mouse click) -- the app previously had almost no visible keyboard-focus indication at all. The dialog overlay is the one deliberate `box-shadow` in the whole app -- a true overlay needs to read as elevated above the page behind it, which the app's otherwise-flat, shadow-free aesthetic doesn't achieve on its own; everything else stays shadow-free by design.

**Correctness fixes**, the professionalism pass's third slice -- the three real issues `oxlint` caught, not stylistic nits: `SessionBlock.jsx`'s `useState(Date.now())` called the impure `Date.now()` fresh on every render (harmless here since React only used the value once, at mount, but still wrong) -- fixed with the lazy-initializer form, `useState(() => Date.now())`. `LedgerView.jsx` built its running-total column by mutating a `let runningTotal` variable inside a `.map()` callback; rewritten with `.reduce()` building a new array each time, so there's no mutated variable for the linter (or a future reader) to worry has escaped its render scope, even though in this specific case it never actually did. `GraphView.jsx`'s several "ref accessed during render" warnings were investigated and left as-is, deliberately: `nodesRef.current` (holding every node's live x/y/vx/vy from the hand-rolled physics loop) is read in the render body on purpose, because the alternative -- copying the whole node array into `useState` on every animation frame just to satisfy the rule -- would mean a full React reconciliation pass 60 times a second for data React never needs to diff against anything. The physics loop's own `forceRender` call already guarantees a fresh render every frame the simulation runs, so nothing actually goes stale in practice; a comment at the read site now says so directly, so a future reader doesn't mistake the linter warning for an unexamined bug. The one genuinely free fix taken there: an unused `event` parameter on `onMouseUp` removed.

**An "expert-grade aesthetics" pass**, at the person's explicit request once the test suite was finished, "to make this almost industry-standard level legible/sensible." Grounded in a real audit before touching anything, matching every prior open-ended ask's method: screenshotted every real page at 1440px (Dashboard, Tools, Insights, Log, the Map, Templates, a Space page, all three Creation flows) and read through the whole of `index.css` (2,644 lines) rather than guessing. That audit found four concrete, independent issues; presented as a multi-select confirmation, three were chosen first, and the fourth followed once its own PR was up, at the person's explicit request to include it too. All four landed as a result, each small and self-contained:
- **A type scale.** `index.css` had drifted to 24 distinct font-size values with no defined steps, several only 0.5px apart (10.5px, 12.5px, 13.5px, 14.5px, 15.5px, 16.5px) -- reading as accidental rather than intentional at any of those sizes. Replaced with 13 named tokens (`--fs-2xs` through `--fs-5xl`) in `:root`, grouped by real, observed usage (tiny mono chip/badge text, small UI chrome, body-adjacent reading text, headings and large display numbers) -- `--fs-body-lg` (16px) deliberately matches `body`'s own default size, so ordinary reading text inside a card and the page's base text stay the same unless a component has a real reason to differ. Every one of the file's 132 `font-size` declarations now reads from a token. The border-radius split (27 uses of 3px, 25 of 2px) turned out, on inspection, not to be arbitrary at all -- small inline chrome (pills, chips, buttons, form controls) already consistently used 2px and larger surfaces (cards, panels) already consistently used 3px; the actual gap was that this convention existed only as muscle memory, never named, so a future addition could easily drift from it without anyone noticing. Named it instead: `--radius-sm`/`--radius-md`. Three deliberate exceptions (50% circles, one literal `0` for a square corner already commented as intentional, and two 4px radii on the app's two most attention-grabbing surfaces -- the Insights banner and the confirm-dialog overlay) were left as plain literals with a one-line comment each, rather than forced onto the shared scale.
- **Native control theming.** Two spots fell back to unstyled browser defaults inside the otherwise fully art-directed theme. The Tools catalog's Media demo (and any real Media block with no image) used a hand-authored placeholder SVG (`DEMO_IMAGE_SVG` in `frontend/src/registry/blocks.js`) hardcoded to a light-gray fill and black default-sans-serif text -- recolored to the app's own `--surface-2`/`--ink-faint` hex values and mono font (an SVG data URI can't read CSS custom properties, so the literal hex values had to be copied in directly, called out in the code's own comment). `MediaBlock.jsx` also had a real, separate gap the same audit surfaced: a Media block with an empty `url` rendered a raw `<img>` tag with no `src`, showing the browser's native broken-image icon -- now shows the same themed `.media-placeholder` treatment the audio/sketch stub types already used, closing the gap consistently rather than inventing a new pattern. Every `<input type="date">` (the Space page's Due field, a Milestone's target/reached dates) now explicitly sets `font-family: var(--mono)` (previously inherited whatever serif/sans context it sat in) and tints its calendar-picker icon via `::-webkit-calendar-picker-indicator` to match the app's dimmer ink tones -- `color-scheme: dark` on `:root` already got the browser to draw the field and its placeholder in a dark-appropriate palette, so this was the one remaining piece that needed an explicit hand.
- **Log page readability.** The Log already had 112 entries and only grows -- previously one flat, ungrouped, unpaginated list, ranked the least scannable screen in the audit. `LogPage.jsx` now groups consecutive entries sharing a calendar day (in the viewer's own local time) under one heading, and shows only the 40 most recent entries at a time with a "Show N more" button revealing the rest in the same page-size increments -- purely a client-side slice of the array `getActivity()` already returns in one call (the backend's existing `listGlobalActivity(limit = 300)` needed no change), so no new endpoint or pagination protocol was needed. Grouping by day also let each row's own timestamp shrink from a full date-plus-time string to just the time, since the day heading now carries the date -- removing a redundant repetition on every single row, not just adding a heading.
- **Space page layout & the Graph's fixed width.** The Space page's own metadata (status, accent picker, working-toward, due date, tags, promotion, Categories) previously sat as six bare one-line rows directly under the title, unlike the well-organized cards everywhere else in the app -- now grouped into one bordered `.space-details-panel` (same surface/border/radius family as `.view-card`/`.digest`/`.work-block`), so it reads as one considered section instead of loose metadata. (This panel was later made into a native `<details>` for adaptive density -- see the Interaction & Workflow pass below.) Separately, `#root`'s app-wide 900px content column was actively clipping node labels on the Map, not just leaving unused space elsewhere -- traced to the graph canvas's own fixed logical coordinate space (`WIDTH`/`HEIGHT` in `GraphView.jsx`, an SVG `viewBox` clips anything drawn past its bounds by default) rather than the surrounding CSS, so the real fix widened both together: `WIDTH`/`HEIGHT` went from 640x440 to 900x620 (same ~1.45 aspect ratio, more logical room for a node's label before it can reach the edge), paired with a new `.graph-frame`/`.graph-frame-inner` wrapper on `GraphPage.jsx` that breaks just the canvas out of the centered column via the standard `calc(50% - 50vw)` negative-margin technique (capped at 1400px, not full-bleed to the viewport edge) -- every other page, and the rest of the Graph page itself (heading, the Relational Space form below the canvas), stays at the ordinary 900px width, since prose reads fine there and this was never a blanket "everything should be wider" ask. The physics loop's own force/spring/damping calculations were deliberately left untouched -- only the two size constants they already read from changed, the same caution the professionalism pass's correctness-fixes slice already showed toward this file.

Verified live end-to-end, not just by re-reading the diff: `vite build` and the full frontend (`npx vitest run`, 295 tests, 4 new -- two covering the Log's day-grouping and pagination, one confirming the Space page's details panel groups the expected fields, one confirming the Graph's canvas renders inside the new wide frame) and backend (191 tests, unaffected) suites all pass, `oxlint` shows the same pre-existing warnings only, and every affected page was re-screenshotted afterward and visually diffed against the pre-pass captures -- the Dashboard and Insights pages render pixel-identical (confirming the token/radius consolidation changed no visible sizing), the Space page's metadata now reads as one clear section, and the Graph's node labels render fully within the canvas with no horizontal-scrollbar side effect from the breakout technique (checked directly via `document.documentElement.scrollWidth`).

## Data model, current state
- `spaces` — id, title, template_id (nullable), status (one of `dormant`/`inactive`/`active`/`interesting`/`mature` — the person's own five-word vocabulary, replacing the earlier invented `nascent`/`developing`/`stable`/`dormant` set; `migrateSpaceStatuses()` in `backend/src/db/queries/spaces.js` maps the two retired values onto `active` once at startup), tags (JSON array — Resources and any future category *of Spaces*), goal (nullable text — **superseded** by `goal_ids` below and no longer read by any code, the same treatment `accent` got; retained rather than dropped so existing rows keep loading), goal_ids (JSON array of goal ids — which Goals this Space is working toward, many-to-many, see the Goals vocabulary entry above; named `goal_ids` specifically so it can't be mistaken for the legacy `goal` column), categories (JSON array of freely-named facets *of this Space's own topic*, e.g. "Financial Impact" — see the Categories vocabulary entry above; distinct from tags), accent (nullable text — **superseded** by `theme` below and no longer read by any code; the column is deliberately retained rather than dropped, since dropping it would mean a destructive migration on the person's own real, already-deployed data for no functional gain), origin (nullable text, `'external'` | `'internal'` — Provenance, see the vocabulary entry above; set once at creation by `CreateResource.jsx`/`CreateSynthesis.jsx`, null for an ordinary Space, never edited afterward), due_date (nullable text, `'YYYY-MM-DD'` — a real target date for the Space as a whole, see the Time vocabulary entry above; distinct from a List item's own `reviewBy`, which means "come back and reconsider this," not "this is due"), theme (nullable JSON `{accent, shape, density, typeface}` — the *manual* half of this Space's own look, see the Personalization vocabulary entry above; null means "use the computed default for this kind of Space," and only a hand-picked override is ever stored, so a Space left alone keeps tracking its kind's default if that default is ever changed), created_at, updated_at
- `blocks` — id, space_id, type, content (JSON), properties (JSON — status-like markers: `skeletonLane`, `skeletonRole`, `categories` (a JSON array of the Space's own Category names this block belongs to, many-to-many), `workspaces` (a JSON array of Workspace ids this block has been assembled into, many-to-many, ids rather than names since a Workspace is a real row that can be renamed/deleted independently), `projectId` (a single nullable Project id, not an array -- a Milestone or Session belongs to at most one Project at a time), and `sourceItemIds` (a JSON array of Work item block ids, set only on a Synthesis's own "Source Material" Text block at creation time -- the real, queryable lineage of which specific Work items fed that Synthesis, distinct from the block's own copied-in text; see the Roadmap entry above), and `theme` (a nullable `{accent, shape, density, typeface}` object — the same manual-override half a Space's own `theme` column holds, stored in `properties` rather than its own column since it's a per-entry attribute like `categories`/`workspaces`, not content; null means "use this Tool type's own distinct default")), position, created_at, updated_at
- `workspaces` — id, space_id (a Workspace belongs to exactly one Space, same as a block), name, kind (nullable text — which specialized environment this is, e.g. `'analyst'`/`'etymology'`; the kinds themselves are defined in `frontend/src/registry/workspaceKinds.js`, never in the database, since a kind carries page layout and framing copy a row can't hold — null is a plain, unkinded Workspace, what every Workspace was before kinds existed), created_at, updated_at — a deliberately assembled, named environment; see the Workspaces vocabulary entry above
- `projects` — id, name, goal_id (nullable — the Goal this Project serves, single rather than an array, matching how a Milestone belongs to at most one Project), created_at, updated_at — a real, named piece of work a Milestone or Session belongs to; see the Projects vocabulary entry above. Deliberately has **no `space_id`**: a Project's Spaces are derived from wherever its member entries live. The old column could not simply be left in place and ignored the way `spaces.accent` was, since it carried NOT NULL plus a foreign key into `spaces` and so blocked inserting a standalone Project at all — `migrateProjectsSpaceless()` in `backend/src/db/queries/projects.js` rebuilds the table (SQLite can't drop a column carrying a foreign key) for any database created before this, and is idempotent.
- `goals` — id, name, note (nullable), created_at, updated_at — a pursuit several Spaces can be working toward at once; see the Goals vocabulary entry above. Has no Milestones or Sessions of its own, on purpose.
- `trash` — id, kind (`space`/`block`/`workspace`/`project`/`template`/`resource_template`), label (what it was called), context (where it lived, e.g. its Space's title), payload (JSON: the removed rows keyed by table name), deleted_at — see the Durability vocabulary entry above. Deliberately a snapshot table rather than a `deleted_at` column on every table: a soft-delete column would mean every read query in the app filtering it forever, and one missed filter means deleted content quietly reappearing, whereas this touches only the six delete paths and changes no existing read at all. Nothing expires on its own — there is no background job to run an expiry in, and silently destroying something a second time is exactly what this table exists to prevent.
- `templates` — id, name, block_arrangement (JSON)
- `resource_templates` — id, type (unique, matches a Resource's own type tag), label, facets (JSON array of `{name, prompt}`) — a deliberately separate mechanism from `templates` above; see the Resources vocabulary entry above
- `trail_entries` — id, space_id, kind (auto/manual/review — `review` added for the Time arc's Review layer, see the Time vocabulary entry above), summary, note, skeleton_snapshot (JSON), created_at — no longer write-once: `updateTrailEntry` in `backend/src/db/queries.js` lets `note` (and, for a `manual` entry, its recomputed `summary`) be edited after creation; `kind` and `skeleton_snapshot` stay fixed at write time, since a snapshot is a historical fact about that moment, not something later editing should touch. A Space page reads this table *and* `activity_log` together through `listSpaceHistory` (see the Trail entry above); this table itself still only ever holds real Trail entries, which is what Reports, Review and Rewind all depend on.
- `activity_log` — id, space_id (nullable, no foreign key), space_title (snapshotted at write time, since a "Space deleted" entry must survive the Space itself being gone), block_id (nullable — set only on a `block_added` event, while that block still exists; left null on `block_removed` and every other kind, since deep-linking (see the Roadmap entry above) has nothing left to link a removed block to), kind, summary, event_count (how many occurrences this one row stands for — always 1 except on a coalesced `block_edited` row, see the Log vocabulary entry above), created_at — the Log's recorded-event half; merged with `trail_entries` at query time by `listGlobalActivity` (globally) and `listSpaceHistory` (for one Space), not duplicated into either table
- References are blocks of type `reference` with a `target_space_id` in their content — indexed, since backlinks and the Relational Map query it constantly.
- A Text block's content is `{lines: [{id, text, tag}]}` — each line has a stable id and its own attribution tag, independent of every other line (see the Tools vocabulary entry above). Comparison's embedded text-kind sides keep the older `{tag, text}` shape forever, since they're plain objects inside a `comparison` block's own content, never real `type='text'` rows.
- A Tensions-lane List item can carry `statementA`/`statementB`, each `{blockId, itemId}` pointing at an item in one of the other three Skeleton lanes' blocks — resolved live at read time (see the Skeleton entry above), not snapshotted or duplicated.
- A Work block's content is `{statement, support, confidence}`, where `support` is a JSON array of `{id, text}` (a free-text point) or `{id, pointer: {spaceId, blockId, itemId}}` (a live link to another claim -- `itemId` null means the pointer targets a whole Work block's own statement, set means it targets a specific Skeleton lane item, same shape a Tension's own pointers use). `spaceId` is only present when the link crosses Spaces (see the Roadmap entry above); a same-Space pointer omits it entirely, including every pointer saved before cross-Space linking existed, so an old pointer still resolves exactly as it always has. `backend/src/db/queries.js`'s `WORK_TYPES` is the one list of which block types `listWorkItems()` (and so Synthesis's picker) draws from — see the Work vocabulary entry above. Synthesis itself needed no schema change at all: it's just `createSpace`'s existing `tags`/`extraBlocks` parameters, the same ones Resource creation already uses.
- A Word Evolution block's content is `{term, senses: [{id, period, sense, note}]}` — `period` is deliberately free text ("Old English", "c. 1650", "post-Enlightenment"), so order is manual rather than sorted; no date parser handles those honestly. See the Mapping vocabulary entry above.
- A Concept Map block's content is `{referent, gloss, renderings: [{id, label, sense, alignment, note}]}` — `alignment` is one of `aligned`/`partial`/`divergent` (`ALIGNMENTS` in `frontend/src/blocks/mappingRows.js`), a fixed three-value scale rather than free text so the divergences can be *seen* rather than read for, the same reasoning behind the fixed confidence scale.
- A Model block's content is `{subject, components: [{id, name, role}], relations: [{id, from, to, kind, note}]}` — a relation's `from`/`to` store component *ids*, resolved live on every render, so renaming a component updates every relation mentioning it with no sync step (the same live resolution Tension pointers and Work support links already use). A relation whose component was removed renders as `(removed)` rather than vanishing: silently dropping half a relation would quietly change what the model claims.
- A Milestone block's content is `{label, targetDate, reached, reachedAt, note}` — `targetDate`/`reachedAt` are nullable `'YYYY-MM-DD'` text, `reached` is a plain boolean toggled directly (setting `reachedAt` to today when turned on, clearing it when turned off). See the Time vocabulary entry above.
- A Session block's content is `{label, startedAt, endedAt, durationMinutes, note}` — `startedAt`/`endedAt` are nullable ISO datetime text (client clock, set by Start/Stop), `durationMinutes` is a plain integer computed and stored at Stop time. A session is "running" whenever `startedAt` is set and `endedAt` isn't; elapsed time while running is always derived from `startedAt`, never stored separately. See the Time vocabulary entry above.
- A List item's and a Work item's `confidence` field share one 5-value scale, `questioned`/`tentative`/`moderate`/`solid`/`certain`, defined once as `CONFIDENCE_LEVELS` in `frontend/src/registry/blocks.js` (List's own `CONFIDENCE_CYCLE` in `frontend/src/blocks/listItems.js` mirrors it, kept as a separate export since other code already imports from there).
- Tags are queried by membership (`listSpacesByTag` in `backend/src/db/queries.js`, `GET /api/spaces?tag=...`) — general-purpose, not written specifically for Resources.
- A block's Categories are edited independently from its content via `updateBlockCategories` in `backend/src/db/queries.js` (PATCH `/blocks/:id` accepts `content` and/or `categories`, since they're independent edits) — kept separate from `updateBlockContent` because Categories are a property/attribute of the block, not its actual content.
- A block's Workspace membership is edited the same independent way, via `updateBlockWorkspaces` (PATCH `/blocks/:id` also accepts `workspaces`, any subset of `content`/`categories`/`workspaces` together).
- A Milestone/Session's Project membership is edited the same independent way, via `updateBlockProject` (PATCH `/blocks/:id` also accepts `projectId`, alongside any subset of `content`/`categories`/`workspaces`/`projectId`) -- a single id or `null`, not an array.
- A Media block's content depends on its `mediaType` (see the Tools vocabulary entry and content-ingestion Roadmap entry above): `'image'` is `{mediaType, url, caption}`; `'link'` adds `linkTitle`/`linkDescription`/`linkImage`/`linkSiteName` (fetched once at creation from the pasted URL's own Open Graph/meta tags via `POST /link-preview`, never re-fetched afterward -- a snapshot, not a live pointer); `'document'` adds `fileName`/`fileType` (the uploaded file's own original name and MIME type, with `url` pointing at `GET /uploads/:filename`); `'audio'`/`'sketch'` remain placeholder-only. Uploaded files live on the local filesystem under `backend/data/uploads/` (gitignored, `backend/`-only for now -- see Open) with server-generated UUID filenames, never the person's own original filename, so nothing about a chosen file's name ever becomes part of a server-side path.
- `changeSummary` is not a stored field on anything -- it's an ephemeral, response-only string a handful of write endpoints attach to what they already return (a created/updated Space, block, Workspace, Project, Template, Resource Template, or Trail entry), computed either by reusing a sentence the function already builds for `activity_log`/Trail, or (for a few real, named implications -- a Milestone's `reached` flipping, a Space's `due_date` changing, a Tension pairing) by a small pure helper, `backend/src/changeSummary.js`'s `describeBlockContentChange()` plus inline logic in `updateSpace`/`createTensionPair`. `frontend/src/api.js`'s `request()` reads it off a PATCH/POST response to drive the toast (see the change-legibility Roadmap entry above and Toast.jsx) -- never written back to the database, never present on a plain `GET`.
- `createSpace` (and `createSpaceWithSetup`/`POST /spaces` on top of it) accepts an optional `categories` array at creation time, alongside the existing `tags` — used by `CreateResource.jsx` to seed a new Resource's four starting facets before any block exists; an ordinary Space creation still leaves this empty and adds Categories later from the Space page, same as before.
- `createSpaceWithSetup` also accepts an optional `workspaces` array (plain names) — used by `CreateSpace.jsx`'s Workspaces step. Each is created via `createWorkspace` right after the Space itself, before `extraBlocks` are added, specifically so a block spec carrying `properties.workspaceNames` (draft-time names) can be resolved into real `properties.workspaces` (ids) in the same request.

Propose refinements to this if something better fits once more building happens — it's a foundation, not gospel.

## Roadmap

Complete: Passes 1–5 of the original plan (Dashboard/Creation Mode/Test Space; Tools & Resources; the 5 Templates; Dev Mode as originally scoped; the Relational Map). Visual polish. Two editability passes: the first made Space title/status/tags/goal editable in place and List items removable; the second (alongside the Dev Mode decision) closed the remaining "created but never removed/changed" gaps found by auditing every page — a Space itself can now be deleted (`deleteSpace` in `backend/src/db/queries.js`, a delete control on both the Dashboard and the Space page, guarded so the Test Space can't be deleted), a List block's heading is editable, and a Comparison block's contrast flag/note can be toggled and edited after creation. The Tools catalog, the Creation Mode overhaul, the Log, and the Relational Map overhaul (see above) are all done. Categories (see the vocabulary entry above) — added after the person said the Space page felt too generic/modular ("block," a Text/List dropdown) with no sense of what belonged together, and asked for a way to zoom in on different aspects of a Space's own topic where one thing can matter to several aspects at once. Considered a literal zoomable canvas inside a Space first, but the person picked the simpler named-sections direction instead (confirmed via direct question, not assumed). A dedicated Resource creation flow (see the Resources vocabulary entry above) — the person asked for Resources to be properly homed in on (what it is / affords / touches-is touched by / offers) and creatable through something "entirely different" from ordinary Space creation, while still able to engage every other Tool/View/Category/Reference once created. Confirmed two forks via direct question before building: a genuinely separate top-level entry point rather than a cluster card inside Creation Mode, and picking existing Spaces as real References (not just free text) for "what it touches / is touched by." Most recently: Workspaces (see the vocabulary entry above) — the person asked for every Tool to get its own tailored, more spacious environment to "pop into" when working with it, rather than a cramped inline row. Confirmed two forks via direct question before building, both toward the bigger option: Workspaces are reached via their own dedicated page (not an overlay), and a Workspace is a genuinely new, independently-named entity you assemble multiple Tools into (not just a bundled view of one block and its Views). Deliberately scoped as a foundation-first pass: the Workspace concept itself (create/assemble/navigate/rename/delete) is built and reconciled with the Dev Mode decision; bespoke per-Tool-type designs inside a Workspace, Creation Mode integration, and Relational Map integration are all explicitly deferred, not forgotten. The first of those bespoke per-Tool redesigns, for Text (see the Tools vocabulary entry above) — asked for directly once Workspaces existed to redesign into. Confirmed which Tool to start with via direct question (Text, as the most-used Tool across every Template and the Skeleton) rather than assuming an order across all five Block types and five View types at once. The other two deferred pieces, done together at the person's request -- Creation Mode integration (a Workspaces step in `CreateSpace.jsx`, see the Creation Mode vocabulary entry above) and Relational Map integration (Workspaces as real nodes/edges in the Graph, see the Relational Map vocabulary entry above). The second bespoke per-Tool redesign, for List (see the Tools vocabulary entry above) -- picked as the next Tool since it's the other backbone Tool alongside Text (three of the four Skeleton lanes are Lists). The third, fourth, and fifth, for Reference, Media, and Comparison together in one pass, at the person's request -- the last of the five Block types to get one. Most recently: the Writing Surface and the Skeleton, together in one pass, at the person's request ("lets do the skeleton and the writing surface") -- Text's per-line attribution-tag redesign (superseding the earlier single-tag-per-block TextWorkshop from the previous pass) and Focus Mode, plus the Skeleton's paired-Tensions structure and a second, non-destructive capture path ("File") alongside promotion shorthand. Two design forks confirmed via direct question before building, both toward the more structured option: per-line tags stored as discrete `{id, text, tag}` lines rather than one tag for the whole block, and filing a line into a lane copies it rather than removing it from the Writing Surface. Voice capture, raised in the same design corpus, was deliberately left out of this pass as the most speculative/environment-dependent item raised, flagged rather than dropped. Most recently: Trail and Visual Identity, at the person's request ("lets do trail and visual identity next"), checked directly against the same wireframe corpus. Trail's real gap turned out to be narrower than a first pass at comparing the wireframes to the codebase suggested: Rewind (viewing the Skeleton as it stood at an earlier point) already existed from an early pass, so the actual open item was only that entries were write-once, with no way to attach a manual "why" to an auto entry after the fact. Confirmed via direct question whether Rewind's presentation was still worth upgrading to the wireframe's explicit Now-vs-As-of side-by-side layout (it was) alongside building the note-attach capability. Visual Identity's one real gap -- the manual accent layer (`star`/`underline`/`triangle`/`dot`) layered on top of the computed glyph -- had already been anticipated and deliberately deferred when the glyph itself was first built, so building it now closed a known, named gap rather than an unexamined one. Most recently: Work and Synthesis (see both vocabulary entries above), a new pass the person raised unprompted -- not from a wireframe batch, but from directly describing a gap: the individual acts of thinking a Space is for (assess, question, deduce, define, ...) had nowhere to become real content of their own, and there was no way to compile several of them into a more polished piece. Confirmed three forks via direct question before building, all toward the more structured/ambitious option: real distinct Tools sharing one underlying skeleton rather than a label on a generic block; Assessment and Question as the starting pair (not the full list of thinking-verbs raised) to establish the pattern first; and Synthesis producing a genuinely new Space (with its own Categories/Trail/References) rather than a block bolted onto an existing one. The rest of the thinking-verb list (analyze, deduce, define, demonstrate, ...) is explicitly a later, one-kind-at-a-time rollout, same discipline as the five original Block types. Most recently: four more Work Types in one pass, at the person's direct request naming exactly which ones -- Analysis, Deduction, Definition, and Demonstration. With the shared-skeleton pattern already proven out by Assessment and Question, adding these four was mechanical rather than another round of design forks: each is a thin wrapper around the same `WorkBlock.jsx`, just naming its own two fields (Definition again the one exception, since its statement holds a term rather than a Definition-shaped headline). Verified live that both `WORK_TYPES` (backend) and the frontend registry were the only two places that needed touching -- the new types showed up correctly in the Tools catalog, the "+ Add Block" form, and Synthesis's cross-Space picker with no changes to Synthesis itself. Most recently: two more Work Types, Insight and Implication, at the person's request to "keep going" once they'd laid out a much longer original list. Rather than working through every remaining verb, walked the list back to the person as a recommendation via direct question: most of what remained either already maps onto an existing Tool (plan/outline onto List, integrate onto Synthesis) or is a near-duplicate of one of the six already built (derive/infer of Deduction, explain/expound of Definition) -- Insight (an unplanned realization) and Implication (what follows from something, short of proof, softer than Deduction's explicit reasoning) were the two that actually stood on their own. Confirmed via the same question rather than assumed. Same mechanical rollout as the prior four: WORK_TYPES and the frontend registry only, verified live end to end. Most recently: a "surgical granularity" pass, at the person's request for finer distinctions than the app allowed. Rather than assume one meaning, confirmed three separate forks via direct question: how far to widen the shared confidence scale (and whether List should keep its own simpler one -- widen everywhere, chosen), what "surgical structure inside a Work item" should mean (break the single rationale blob into discrete points, and/or add structured links to other claims -- both, chosen), and which new Work Types the finer structure made viable (Hypothesis + Objection, chosen from two candidates raised). Confidence widened from 3 levels to 5 with no data migration needed (the original three words were kept as-is). Work's `rationale` became `support`, a list of discrete points each either free text or a live link to another existing claim -- reusing the exact live-resolution pattern Tensions' `statementA`/`statementB` pointers already established, extended to also target a whole Work block (not just a Skeleton lane item). Objection turned out to need no dedicated field of its own once linking existed: what it challenges is just a linked support point, which is what confirmed the ordering (structure before the two new Types, not after) was the right sequence. A `normalizeWorkContent`/`migrateWorkItemSupport` pair upgrades any block still on the old `rationale` shape, same approach the Text-lines migration already established -- verified live against a manually-inserted legacy-shaped block before considering the pass done. Most recently: Provenance (see the vocabulary entry above), from the person describing a gap in their own words -- "Spaces can produce things which turn into tools/resources ... down the line," and wanting the app to distinguish what's brought in from outside versus what it produces itself. The same message also floated a full front-end redesign; confirmed via direct question to separate that out rather than fold it into this pass -- redesigning "everything at once" runs directly against this project's own small-slices method, so it's tracked as its own, still-unscoped thread (see Open) rather than assumed to be part of Provenance. Provenance itself: confirmed via direct question to build the bigger of two options -- not just a visible origin marker, but a real promotion action -- rather than the marker alone. Landed as `spaces.origin` (`'external'`/`'internal'`), set once at creation by `CreateResource.jsx`/`CreateSynthesis.jsx`, plus a `PromoteToResource` action on the Space page that adds the `'resource'` tag to a mature Synthesis with no new Space and no new backend endpoint -- deliberately scoped to promoting a whole Synthesis, not a raw Work item directly, since Synthesis is already the maturation step this builds on top of. Most recently: the first slice of the front-end discoverability thread, after the person clarified "hard to find things" as the actual concern behind the redesign ask, and declined the offer to fix named screens ad hoc instead of pursuing a real redesign track. Rather than guess where to start, ran a grounded audit of six real screens before proposing anything; it found four independent gaps, three sharing one root cause -- no grouping metadata existed on the Tool registry. Confirmed via direct question which of the four to start with: the registry fix, since it's small and improves three screens at once (the Tools catalog, the "+ Add Block" dropdown, and Creation Mode's Tools step, which reuses that same dropdown). The other three findings (Dashboard search/filter, a type-based block filter on the Space page, search on Synthesis's picker) are tracked in Open, not assumed to be next. Most recently: those same three findings, all at once, at the person's explicit request ("start on the next 3") once the first slice merged. Each addressed independently: search plus a status filter on the Dashboard's Space list, all 5 digest sections made individually collapsible (native `<details>`, no state to manage); a type-based filter strip on a Space's block feed, combinable with the existing Category strip rather than replacing it; and a statement-text search box plus a live selected-count on Synthesis's cross-Space picker, confirmed to leave selections intact when narrowing the search rather than only filtering what's checkable. This closes out the front-end discoverability thread as scoped by the audit -- see the Open section for how that's phrased going forward. Most recently: Insights (see the vocabulary entry above), at the person's explicit request to surface "what's actually going on in the app in general," stated as fundamentally the reason the app was being built in the first place. Confirmed which facets to build and where to put them via two direct questions before starting: all four candidate insight categories raised (Work Type & confidence mix, Themes & unresolved tensions, Activity & staleness trends, Provenance & Synthesis yield), and its own dedicated page (not folded into more Dashboard digests) made deliberately prominent, per the person's instruction, with both an ordinary nav link and a visually distinct banner. Purely computed from data that already existed -- no new schema, no new stored fields -- and deliberately scoped away from item-level Synthesis lineage tracking (see Open). Most recently: Reports (see the vocabulary entry above), at the person's request that "every single page ... should be able to derive a report which captures the state of that page," framed explicitly as raw material for external Claude conversations that can give one Space/Tool/thought closer attention than an in-app view. Confirmed three forks via direct question before building, all toward the more structured/ambitious option: both a structured JSON snapshot and a rendered prose narrative (generated from the same source, so they can't drift apart) rather than either alone; an ordinary "Report" button on each page rather than folding reports into Trail as a stored entry kind; and the generic pattern built across every level in one pass rather than starting from just Spaces or just Work items. Landed on three levels, not five, once the actual data model was checked against the person's examples: a Relational Space is already just an ordinary Space, and a "connection" is already just a Reference block, so Space/Workspace/block reports (the third covering every Tool and Work item uniformly) cover every example raised with no redundant fourth or fifth report kind invented. Surfaced this while testing: `deleteSpace` in `backend/src/db/queries.js` never deleted a Space's `workspaces` rows, so any Space that ever had a Workspace couldn't be deleted at all (a foreign key violation) -- a real, if narrow, break of the standing "anything created can be removed" principle, unrelated to Reports itself and flagged as a separate follow-up rather than folded into this pass. Most recently: the first slice of Time (see the vocabulary entry above), from the person naming a felt-but-unspecific gap -- "something missing... has to do with how things are able to be measured in general" -- across a long list (timeframes, due dates, milestones, time blocks, sessions, timers, retrospectives, reviews, logs). Rather than guess which of those was the real ask, reflected back what already existed in each area (a List item's buried `reviewBy`, Trail's ad hoc notes, Insights' staleness read) and asked directly which felt closest to the actual gap; the person picked all of it, confirming this is one threaded arc, not a single feature. Proposed and confirmed a dependency order before building anything: due dates + Milestones first (foundational, cheapest), then Sessions/Timers, then Retrospectives/Reviews (building on both), then better surfacing last (nothing to surface until the rest exists) -- the same layering Work → Synthesis → Insights already followed. The first layer -- a real `due_date` on a Space, computed `isOverdue` (independent of status, mirroring Insights' staleness logic), and Milestone as a new Block type with its own `family: 'time'` (a third value alongside `'general'`/`'work'`, needed since a checkpoint's state fits neither existing shape) -- threaded into Reports rather than left to coexist separately, so a Space's report now surfaces its due date and Milestones directly. Most recently, at the person's request to "keep going" once the first layer's PR was up: Session, the second Time Type and the first genuinely live/running Tool in the app (start it, stop it, elapsed time derived from a persisted `startedAt` rather than a client-only ticking counter, so it survives a closed tab) -- mechanical to add once Milestone had already proven out the `family: 'time'` pattern and the registry-driven Tools-catalog/dropdown grouping, needing only its own `handleSubmit` branch in `NewBlockForm.jsx` and a Reports case, same as Milestone did. Most recently, at the person's one-word "reviews" once Sessions' PR was up: Review, the Time arc's third layer -- deliberately built as a third `trail_entries.kind` value rather than a new concept, so it inherits Trail's storage, snapshots, Rewind, and note-attaching for free. `getReviewDraft` computes what changed since the last Review (or since the Space was created, for the first one); `createReview` writes that exact text as the new entry, so the preview and the permanent record can't disagree. Caught and fixed one real bug while testing: comparing a Session's `endedAt` (a JS ISO string) directly against a Trail timestamp (SQLite's `datetime('now')` format) as plain strings gave wrong answers, since the two formats don't sort the same way -- fixed by normalizing one to the other before comparing, called out directly in the function's own comment so it isn't reintroduced. Most recently, at the person's "keep going" once Review's PR was up: the surfacing pass, closing out the Time arc -- a fifth Insights facet (`getTimeInsights()`) threading due dates, Milestones, Sessions, and Review into one section, placed last on the Insights page after Provenance & Synthesis yield. Needed no new CSS at all: every visual piece Insights' first four facets already established (bar rows, plain lists, funnel numbers, empty-state text) was reusable as-is, confirming the payoff of building those generically the first time -- the one small addition, spacing between two stacked rows of columns, was a gap fix, not new visual language. The Time arc, raised as a felt-but-unspecific gap across nine named concepts, is now fully built. Most recently: a "professionalism pass," at the person's request to take the app up a tier on ten named criteria at once (professionalism, optimization, efficiency, organization, intuitive UI/UX, modern surface, interactability, editability, clarity, precision/accuracy) -- explicitly not narrowed to one thing up front, unlike every prior pass. Rather than guess or start editing code blind, ran a real, grounded audit first: screenshotted the live Tools catalog, a real Space page, and the Dashboard; grepped for native `window.confirm`/`window.prompt` calls (6), scattered inline `style={{}}` props (31), and CSS transitions (0 in a 2,400-line stylesheet); ran the existing `oxlint` setup and read its output rather than assuming code quality was fine. That audit found one dominant, repeated problem -- every Block type, View type, and Dashboard digest rendered as a visually identical box -- plus three separate, smaller threads (native dialogs/interaction polish, three real linter-caught correctness bugs, and `queries.js`'s size). Presented all four as a single multi-select confirmation rather than assuming which the person meant by "spice it up"; all four were chosen. Sequenced as separate slices rather than one giant commit, same discipline as every other multi-part pass in this project. The first slice: Tool/section personality (see the Visual direction entry above) -- family-colored left borders plus a per-Tool icon on Blocks, Views, and Dashboard digests, and a `block-type-tag` fixing the separate, real gap that a Work block's own kind was never actually shown anywhere. Most recently, at the person's "keep going" once that slice's PR was up: the second slice, dialogs and interaction polish (see the same Visual direction entry) -- every native `window.confirm`/`window.prompt` call replaced by a themed, Promise-based `ConfirmDialog`, plus app-wide CSS transitions and `:focus-visible` outlines where there previously were none. Verified live that no native dialog ever fires anymore (a Playwright `page.on('dialog')` listener that would have caught one stayed silent through every delete flow tested). Most recently, at the person's "keep going" once that slice's PR was up: the third slice, the three real `oxlint`-caught correctness fixes (see the same Visual direction entry) -- `SessionBlock.jsx`'s impure `Date.now()` call fixed with a lazy `useState` initializer, `LedgerView.jsx`'s mutated running-total variable rewritten functionally with `.reduce()`, and `GraphView.jsx`'s ref-during-render warnings investigated and deliberately left as a documented, working design rather than rewritten -- restructuring a 60fps hand-rolled physics loop to avoid a lint rule that isn't catching an actual manifesting bug here would trade a real, working, tested pattern for a theoretical purity gain. Verified all three live: Ledger totals still compute correctly, a Session still ticks and completes correctly, and dragging a Graph node still works. Most recently, at the person's "please merge and move to that next piece, afterwards lets do a category hygiene audit": the professionalism pass's fourth and final slice, the organization pass, closing out the original audit's four confirmed threads. Two independent halves. First, `backend/src/db/queries.js` (grown to ~1,800 lines) was split into `backend/src/db/queries/` -- thirteen small files, each one cohesive concern (`spaces.js`, `blocks.js`, `workspaces.js`, `templates.js`, `skeleton.js`, `trail.js`, `review.js`, `work.js`, `log.js`, `insights.js`, `reports.js`, `dashboard.js`, plus a dependency-free `constants.js`), mirroring the same `--- Section ---` divisions the single file already used internally. `queries.js` itself stays in place as a thin barrel (`export * from './queries/xxx.js'` per module) -- deliberately, so every one of the 9 existing route files and the 2 seed files keeps importing `from '../db/queries.js'` completely unchanged, and CLAUDE.md's Transparency principle ("keep cross-Space queries going through a small, consistent set of functions") stays true in spirit: this remains the one recognizable public entry point, it just no longer requires scrolling 1,800 lines to find one function. Three functions moved to their true conceptual home rather than staying where they were originally typed: `nextPosition` into `blocks.js` (a Blocks-table concern, previously sitting near the Skeleton section only because Skeleton lanes happened to need it first), `createRelationalSpace` into `spaces.js` (a Space-creation function, previously grouped with Blocks), and `getSkeletonSnapshot` into `skeleton.js` (previously living under "--- Trail ---" even though reading the Skeleton is Skeleton's own concern, not Trail's). One deliberate, documented exception: `skeleton.js` and `trail.js` depend on each other (a Skeleton edit logs a Trail entry; a Trail entry snapshots the Skeleton) -- accepted as a genuine mutual dependency rather than merged into one file or awkwardly indirected, safe under Node's ESM module system since every cross-reference happens inside a function body, called lazily at runtime, never at module-load time; both files carry a comment explaining this. Verified end-to-end after the split, not just syntax-checked: the backend boots clean, and a full regression pass (Space/Workspace/block CRUD, Skeleton promotion shorthand, Trail auto-entries, Review draft+create, Work items, all three Report levels, Insights, Templates, the Graph) was run live via curl against a temporary "TEST" Space, confirming the exact same behavior as before the split, before deleting it and confirming the database returned to its known 4-Space baseline. Second, the 31 scattered inline `style={{}}` props across the frontend were audited one by one and 28 -- every one that was a fixed, static value repeated at multiple call sites (an inline-edit field's `width`/`font: inherit`, a stacked form field's spacing, a monospace caption's color) -- were pulled into named classes in a new "Shared field utilities" section of `index.css` (`.field-full`, `.field-inherit-font`, `.inline-title-field`, `.stacked-field`, `.field-width-40`/`55`/`60`, `.field-inherit-heading`, `.list-reset`, `.mono-caption`, `.cursor-pointer`). The remaining 3 were deliberately left inline, not missed: a progress bar's fill percentage (`ProgressView.jsx`, `InsightsPage.jsx`) and the Graph's pan-vs-grab cursor (`GraphView.jsx`) are genuinely computed at render time, so there's no fixed value to name a class after. Verified with a full rebuild (`vite build`, no errors), a fresh `oxlint` pass (same pre-existing warnings as before this pass, nothing new), and a live Playwright check of the Test Space page, Templates page, and Graph page plus interactive clicks on several of the newly-classed inline-edit fields (the Space title, the "Working toward" field), confirming the classes render with the correct computed styles and the click-to-edit behavior is unchanged. Most recently: a category hygiene audit, at the person's explicit request once the organization pass merged -- named as a distinct concern from professionalism/polish: "can I tell what page I'm on," several specific concept pairs (Tools vs. Resources, Spaces vs. Relational Spaces, the Dashboard vs. the Map, Log vs. Insights, Dev Mode vs. Creation Mode), and every control reading as obviously distinct from a similar-looking neighbor. Grounded in a real, live audit before proposing anything -- read the routing table and every page's own header markup, screenshotted the Tools/Log/Insights/Graph/Creation pages, and created a live Relational Space via the API specifically to check whether it stayed identifiable after creation. That audit found the actual answer to "can I tell what page I'm on" was mixed: every page already has its own `<h1>`, but only the Dashboard had the nav bar (`Insights`/`Tools`/`Manage Templates`/`View the Map`/`Log`) -- every other page had nothing but a lone "back to Dashboard" text link, so you could never jump from, say, the Log straight to Insights without funneling back through the Dashboard first; and the browser tab title was hardcoded to "Thinking Spaces" in `index.html`, never updated per route, so open tabs were indistinguishable from the tab bar. It also found two real terminology collisions (a Resource's own suggested type-chips literally offered "tool" as an example, right next to the app's own capitalized Tool concept; the Relational Space form described its seeded blank block as "a blank space for your synthesis," colliding with the separate, formal Synthesis feature) and one stale-copy bug (`CreateSynthesis.jsx` still said "Compile a few existing Assessments and Questions," years behind the ten Work Types Synthesis actually draws from now). Presented all of this as a multi-select confirmation rather than assuming which findings mattered enough to fix; all four were chosen (persistent navigation, dynamic tab titles, a Relational Space marker, and the terminology/stale-copy fixes). Persistent navigation: pulled the Dashboard's own header out into a new shared `TopNav.jsx` (`frontend/src/components/TopNav.jsx`), rendered at the top of every page in the app, with a `current` prop that visually marks whichever nav entry matches the page you're actually on (an underline plus brighter text, `.nav-link-current` in `index.css`) -- a "you are here" signal at the chrome level, on top of each page's own heading. The wordmark itself became a real link back to the Dashboard for the first time (it was already styled like one -- `text-decoration: none` -- just never wrapped in a `Link`), which is what let every top-level page's own separate "back to Dashboard" text link get removed as pure duplication rather than kept alongside a second way to do the same thing; a contextual back-link that goes somewhere more specific than the Dashboard (a Workspace back to its own Space, the Template Editor back to the Templates list, a Space back to whichever other Space referenced it via `?from=`) was kept exactly as before, since that's real information TopNav doesn't carry. Dynamic tab titles: a tiny `usePageTitle(title)` hook (`frontend/src/hooks/usePageTitle.js`) sets `document.title` to `"<page> — Thinking Spaces"`, called once per page with either a fixed name (`"Tools"`, `"Log"`, ...) or the loaded entity's own name once it's fetched (a Space's title, a Workspace's name) -- no cleanup needed on unmount, since the next page's own call overwrites it the moment it mounts. The Relational Space marker and the terminology/stale-copy fixes are covered in the Relational Map and Resources vocabulary entries above and in `CreateSynthesis.jsx`'s own corrected copy -- kept minimal and targeted rather than a wider copy pass, since those were the two specific collisions the audit actually found, not a general copyediting sweep. Verified live end-to-end: `vite build` clean, `oxlint` shows the same pre-existing warnings only, and a Playwright pass confirmed every route's tab title and `TopNav` render correctly (including that a Space/Workspace/Creation page correctly shows no highlighted nav entry, since none of those are one of the five top-level destinations), that a freshly-created Relational Space carries the `relational` tag and renders it as a chip on both the Dashboard and the Space page's own `TagEditor`, and that the two stale "Dev Mode" code comments (never user-facing, but a stale name relative to the Dev Mode decision above) are gone. Most recently: the backend half of a real automated test suite (see the Testing section above), at the person's own diagnosis that the single biggest gap standing between this app and an "expert-grade" build was the total absence of persistent test coverage -- every prior pass had only ever been checked by hand, live, then thrown away. Confirmed two forks via direct question before starting: Vitest over Node's built-in test runner (one tool covers both the Vite frontend and the Node backend, rather than two different runners later), and, once asked how far to take it, "comprehensive from day one" over starting narrower. Built the database-isolation seam first, since without it no backend test could safely run at all: `THINKING_SPACES_DB_PATH`, an environment variable `backend/src/db/index.js` now reads (falling back to the real file when unset), pointed at `:memory:` in `backend/vitest.config.js` so every test file gets its own fresh, isolated database, verified live afterward that the real personal data file's Space count never moved. Then one test file per `backend/src/db/queries/*.js` module, mirroring the organization pass's own split exactly -- 191 tests across all fifteen modules, covering every CRUD path, every cross-Space aggregate, and the deliberate `skeleton.js`/`trail.js` circular pair together. Writing real tests against real behavior, not assumptions, caught three genuine bugs no amount of the prior pass-by-pass manual spot-checking had surfaced: `deleteSpace` never deleted a Space's own `workspaces` rows (the exact foreign-key gap flagged twice before and never fixed -- fixed here, closing Open item 11 for good); `getSkeletonSnapshot`'s articulation reader still read `content.text` directly, a field that stopped existing the moment Text blocks were migrated to their current `{lines}` shape, so every Trail snapshot taken since that migration had silently recorded an empty Current Best Articulation no matter what was actually written (fixed by rejoining `content.lines`, the same join `saveTextBlockWithPromotion` already used for its own comparison); and `getReviewDraft`'s new-blocks filter used a strict `created_at > sinceDate`, which meant a Space's own starter blocks -- added synchronously, right after the Space itself, almost always within the same SQLite datetime('now') second in real usage -- were excluded from that Space's very first Review, silently reporting "nothing new" on a freshly templated Space full of real starter content (fixed by widening to `>=`, accepting the same kind of narrow precision tradeoff already accepted for Milestones). All three fixes were verified twice: once by the new failing-then-passing test, once live against the real dev server. Most recently, at the person's "keep going": the frontend half of the test suite (see the Testing section below), the deliberately separate next slice once the backend half's own infrastructure decisions were settled. Vitest again (one tool for both halves of the app, per the fork confirmed before the backend half began), plus React Testing Library and `@testing-library/user-event` for real rendering and interaction rather than shallow snapshots, and jsdom as the test environment. `frontend/src/api.js` is mocked per test file rather than faking real HTTP calls, since every one of its 40-plus exports already funnels through one `request()` helper -- mocking the module directly is simpler and just as faithful. 167 tests across 20 files: every pure logic module, both registries' own internal consistency (including a self-check that every View's `appliesTo` actually accepts its own demo data -- the same kind of drift-guard the backend's registry tests already established), the shared navigation/dialog/page-title infrastructure built in the category-hygiene pass, all eight Block components, and all five View components. `GraphView` -- a continuous hand-rolled physics simulation -- deliberately got a shallow smoke test instead of deep interaction coverage, a judgment call stated directly rather than quietly skipped: faking the real layout geometry jsdom doesn't provide to test drag/pan/zoom would cost more than it proves for a component whose correctness is best judged by looking at it. Page-level components (Dashboard, SpacePage, the three Creation flows, WorkspacePage) were the one deliberately deferred piece at the time. Most recently, at the person's "finish up all tests": that deferred piece, plus the remaining pages that had no coverage at all yet (`ToolsPage`, `LogPage`, `InsightsPage`, `GraphPage`, `TemplatesPage`, `TemplateEditor`) -- 124 more tests across 12 new files, bringing the frontend total to 291 across 32 files, same patterns as the component-level pass (`vi.mock('../api.js')`, `useNavigate` mocked where a page navigates, `ConfirmDialogProvider` wrapping any page with a delete/confirm flow). `ToolsPage` needed no mocking at all -- it reads straight from `blockRegistry`/`viewRegistry`, the same transparency property the registries themselves are tested for. The recurring snag, resolved the same way each time: React Testing Library's default text matching only reads a single text node, so a line broken across a plain-text run and a nested `<span>` (a Work item's own confidence bar, a Milestone's "N of M reached" line) needs `document.querySelector`/`within()` scoping or a check against the container's own `textContent` instead of a `getByText` regex spanning the whole line -- the same disambiguation habit the component-level pass had already established for duplicate labels, just for a different root cause. Re-verified live afterward: the backend's own 191 tests still pass unaffected, `oxlint` and `vite build` both stay clean, and the real dev server still shows its known 4-Space baseline, confirming the `:memory:` isolation seam still holds. This closes out frontend test coverage as scoped by the original "comprehensive from day one" decision -- Open item 12 below is fully struck through. Most recently, once the test suite's PR was up: the "expert-grade aesthetics" pass (see the Visual direction section above for the full breakdown) -- a type scale, native control theming for the Media placeholder and date inputs, Log page day-grouping/pagination, and (added right after, at the person's explicit request once the first three's PR was up) the Space page's own metadata grouped into a bordered panel plus a widened Graph canvas that no longer clips node labels. Grounded in the same screenshot-plus-code audit method as the professionalism pass; all four of that audit's findings are now built, closing out Open item 13. Most recently: a "capabilities and affordances" pass, at the person's explicit request once they'd already flagged three larger, separately-tracked infrastructure gaps (backup/export, full-text search, and a trash/undo window for deletes -- none built yet, tracked in Open below) and asked what else an expert would flag from a pure UI/UX angle. Presented three candidate buckets (discoverability/affordances, data durability & search, interaction & workflow) via direct question; discoverability was picked as the cheapest, most contained one to start with, with the notification-channel question inside "interaction & workflow" deliberately left open rather than guessed. Five small, independent fixes, each grounded in an actual gap found by reading the relevant code rather than assumed: filter tabs (Dashboard's status filter, a Space's Category and type filters) now show a count per tab, so clicking one is no longer a guess; `NewBlockForm.jsx`'s "+ Add Block" dropdown shows the selected Tool's own registry description live underneath the `<select>` (the same text ToolsPage already shows, just also present at the actual moment of choosing, since a native `<option>` can't carry a description of its own); `SpaceGlyph.jsx` gained a native SVG `<title>` (reusing the same computed description already backing its `aria-label`), so a sighted mouse user can finally hover and learn how to read a shape that CLAUDE.md has always insisted is "computed from real data, never decorative"; the Tools catalog gained a new "Skeleton & Tensions" section, written as plain prose rather than forced into the registry-driven `ToolCard` grid (Skeleton lanes are four ordinary List blocks plus a Text block identified by a marker, not a registered Block/View type), closing a real gap where some of the app's most sophisticated capabilities -- Skeleton filing, typed promotion shorthand, Tension pairing -- had zero presence on the one page whose whole job is "browse everything this app can do"; and `CreateSynthesis.jsx` now mentions promotion up front, so the capability is learned at creation time rather than only discovered by chance later on a Space page that happens to meet the right conditions. Verified live: full frontend suite (301 tests, 5 new files/additions -- filter counts on both Dashboard and SpacePage, the live Tool description, a new dedicated `SpaceGlyph.test.jsx`, and the Tools page's Skeleton section) and backend (191 tests) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean, and every changed page re-screenshotted to confirm each fix reads and behaves as intended. Most recently: Problem Formulation, from the person realizing mid-conversation that their actual first move when starting to think about anything -- naming what a phenomenon or an utterance/behavior/value fundamentally *is*, read through a specific interpretive lens, before Assessment/Question/Analysis even have something settled to work with -- had nowhere to live. Confirmed three forks via direct question before building, all toward the option already implicit in the person's own words ("include an ability to add, like a resource"): the act itself as a new Work Type (not a new, differently-shaped Tool) sharing the existing `{statement, support, confidence}` skeleton; the lenses themselves (etymology, phenomenology, anthropology, history, epistemology, philosophy, and whatever subset of the person's own "78 mode thinking" project earns a place over time) as Resources, not Categories -- reusable and cumulative across every phenomenon a lens gets applied to (a lens-Resource's own page shows every backlink for free, becoming a real, developing record of "how I do this lens" rather than a retyped label); and a new 6th starting Template, Problem Formulation, rather than leaving the Work Type to be added to any Space by hand. Landed as: `formulation` added to `WORK_TYPES` (backend) and the frontend registry (`FormulationBlock.jsx`, icon `⊢` for "derives/proves," support labeled "Grounds"); `"lens"` added alongside `"book"`/`"person"`/... as a suggested Resource type tag in `CreateResource.jsx`; and a new Template in `seedTemplates.js` seeding a blank Writing Surface, a "Lenses to Consider" checklist pre-filled with the person's own six habitual disciplines, and a blank Formulation block. Verified live exactly the way every prior Work Type rollout has been: the new Type showed up in the Tools catalog, the "+ Add Block" form, and Synthesis's picker with zero changes to Synthesis's own code, and the new Template appeared as a 6th cluster card in Creation Mode with zero frontend changes at all, purely from `seedTemplates()`'s existing idempotent seeding running once against the real database.

Most recently: the Interaction & Workflow pass, the third of the three buckets raised in the same "what would a senior engineer flag" conversation that produced the discoverability & affordances pass -- data durability & search (Open item 15) is still untouched, deliberately. Confirmed the one genuinely open fork (what "proactive notification" should even mean, since there was no obvious default) via direct question: a louder in-app badge, not email or a desktop notification, keeping the app self-contained rather than adding an external delivery dependency. Four pieces, each independent:
- **Save/delete confirmation.** Nearly every edit in this app is click-to-edit-then-blur-to-save with nothing else confirming it actually took. Rather than retrofit a toast call into dozens of individual save handlers, the fix sits at the one real chokepoint every mutation already passes through: `api.js`'s private `request()` helper now calls a registered listener on a successful PATCH ("saved") or DELETE ("deleted") -- POST is deliberately excluded, since creating something is already followed by an obvious change (a new item appears, or the page navigates), so a toast on top would be noise, not reassurance. `Toast.jsx`'s `ToastProvider` (mounted at the app root, same pattern as `ConfirmDialogProvider`) registers itself as that listener and renders one fixed-position slot -- a second mutation while a toast is already showing resets its own dismiss timer rather than stacking a second bubble, since routine actions like checking off several List items in a row fire more than one PATCH in quick succession.
- **A "needs attention" badge in TopNav.** Visible on every page, since TopNav already renders everywhere. Deliberately narrow: `getNeedsAttentionCount()` (`backend/src/db/queries/dashboard.js`) sums three already-actionable signals -- overdue List `reviewBy` items, overdue Space due dates, and overdue/unreached Milestones -- behind one new `GET /notifications/count` endpoint, cheap enough to call on every page load without pulling the full Insights payload just for a number. Trail Review staleness ("never reviewed"/"14+ days") is deliberately left out of the count, on purpose: it's true of nearly every Space nearly all the time (see `getTimeInsights`), so counting it here would make the badge read as permanently alarmed rather than a genuine signal worth a glance -- the same "notification fatigue" concern that ruled out a louder channel in the first place.
- **Quick capture.** Getting a thought into the app previously always meant the full Creation Mode flow. Confirmed via direct question which shape it should take: just a title (not a first-thought box with the title derived from it) -- type a few words into a small inline field that opens right in TopNav, hit Enter, land on a new blank Space with that title, the same "Start Blank" a Space already supported minus every step in between. No new backend endpoint needed; it's the same `createSpace({ title })` Creation Mode's own blank path already calls.
- **Adaptive density.** A quick scratch Space and a deep, months-long one previously carried identical visual weight -- the Space page's own `.space-details-panel` (see the Visual direction entry above) always rendered fully expanded regardless of whether anything in it was actually set. It's now a native `<details>`, same family as a Dashboard digest, defaulting open or closed based on `spaceHasMetadata()` (accent, goal, due date, tags, Categories, or an unpromoted internal Synthesis worth acting on) -- computed once from the Space as first loaded via an initializer effect, not recomputed on every render, so a manual open/close toggle later in the session is never fought by React re-asserting a stale default.

Verified live end-to-end against the real app, not just the test suites: quick capture created a real Space and navigated to it; editing that Space's title showed the "Saved" toast in the corner; its own details panel started collapsed (a brand-new Space has no metadata yet); and the real badge already showed "1" from the one pre-existing overdue Resource in the seed data, confirmed by hitting `/api/notifications/count` directly. The test Space created during that check was deleted afterward, restoring the real database's known 4-Space baseline. `npx vitest run` (323 tests, 16 new -- covering the badge/quick-capture/notification-degradation behavior in `TopNav.test.jsx`, the toast timing/coalescing behavior in a new `Toast.test.jsx`, the mutation-listener behavior in `api.test.js`, and the adaptive-density defaults in `SpacePage.test.jsx`) and the backend suite (199 tests, 7 new covering `getNeedsAttentionCount`) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean.

Most recently: a persistent left sidebar, replacing the horizontal `TopNav.jsx` bar entirely, at the person's explicit request that the desktop version "feel spread out like a desktop app typically would be rather than an expanded version of the mobile." Confirmed via direct question which structural change that actually meant, among three options raised (a wider centered column, a persistent sidebar, or a multi-column dashboard) -- a persistent sidebar was picked as the most recognizably "desktop app" pattern (Slack/Notion/Obsidian all share it), and the biggest structural change of the three. `frontend/src/components/TopNav.jsx` is gone; `Sidebar.jsx` carries over everything it held (the wordmark-as-home-link, the five top-level nav entries with a `current`-page marker, the "needs attention" badge, quick capture) rearranged into a fixed vertical rail instead of a horizontal bar. One deliberate architectural choice made and verified rather than assumed: kept the existing "each page renders its own `<Sidebar>` instance" pattern (same as `TopNav` always did) instead of hoisting it once to `App.jsx` outside the router -- hoisting would give a technically "truer" persistent element that never remounts on navigation, but would also mean rewriting every one of the ~14 page-level test files' render helpers for a difference invisible to a user in an already-fast SPA; confirmed safe by running the full suite afterward; nothing broke, since React Testing Library's queries are role/text-based, not sensitive to where in the tree the sidebar mounts. The old `#root`-centered, 900px-capped column is gone too -- replaced by `.app-shell` (a flex row: the sidebar, then `.app-content`), and `.app-content` deliberately does *not* re-center itself in the leftover space; it hugs the sidebar's edge at up to 1100px wide, which is the actual "spread out" effect being asked for, rather than a slightly wider version of the same centered-mobile-column feel. This also retired the Graph page's `.graph-frame`/`.graph-frame-inner` viewport-relative breakout hack (`calc(50% - 50vw)`), which only ever worked because `#root` was a centered column to begin with -- GraphPage now just applies a new `.app-content-wide` modifier (no max-width cap) instead of computing a manual full-bleed escape. `ToolsPage` was deliberately left at the ordinary, capped `.app-content` width even though it could take the wide variant -- its `.tool-grid` is a fixed two-column grid, so extra width would only enlarge the existing cards, not add a third column, and that's a separate, unscoped layout change, not part of this pass. Below 760px the sidebar collapses back into a horizontal bar via a new media query, matching the app's existing "narrow-viewport gets minimal, functional care" pattern used elsewhere (`.view-grid`, `.log-stats`) rather than a bespoke mobile nav. Verified live end-to-end: `npx vitest run` (324 tests, net +1 after replacing `TopNav.test.jsx`'s 10 tests with `Sidebar.test.jsx`'s 11) and the backend suite (199 tests) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean, and every page was screenshotted after the change (Dashboard, Tools, Insights, Log, the Space page, the Graph, and the narrow-viewport fallback) confirming the sidebar, the current-page marker, the needs-attention badge, the wider content pane, and the Graph's no-longer-clipped node labels all render correctly -- plus a direct `scrollWidth`/`clientWidth` check on the Log page (the app's longest-scrolling page, the real stress test for a `position: sticky` sidebar) ruling out any horizontal-overflow regression from the new flex-row shell. The desktop icon/launcher part of the same request, and the separate dashboard-titling ask, are both still open -- see Open below.

Most recently: a Windows desktop launcher, once the person supplied what building one actually needed -- their OS (Windows 11 Pro) and the absolute local path to their own clone of this repo (`C:\Users\twglo\thinking-spaces`), since this session runs in an ephemeral remote container with no relationship to their actual machine and couldn't have built this without that information. A new `launcher/` folder, deliberately separate from the app's own `frontend`/`backend` code: `Start-ThinkingSpaces.bat` (`git pull`s, installs each side's dependencies only the first time `node_modules` is missing, starts both dev servers each in their own window, then opens the browser) and a one-time `Setup-Desktop-Icon.ps1` that creates a "Thinking Spaces" desktop shortcut pointed at it, using a hand-drawn `thinking-spaces.ico` -- a thought bubble (the shape the person specifically asked for) built from overlapping circles in the app's own matte-black/oxblood/gold palette (`--bg`/`--maroon-bright`/`--accent` from `frontend/src/index.css`), not a stock icon, with the smallest trailing dot in the same gold the app's own manual accent layer uses -- a deliberate small echo of that idea, not a random color choice. `README.md`'s existing manual two-terminal instructions stay in place underneath a new pointer to the launcher, since they're still what the launcher is doing under the hood and still useful for reading an error a server window scrolled past.

Most recently: the Dashboard's Week digest, redesigned into a real calendar grid, from the person naming the actual problem with the old one directly -- "the 'this week' thing doesn't make it obvious how it knows what happened 'this week'," paired with asking whether something more like a 7-day forecast/schedule with real click-through existed. Presented three options via direct question (add a separate week-ahead section, just relabel the existing backward-only list, or a real day-by-day calendar grid); the calendar grid was picked -- the biggest change of the three, and the only one that answers "how does it know" by construction rather than by adding an explanation. `listRecentTrailEntries()` (a flat, unlabeled rolling-7-days-back read) is gone; `getWeekCalendar()` (`backend/src/db/queries/dashboard.js`) returns the current calendar week, Sunday through Saturday, as seven day buckets, each carrying that day's actual Trail entries plus whatever's due that day (a Space's own `due_date`, a Milestone's `targetDate`) -- past days read as a small log, today and later read as a small forecast, and a reached Milestone counts as past activity while an unreached one counts as upcoming. `Dashboard.jsx`'s new `WeekCalendarDigest` renders this as seven columns (collapsing to one below 700px, the same narrow-viewport pattern used elsewhere) with the actual date range in the digest's own header (`This week (Aug 30 – Sep 5)`) -- the header text itself is now the answer to how it knows, not something you have to trust. Click-through was confirmed to stay exactly as it already worked everywhere else on the Dashboard -- straight to the Space a Trail entry/due date/Milestone belongs to, not a new deep-link into a specific block, which the Space page has no way to jump to yet (see Open). Verified live: a temporary Space with a due date, an unreached Milestone, and a manual Trail note all set to today rendered correctly in today's column (past activity plain, due/upcoming items marked with the Time family's own gold accent), then deleted, restoring the real database's known 4-Space baseline (which currently has nothing at all landing in this week, so the whole digest correctly stays hidden on the real Dashboard -- same "hide entirely when there's nothing to show" rule every other digest already follows). `npx vitest run` (329 tests, 5 new covering the calendar's empty/trail/due/Milestone/today-marker behavior) and the backend suite (203 tests, 4 new covering `getWeekCalendar`) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean. A second, separate observation from the same message -- that the Tools catalog's demos read as static rather than clickable -- was confirmed as its own pass (make them genuinely interactive without ever persisting) and is tracked as not yet started, see Open.

Most recently: interactive Tools-catalog demos, the second observation from the same conversation as the Week-calendar redesign above -- confirmed via direct question to make every demo on `/tools` genuinely clickable (checkboxes toggle, text opens for editing, items can be added/reordered) without ever persisting anything, rather than the cheaper option of just labeling them as view-only. `ToolsPage.jsx`'s new `DemoBlock` wrapper holds each demo's own content as local component state and passes it back in through `onSave` instead of a real block id -- reusing the exact `onSave` override every Block component already supported for a Comparison-embedded side (see `ReferenceBlock.jsx`'s own comment), rather than inventing a second, demo-specific mechanism. `TextBlock`, `ReferenceBlock`, `MilestoneBlock`, and `SessionBlock` already had this override; `ListBlock`, `MediaBlock`, `ComparisonBlock`, and `WorkBlock` gained it in this pass, each following the identical pattern already established by the other four -- `editable = Boolean(block.id) || Boolean(onSave)`, and a mutation redirects to `onSave(newContent)` in place of `updateBlockContent(block.id, newContent)` when the override is present. `WorkBlock`'s Space-blocks fetch (for resolving/offering linked support points) was switched from gating on `editable` to gating on `block.id` specifically, since a demo is "editable" via `onSave` but has no real `space_id` to fetch against -- the same distinction the whole pass turns on: `editable` now means "does the UI let you interact," not "is there something real behind this to persist to." Caught one real, live-only bug in the process that the frontend test suite's own 338 tests never could: `.tool-demo` still carried a `pointer-events: none` rule from when a demo was deliberately inert, silently blocking every click in a real browser while every Testing-Library test kept passing regardless, since jsdom doesn't enforce real CSS pointer-events the way a browser does -- found only by a live Playwright click landing on the wrong element, and removed. Verified live afterward, not just re-run against the suite: the List demo's checkbox toggled and its "+ Add item"/move/remove controls appeared, the Text demo opened a real editable textarea on click, the Reference demo's trust marker cycled, and the Milestone demo's reached checkbox toggled with a live "REACHED" badge -- each confirmed by an actual click landing where it should, not by assuming the fix generalized from one Tool to the rest. `npx vitest run` (338 tests, 9 new -- one `onSave`-override test each for List/Media/Comparison/Work, plus three new interactive-demo tests on `ToolsPage.test.jsx` itself, including one proving a demo's own edits never mutate the registry's shared `demoBlock` data across separate renders) passes, `oxlint` shows the same pre-existing warnings only, `vite build` clean. Views were deliberately left untouched -- they're already read-only, computed lenses with no edit surface to begin with, so there was nothing gating their own demos the way `editable` gated a Block's.

Most recently: the app went live at `https://thinking.thegardners.xyz` -- Hosting's Step 2 (see the Hosting section above) completed end-to-end against the real Cloudflare account, not just verified locally. Live deployment surfaced three real things the local-only verification couldn't have caught: (1) GitHub Pages needs its "Build and deployment" source explicitly set to "GitHub Actions" once per repo before `actions/deploy-pages` can publish anything -- the first deploy failed on this, fixed with a one-time dashboard setting, then re-triggered via the workflow's `workflow_dispatch` trigger rather than an empty commit; (2) a custom domain proxied through Cloudflare (orange cloud) blocks GitHub's own Let's Encrypt certificate issuance for that domain -- worked around by temporarily switching the DNS record to "DNS only," letting GitHub finish issuing the cert and enabling "Enforce HTTPS," then switching back to "Proxied" (which is what actually lets the Worker's `/api/*` route intercept traffic -- the app doesn't function on DNS-only); (3) the Cloudflare Access application gating `thegardners.xyz` needed `thinking.thegardners.xyz` added as an additional public hostname on the *same* application (not a new one) to inherit the existing PIN policy. The real, already-accumulated personal data (8 Spaces, 41 blocks, 4 Trail entries, 35 activity log entries) was migrated via `export-to-d1.mjs` and confirmed live on the real site, not just locally. A companion desktop shortcut (`launcher/Setup-Desktop-Icon-Web.ps1`) opens the live site directly for everyday use, alongside (not replacing) the existing local-dev shortcut.

Also most recently, a small fix from the same round of feedback that prompted the deployment above: `WorkBlock.jsx`'s "🔗 Link a claim" feature was reported as feeling broken -- it wasn't (verified against the code and this session's own earlier live testing of the exact same flow) -- the real gap was that a Space with no other Work items or Skeleton-lane claims yet just showed a bare "No other claims yet to link to," which reads as broken rather than "correctly empty and here's why." Fixed with a `title` tooltip on the button itself (explaining the feature before it's even clicked) and a real explanatory empty state naming what a "claim" actually is and what to add first.

Most recently: a vocabulary audit, the fourth thread of the large feedback batch above (item 24) -- the person named two specific words as feeling wrong ("Block" reads brutalist, "Text" as a Tool name feels patronizing) and, via direct question, confirmed treating this as a real audit of the app's naming surface rather than a two-word patch. Read every button, heading, tooltip, empty state, error message, and generated Report/Log/Review sentence across both the frontend and the backend (not just the two named spots) before proposing anything, then confirmed four things via one direct-question batch rather than picking words unilaterally: **Block → Entry** (over Item, which was already used elsewhere for List/Work items and would have added a new ambiguity rather than removing the old one); **Text → Writing** (matching "Writing Surface," already the Workshop's own name for this same Tool); widening the audit to one more term the read-through surfaced but the person hadn't named -- **Skeleton "lane" → Skeleton "section"** in visible copy (a lane is shown directly in tooltips and the Tools catalog, e.g. "File this line into a Skeleton lane," and reads like a leftover traffic/bowling metaphor rather than something chosen); and, on scope, confirmed a **copy-and-labels-only** rename over renaming through the code as well -- `blockRegistry`, every `*Block.jsx` file and component name, the `blocks` table, `block.type`, and every JSON field name (`skeletonLane` included) all keep their original names, since nothing about the code itself needed to change, only what's read on screen. Landed as: `blockRegistry.text.label` changed from `'Text'` to `'Writing'` (which cascades for free through every registry-driven surface -- the Tools catalog card, the "+ Add Block" dropdown's live description, the block-type-tag, Synthesis's item picker, the type-filter strip); every literal on-screen string containing the word "block" as a noun for a piece of content changed to "entry" (`"+ Add Block"` → `"+ Add Entry"`, `"Block type:"` → `"Entry type:"`, `"No blocks yet."` → `"No entries yet."`, `"Remove block"`/`"Delete block"` → `"Remove entry"`/`"Delete entry"`, `"Unknown block type"` → `"Unknown entry type"`, the `ToolCard` kind badge, the Tools-catalog h2/intro prose, and a Template's own delete-confirmation copy); and every generated sentence a person actually reads -- a Report's `"Structure (N blocks)"` heading and `"(no summary available for block type ...)"` line, a Review's `"N blocks added"` summary, and the Log/Trail's own `"Added a text block to ..."`/`"Removed a ... block from ..."` activity sentences (in both `backend/` and its `worker/` mirror) -- updated to match, plus the three literal `"Skeleton lane"` mentions in tooltips/catalog prose and a Report's `"Skeleton lane: ..."` line changed to "section." A `"Block not found"` API error (surfaced verbatim to the user on a stale/deleted reference, since `api.js`'s `request()` throws the backend's own `error` string) became `"Entry not found"` in both `backend/` and `worker/`. Deliberately left alone: the Test Space's own seeded demo content in `backend/src/db/seedTestSpace.js` (a handful of lines that narrate the app's own design history in first person, e.g. "Blocks are the only content primitive") -- that's simulated *written* content from a specific moment, not app chrome, so it was left as an honest artifact of when it was written rather than retroactively updated to current terminology, the same reasoning this document's own historical Roadmap entries below aren't being rewritten either. Verified end-to-end: `npx vitest run` (338 frontend tests, `backend/`'s 203) both needed a handful of test-assertion updates to match the new copy (an exact-text or exact-heading match on old wording, never new behavior) and now pass in full, `oxlint` shows the same pre-existing warnings only, and `vite build` stays clean.

Most recently: Projects, the second thread of the same large feedback batch (item 24(b)) -- Sessions and Milestones becoming a real structural concept, from the person asking "is [a Session] its own unique thing within a Space? It should be. Same for milestones, which should be associated with goals/projects." Confirmed three forks via direct question before building, all toward the bigger/more structured option: a real dedicated entity per Space (like Workspaces), not just a shared free-text field on Milestone/Session content; closing the separate gap that Sessions never appeared on the Dashboard's Week calendar at all (only Milestones and Space due dates did); and a dedicated one-click "Start a Session" affordance, not just the generic "+ Add Entry" dropdown. Landed as: a new `projects` table (id, space_id, name -- see Data model), a Milestone/Session joining one via a single nullable `properties.projectId` (not an array -- a checkpoint or a timed sitting most naturally serves one project, unlike a Tool's many-to-many Workspace membership). Named "Project," not "Goal," after catching a real naming collision while wiring this into Reports: a Space already has its own `goal` field ("Working toward," a single free-text line for what the Space as a whole is working toward) -- confirmed via direct question to rename the new concept rather than let the two collide or rename the older, already-shipped field. `frontend/src/pages/ProjectPage.jsx` mirrors `WorkspacePage.jsx` closely but scoped specifically to Milestone/Session blocks: shows every Milestone/Session assigned to the Project, a computed progress line (`getBlocksForSpace`'s own already-fetched data -- no new backend aggregation needed, same reasoning `WorkspacePage`'s member list already uses), lets you add a new Milestone or start a new Session directly into it, pull in one already on the Space, remove one (clearing `projectId`, never deleting the block), and delete the Project itself (which -- same precedent `deleteWorkspace` set -- only ever removes the Project row; a block's own stale `projectId` is left exactly as-is, resolved gracefully wherever it's read). A `ProjectList` section on the Space page (next to `WorkspaceList`) and an inline `BlockProjectPicker` (a plain `<select>`, not a chip-toggle, since membership is single-valued) let a Milestone/Session's Project be created/set from the ordinary feed too, not just from the Project's own page. `frontend/src/blocks/sessionActions.js`'s `newSessionSpec()` is the one place a quick-started Session's starting shape is defined, shared by the Space page's own "▶ Start a Session" button and the Project page's own -- so the two can't drift on what a freshly-started Session looks like. `getWeekCalendar` (`backend/src/db/queries/dashboard.js`) gained a `sessions` bucket alongside its existing `trail`/`dueSpaces`/`milestones` ones -- a completed Session lands on the day it ended, a still-running one on the day it started -- and both Milestones and Sessions now resolve their own Project name (looked up once per call, not per row) so the Dashboard's Week digest can show, e.g., "reached: Ship it (Ship the redesign)." `getBlockReport` gained a `Project: <name>` membership line, same shape its existing `Workspaces: ...` line already has. Every backend change was ported to its `worker/` mirror in the same pass, per this session's own standing practice -- `worker/schema.sql`'s new `projects` table needs applying to the real, already-deployed D1 database (`npx wrangler d1 execute thinking-spaces --remote --file=schema.sql` from `worker/`, the same idempotent `CREATE TABLE IF NOT EXISTS` command the original deployment used) before this feature works on the live site; not yet run, since this session has no live Cloudflare access of its own. Verified live end-to-end against a temporary local backend, not just the test suites: created a Project, assigned a Milestone and a Session to it via `properties.projectId`, confirmed the Block Report's "Project: Ship the redesign" line, the Space Report's "Structure (2 entries)" heading, and the Week calendar correctly showing both under today's date with the Project name attached; then deleted the Project (Milestone survived, stale id left in place) and the Space (no foreign-key violation on the new `projects` table, closing off the exact class of bug `deleteWorkspace`'s own precedent was written to avoid repeating). `npx vitest run` (359 frontend tests, 17 new -- a `projects.test.js`, `ProjectPage.test.jsx`, and additions to `SpacePage.test.jsx`/`Dashboard.test.jsx`) and the backend suite (215 tests, 12 new) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean. Deliberately not built in this pass, consistent with not assuming more than was asked: Projects as nodes/edges on the Relational Map (Workspaces' own Graph integration was originally a separately deferred, later-confirmed piece too), a Projects facet on Insights, and a Project-level Report -- none raised by the person, so none built speculatively.

Most recently: expanding SpaceGlyph's reactivity, the third thread of the same large feedback batch (item 24(c)) -- widening the glyph beyond its original three computed dimensions (status, relationDensity, openTensionCount) to more already-computed state, per the person's own list of candidates (overdue, Milestone-reached ratio, Review staleness). Caught a real design snag before building anything: Review staleness (14+ days since the last Review, or never reviewed) is true of nearly every Space nearly all the time, per `getTimeInsights`' own reasoning for deliberately excluding it from the "needs attention" badge (Interaction & Workflow pass, above) -- showing it on every glyph would repeat that exact mistake, reading as permanently alarmed rather than a genuine signal. Confirmed via direct question to drop it rather than force it in, keeping the widened glyph to the two dimensions that actually carry an occasional, real signal. Landed as: a new `milestoneStats` (`{reached, total}`) computed field on every Space, alongside the existing `isOverdue` (already computed, just not yet read by the glyph) -- `backend/src/db/queries/spaces.js`'s `getMilestoneStats(spaceId)` mirrors `getRelationDensity`/`getOpenTensionCount`'s own per-Space subquery pattern exactly, using `json_extract(content, '$.reached')` summed directly in SQL (SQLite's JSON functions read a JSON boolean back as native 1/0) rather than pulling every Milestone into JS first. `SpaceGlyph.jsx` draws `isOverdue` as the trunk itself going dashed instead of solid, and Milestone progress as a column of small rings alongside the trunk (one per Milestone, capped at 4, filled from the bottom up as each is reached) -- deliberately offset a few px to the side rather than drawn on the trunk's own centerline, so it never visually collides with the existing crack (open Tensions), which already draws through that exact line. The accessible description (both `aria-label` and the native `<title>` hover tooltip) gained matching clauses -- "...overdue" and "...N/M milestones reached" -- so the new dimensions are always nameable in words even where the visual encoding is necessarily subtle at a 28px size. Ported to `worker/` in the same pass. Verified live against a temporary local backend: created a Space with two Milestones (one reached), confirmed `GET /spaces/:id` returned `milestoneStats: {reached: 1, total: 2}`. `npx vitest run` (363 frontend tests, 6 new in `SpaceGlyph.test.jsx`) and the backend suite (217 tests, 2 new) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean.

Most recently: Resource Templates, the fourth thread of the same large feedback batch (item 24(d)) -- from the person's own note, "Resource templates (like mini space templates, ability to expand on/modify these categories as needed)," naming 17 specific types (Book, Lecture, Article, Art Piece, Lesson, Parable, Video, Seminar, Website, Song/Musical Piece, Aphorism, Debate, Essay, Film, Poem, Story, Riddle). Confirmed three forks via direct question before building, all toward the bigger/more structured option -- notably, all three went the *opposite* way from the recommendation offered: a Resource Template **replaces** CreateResource.jsx's three generic descriptive facets entirely for that type, rather than adding a few on top of them; it's a **new, separate mechanism** (a new `resource_templates` table and its own editor page) rather than reusing the existing Templates table/editor, even though that reuse would have made "expand/modify" come for free with zero new admin UI; and starter content was written for **all 17 named types in this one pass**, rather than 2-3 first to prove the pattern the way every other type rollout in this app (Work Types) had done. Landed as: `backend/src/db/queries/resourceTemplates.js` (list/get-by-id/get-by-type/create/update/delete, mirroring `templates.js`'s own CRUD shape) backed by a new `resource_templates` table (id, type, label, facets -- see Data model); `backend/src/db/seedResourceTemplates.js` seeds all 17, each with three genuinely distinct facets written specifically for that type (a Book's "Core Argument or Story"/"Key Passages"/"Personal Notes" reads nothing like a Debate's "Position For"/"Position Against"/"Where You Land"), following the exact same idempotent per-id `seedTemplates()` pattern. `CreateResource.jsx` looks up a Resource Template by the first chosen type tag that actually matches one (checked in order, not in parallel, so a slower-resolving earlier tag can't be overtaken by a faster later one) and swaps its three facets in for the generic ones; removing that type tag falls back to the generic three again. The fourth, structural facet (Touches / Touched By -- the cross-Space reference picker) was deliberately kept universal across every type rather than folded into each Template's own `facets`, a judgment call made and documented directly rather than assumed away: it's a mechanical capability (create a Reference to an existing Space), not a description that varies by what kind of thing a Resource is, so removing it for some types would be a real capability loss, not a design choice the person's own note seemed to be asking for. `ResourceTemplatesPage.jsx`/`ResourceTemplateEditor.jsx` (new pages, mirroring `TemplatesPage.jsx`/`TemplateEditor.jsx`'s own shape closely but editing facets instead of a block arrangement) give the "expand/modify as needed" capability the person explicitly asked for -- reached via a contextual link from the ordinary Templates page and from CreateResource.jsx itself, not a new top-level Sidebar entry, since it's a configuration page for one specific creation flow rather than a destination in its own right. Ported to `worker/` in the same pass, including a generated `worker/resource-templates-seed.sql` (same "generated from the real seed file, not hand-transcribed" discipline `templates-seed.sql` already established) -- still needs applying to the real, already-deployed D1 database (`npx wrangler d1 execute thinking-spaces --remote --file=schema.sql` then `--file=resource-templates-seed.sql` from `worker/`) before this feature works on the live site. Two small things caught and fixed while building, unrelated to Resource Templates itself but surfaced by working nearby: `TemplatesPage.jsx`'s own entry-count label had been missed by the earlier vocabulary-audit pass and still said "block(s)" instead of "entry/entries," fixed here; and `backend/test/helpers/resetDb.js`'s `TABLES` list was missing both `projects` (from the Projects pass) and the new `resource_templates`, a real test-isolation gap that surfaced immediately once a `resourceTemplates.test.js` test actually depended on a clean table between runs -- both added. Verified live end-to-end against a temporary local backend: fetched all 17 seeded Resource Templates, looked up "poem" by type, created a Resource tagged "book" and confirmed its Categories came from the Book template's own facets rather than the generic three, then exercised full CRUD (create/update/delete) on a custom "podcast" Resource Template directly. `npx vitest run` (381 frontend tests, 18 new across `CreateResource.test.jsx` additions and two new page test files) and the backend suite (225 tests, 8 new) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean.

Most recently: Synthesis's Kinds and Source Material, the fifth thread of the same large feedback batch (item 24(e)) -- "needs more Kinds," and "what is Source Material?" Read the actual code before proposing anything, which reframed the second half: "Source Material" is a clear enough term on its own, but the block it names had no visible label at all once you actually landed on the new Synthesis's Space page -- an ordinary, unmarked Writing entry, indistinguishable from any other. The real gap was a labeling gap, not a wording gap. Landed as: every one of `CreateSynthesis.jsx`'s three starter blocks now carries a real Category -- `Drawn From` (the Reference blocks pointing back to each source Space), `Source Material` (the copied excerpt text), and `Draft` (the blank block to actually write in) -- mirroring `CreateResource.jsx`'s own already-categorized starter blocks, so all three show up as named, filterable sections on the Space page immediately, the same clarity a Resource's four facets already have. `SYNTHESIS_KIND_SUGGESTIONS` widened from 4 (essay, story, definition, writing-entry) to 10, adding poem, letter, dialogue, argument, summary, and proposal -- but the more durable fix underneath it: a free-text `+ custom kind` input next to the suggestion chips, the exact entry pattern `CreateResource.jsx`'s own type tags already use, so a Kind is never capped by whatever's in the suggestion list again (a real, previously-unnoticed asymmetry between the two flows -- Resources could already be freely typed, Synthesis Kinds couldn't). Kind stays single-valued (unlike a Resource's several type tags), so typing a custom one replaces whichever suggestion chip was active, the same as clicking a different one already does. Needed zero backend or `worker/` changes at all -- `createSpace`'s `categories`/`properties.categories` support already existed for `CreateResource.jsx` to use, so this was a purely frontend pass. Verified live against a temporary local backend that a Space created with a Draft-categorized starter block returns that Category correctly. `npx vitest run` (382 frontend tests, 1 new) passes, `oxlint` shows the same pre-existing warnings only, `vite build` clean.

Most recently: interpretive readings on Insights, the sixth thread of the same large feedback batch (item 24(f)) -- "go into the sight of the data ... help us realize the relevance, help us gather the insight," as opposed to Insights' existing bars and raw counts. Landed as a `reading` field on all five facets' payloads (`getWorkMixInsights`/`getThemeInsights`/`getActivityTrendInsights`/`getProvenanceInsights`/`getTimeInsights` in `backend/src/db/queries/insights.js`) -- one short, computed sentence naming what's actually notable in that facet's own numbers, not a restatement of them: which Work Type and confidence level dominate and what share of claims have actually settled; which Category is the strongest cross-cutting theme and how many Tensions sit unresolved; whether activity is picking up or quieting down week over week, plus how many Spaces have gone stale; how much of what exists was brought in versus produced, and roughly what share of raw Work has been distilled into a Synthesis; and how many things are overdue, Milestone progress, and minutes actually logged. Each is pure string composition over data the facet's own query already fetches -- no new queries -- and returns `null` (rendered as nothing extra) whenever there genuinely isn't anything to say yet, the same empty-state discipline every other Insights facet already follows. `InsightsPage.jsx` renders each as a `.insight-reading` callout -- a small `--accent`-bordered block sitting between a section's existing intro paragraph and its bars/lists -- deliberately the same "sits on top of, never replaces the base" role `--accent` already plays for Visual Identity's manual layer, so the raw numbers stay exactly as legible as before; the reading is additive framing, not a replacement for the data underneath it. Ported to `worker/` in the same pass (the reading-builder functions are pure JS, identical on both sides). Verified live against a temporary local backend: two solid-confidence Assessments produced "Assessment is your most common way of thinking here (2 of 2)... 100% of claims have settled into 'solid' or 'certain'." `npx vitest run` (384 frontend tests, 2 new) and the backend suite (235 tests, 10 new) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean.

Most recently: giving the Week view real value, the seventh and final thread of the same large feedback batch (item 24(g)) -- once a plain text brainstorm of four candidate directions was presented via direct question (make it actionable in place, a weekly ritual, surface undated things worth scheduling, or leave it as-is), the person picked two: "actionable in place" and "a weekly ritual." Both build directly on top of the existing calendar grid rather than replacing it. For "actionable in place": `getWeekCalendar()` (`backend/src/db/queries/dashboard.js`, ported to `worker/src/db/dashboard.js`) now includes each Milestone/Session's own block `id` and full `content` alongside the summary fields it already returned -- needed because `updateBlockContent(id, content)` takes the whole content object, not just the field that changed. `Dashboard.jsx`'s `WeekCalendarDigest` uses this to render a "Mark reached" button next to an upcoming Milestone and a "Stop" button next to a running Session, right in the calendar cell -- the same mutation `MilestoneBlock.jsx`/`SessionBlock.jsx` themselves make (`reached`/`reachedAt`, or `endedAt`/`durationMinutes` computed from the persisted `startedAt`), just reachable without leaving the Dashboard, followed by a refetch of the week's data so the cell updates in place. For "a weekly ritual": a new "📋 Review this week (N Spaces)" button appears in the digest's header whenever at least one Space was genuinely active this week -- the union of Space ids behind a Trail entry, a Milestone actually reached, or a Session that actually completed, computed by a new `computeActiveSpaceIds()` helper. A due date merely arriving is deliberately excluded from that union: a date passing on its own isn't an in-Space change worth reviewing, only real activity is. Clicking it, after a themed `confirm()` (the app's own dialog, not the native browser one), loops the existing per-Space `createReview(spaceId)` API once per active Space -- no new backend endpoint, since Review was already built to be triggered one Space at a time. Verified live end-to-end against a temporary local backend, not just the test suites: created a Milestone and a running Session on a temporary Space, confirmed `GET /api/dashboard/week` carried their real `id`/`content`, marked the Milestone reached and stopped the Session via the same `PATCH /blocks/:id` call the buttons make, then logged a real Review for the Space via `POST /spaces/:id/reviews` and confirmed its summary correctly counted both changes ("2 entries added, 1 milestone reached, 42 min logged") before deleting the Space and confirming the real database's known 4-Space baseline held. `npx vitest run` (388 frontend tests, 4 new covering the Milestone/Session actions and the Review-this-week button) and the backend suite (235 tests, one existing assertion updated to match the widened Milestone payload) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean. This closes out the large feedback batch (item 24) in full -- all seven threads are now done.

Most recently: a Projects-vs-Workspaces audit, from an outside review of the repo flagging that the two "may be the same concept wearing two different cardinality rules" -- the `projects` table's own schema comment already called Projects "the Time family's own equivalent of a Workspace," which is what prompted checking rather than dismissing the claim. Read `projects.js`/`workspaces.js`, `ProjectPage.jsx`/`WorkspacePage.jsx`, `ProjectList`/`WorkspaceList`/`BlockProjectPicker`/`BlockWorkspacePicker` on `SpacePage.jsx`, and every read/write of `properties.projectId`/`properties.workspaces` end to end before proposing anything. Found the data layer genuinely is near-identical (the two query modules are the same CRUD functions with names swapped), but real divergences exist beyond cardinality: Project membership is UI-restricted to Milestone/Session (not enforced at the data layer), `ProjectPage` shows a live progress readout `WorkspacePage` has no equivalent of, and -- the strongest tell either way -- Project had literally zero CSS of its own, reusing `.workspace-*` classes verbatim, while separately lacking a page-level Report, Graph nodes/edges, and (attempted, then reverted once shown to be unreachable -- see below) Focus Mode, none of which Workspace lacks. Presented three options via direct question, all confirmed honestly rather than assuming "merge them" was the answer: keep both separate and close the incidental gaps (chosen), merge the two pages into one shared component while keeping the data model separate, or a full schema/page merge with a live-data migration for both the local SQLite file and the deployed D1 database. The full-merge option was scoped but not built -- the person picked closing the gaps, on the reasoning that the type-scoping and exclusive cardinality reflect a real conceptual difference (goal/progress-tracking vs. general tool-bundling), not just an accident of how the code happened to get written.

Closing the gaps landed as: a `.project-card` CSS class (`frontend/src/index.css`) giving a Project's own card a gold `--accent` left-edge stripe instead of Workspace's neutral-then-maroon-on-hover styling, the same family-color convention `.block-row[data-family]` already established, so Project finally reads as its own thing rather than an unlabeled Workspace twin; `getProjectReport(projectId)` (`backend/src/db/queries/reports.js`) and a matching `GET /projects/:id/report` route plus a "Project Report" button on `ProjectPage.jsx`, closing the one gap CLAUDE.md had already named as a deliberate deferral; a `Projects: <names>` line added to `getSpaceReport`'s Structure section alongside the existing `Workspaces: <names>` line, closing a second gap that -- unlike the Report one -- had never been flagged anywhere as intentional; and Project nodes (a small diamond, gold `--accent` stroke) plus `contains-project` edges added to `getGraphData()` and `GraphView.jsx`, mirroring Workspace's own square/`contains` treatment exactly, closing the third previously-named deferral. Focus Mode was the one item dropped mid-build rather than shipped: wiring up the same `focusedBlockId`/`onFocus` plumbing `WorkspacePage.jsx` uses would have been dead code, since neither `MilestoneBlock.jsx` nor `SessionBlock.jsx` reads those props at all -- Focus Mode was built specifically for `TextWorkshop`'s prose-reading case, and a Project can never contain a Text block, so there is no reachable path into it. Caught by checking reachability before finishing the mirror-and-ship reflex, not by assumption. Every backend change was ported to `worker/` in the same pass (`worker/src/db/reports.js`, `worker/src/db/blocks.js`, `worker/src/index.js`'s hand-rolled router) -- no schema change and no data migration were needed anywhere, since this pass deliberately never touched the data model. Verified live end-to-end against a temporary local backend: created a Project with a reached Milestone and a logged Session, confirmed the Project Report's "1 of 1 reached"/"45 min logged" lines, the Space Report's new `Projects: Ship the redesign` line, and the Graph payload's real `contains-project` edge, then deleted the Space and confirmed the known 4-Space baseline held (no foreign-key regression on the `projects` table). `npx vitest run` (389 frontend tests, 3 new/updated across `ProjectPage.test.jsx` and `GraphView.test.jsx`) and the backend suite (238 tests, 3 new) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean.

Most recently: a "Compare Work Types" panel on `NewBlockForm.jsx`, from an outside-review finding that the "Entry type" dropdown's flat 11-item Work optgroup put real friction exactly where the app is supposed to be fastest -- picking correctly at the moment of capture meant already knowing which of several close calls (Insight vs. Implication, "a softer sibling to Deduction") was right, since a native `<select>` can only ever show one option's description at a time (whichever is currently chosen), not all of them at once for comparison. Audited before proposing anything: the Skeleton's own filing/promotion mechanisms (`fileLineInLane`, `saveTextBlockWithPromotion`) turned out not to be reusable machinery here despite the philosophical resemblance -- both operate on a Text block's individual *lines* becoming List *items*, never on a whole block's own `type` changing, and no function anywhere converts one block type into another. But the audit also found that changing this would actually be low-risk if ever wanted: every downstream reader of `type` (the Tools catalog, `listWorkItems()` for Synthesis's picker, `getWorkMixInsights()`, every branch of `reports.js`) queries the column fresh at read time, nothing caches or assumes it fixed at creation. Presented three options via direct question -- improve the picker only, defer classification (start as Writing, reclassify in place afterward), or something else the audit surfaced -- and confirmed sequencing rather than picking once: ship the picker improvement now, since it's cheap and solves real friction on its own, and treat in-place reclassification as a separately-scoped follow-up only if the friction persists once this ships. Landed as: `NewBlockForm.jsx` gained a collapsible `<details className="work-type-compare">` right below the Entry-type `<select>`, listing every Work Type's label and description at once (registry-driven off `blockRegistry`, so a future Work Type needs no edit here either), with clicking a label selecting it in the dropdown above as a shortcut, not a requirement. Deliberately didn't invent a new sub-grouping taxonomy for the Work optgroup itself (one of the candidates raised) -- CLAUDE.md's own standing caution about vocabulary drift argued against introducing new category names unilaterally, and the audit found the real gap was visibility (can't see descriptions while browsing), not organization. Caught one real test-fragility cost while building: RTL's `getByText` doesn't filter by a `<details>`'s collapsed state, so the panel's always-present text collided with two existing `SpacePage.test.jsx` assertions that happened to overlap Work Type description substrings (Question's own "why it matters" phrase, and Assessment's description reappearing once selected) -- fixed by scoping those two queries specifically rather than changing how `<details>` renders, since no other collapsible section in this codebase conditionally unmounts its own children and introducing that pattern just for this would have been a new, inconsistent precedent for a jsdom-only quirk that doesn't actually affect real browser behavior (confirmed live via Playwright: the panel is genuinely hidden until opened). Purely a frontend change -- no schema, no backend, no `worker/` port needed. Verified live via Playwright against the real dev server: the panel starts collapsed, opening it reveals all 11 labels and descriptions at once, and clicking "Implication" inside it correctly updates the dropdown's own selection and single-line description. `npx vitest run` (391 frontend tests, 2 new in a new `NewBlockForm.test.jsx`, plus two existing `SpacePage.test.jsx` assertions rescoped) passes, `oxlint` shows the same pre-existing warnings only, `vite build` clean.

Most recently: a coherence audit, in the same spirit as the professionalism and discoverability passes but aimed specifically at cognitive load and page structure, which neither of those had targeted. Two threads, both grounded in an outside review and verified against the real app (screenshots, a genuinely content-heavy temporary Space, not the Test Space) before anything was built, confirming every fork via direct question rather than assuming the proposed shape was right. Both threads are now done -- see the two Roadmap entries immediately following this one.

**Thread one**, done -- six different pages now answer "what's going on" (the Dashboard, Insights, the Log, a Space's own Trail, Review, and on-demand Reports), each computing something real and distinct, but nothing in the app itself ever said how they relate to each other; that hierarchy only lived in this file's own Roadmap prose. Audited all six as a first-time visitor would encounter them and found the distinctions were real, just unstated -- Trail had no framing copy at all (just a bare heading), and the Dashboard's Insights banner and Insights' own page intro opened with nearly the same words ("What's actually going on across every Space"), actively working against telling the two apart. Confirmed via direct question that a short framing sentence per page closes this -- no merged pages, no "which view do I want" picker. Landed as one honest sentence on each of the Dashboard (which had no `<h1>` or intro at all before this), Insights, the Log, and Trail, each naming what it is and pointing at its two nearest neighbors in a closed loop rather than describing itself in isolation; Reports was deliberately left alone, since it's an explicit user-triggered action with a self-explanatory button label, not a "check here" destination in the same sense as the other five. Also fixed a small accuracy slip surfaced while touching the Log's own copy: it claimed "oldest and newest in one place," but the feed has always shown newest first. Verified live via Playwright against the real dev server, not just the test suite: all four pages render their new copy correctly, and the existing `Dashboard.test.jsx` assertion that matched the old Insights-banner text was updated to match the new. `npx vitest run` (391 frontend tests, one existing assertion updated) passes, `oxlint` shows the same pre-existing warnings only, `vite build` clean. Purely frontend -- no schema, backend, or `worker/` change needed.

Thread two, done -- `SpacePage.jsx` had grown to eleven stacked sections on a real Space (confirmed directly: a genuinely content-heavy temporary Space measured 4654px tall at 1440px width), each added individually and each earning its place, but the accumulated page had never been checked as one whole. Found one concrete bug in the process, independent of the larger grouping question: the Workspaces and Projects sections rendered at full size -- heading, intro sentence, "+ New" form -- even when completely empty, on every Space, forever (confirmed on the real Test Space, which has zero of either), unlike the Details panel, which already correctly started collapsed via `spaceHasMetadata()`. Confirmed four forks via direct question, all toward the smaller/safer option: group the Skeleton-completeness-strip/filters/feed/Add-Entry cluster under a plain "Think" visual label that never collapses (collapsing it would hide the reason the page exists); group Workspaces+Projects under a new collapsible "Organize" panel, adaptively open only once either actually has content -- closing the empty-state bug above for both at once; leave Category management inside the Details panel rather than moving it into Organize, since Categories are Space-level metadata like tags, not a container you navigate into; and keep "Trail" as its own existing name for the third group rather than inventing a new "History" umbrella on top of it, since Review already lives inside Trail as one of its own entry kinds and doesn't need a second name -- Trail's existing section just needed the same adaptive-collapse treatment as Organize, not a restructure. Landed as: a new shared `.space-collapsible-panel` CSS class (`frontend/src/index.css`) factoring out the Details panel's own bordered-box-plus-mono-summary treatment so Organize and Trail could reuse it rather than duplicating three near-identical rule sets, with each panel keeping its own class alongside it (`space-details-panel`/`space-organize-panel`/`space-trail-panel`) purely so tests can still target one specifically; `spaceHasOrganization()`/`spaceHasHistory()` in `SpacePage.jsx`, mirroring `spaceHasMetadata()`'s own exact pattern (a `useState`/`useRef` pair per panel, initialized once from freshly-loaded data, reset on Space-id change so navigating between Spaces doesn't inherit a stale open/closed state); and `TrailSpine.jsx`'s own `<h3>Trail</h3>` heading removed, since the wrapping `<details><summary>Trail</summary>` in `SpacePage.jsx` now provides that label and an inner heading would have duplicated it -- the now-dead `.trail-section` CSS rule was removed with it. Verified live via Playwright against a genuinely content-heavy temporary Space and the real, empty Test Space: on the Test Space (0 Workspaces, 0 Projects, 0 Trail entries), Organize and Trail both now render as a single collapsed line instead of ~260px of empty boxed sections each; on the content-heavy Space, both default open, and Organize's nested `WorkspaceList`/`ProjectList` render without a doubled border (`.space-organize-panel .workspace-section` suppresses their own, now-redundant box chrome). All 391 existing frontend tests passed unchanged, since React Testing Library's queries aren't sensitive to DOM nesting depth -- 6 new tests were still added in `SpacePage.test.jsx` to cover the new adaptive-open behavior directly (Organize collapsed-then-open-on-Workspace, collapsed-then-open-on-Project, Trail collapsed-then-open-on-history) plus one confirming Think renders as a plain, non-collapsible `<h2>`. `npx vitest run` (397 frontend tests, 6 new) and the backend suite (238 tests, unaffected -- this pass touched no schema/backend/`worker/` code at all) both pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean. This closes out the coherence audit in full -- both threads are now done.

Most recently, once asked what was still open on the Roadmap/Open list: rather than build everything named at once (against this project's own small-slices discipline), confirmed via direct question to sequence four well-defined Open items first (skipping Workshop layouts for Views and voice capture, both explicitly deferred), then treat a new content-ingestion feature (pulling in links/videos, M365 docs, PDFs, Markdown, .txt files, and images) as its own later, combined scoping conversation rather than starting narrower. First of the four: automated test coverage for `worker/`'s D1 port (Open item 22). The obvious tool, `@cloudflare/vitest-pool-workers`, turned out to be superseded by `@cloudflare/vitest-plugin` -- confirmed by cloning `cloudflare/workers-sdk` directly (its own official docs were unreachable from this session) and reading its current example fixtures, whose directory name itself (`vitest-plugin-examples`, not `vitest-pool-workers-examples`) was the first clue the tooling had moved on. The plugin runs tests bundled inside a real, sandboxed Workers runtime against an actual Miniflare-backed D1 instance -- the same rigor `backend/`'s better-sqlite3-backed tests give the Express version, just against the real `env.DB.prepare()/.bind()/.all()` calls `worker/src/db/*.js` actually makes, rather than a mock. `worker/vitest.config.js` reads `schema.sql` on the Node side (setup files run inside the sandboxed runtime itself, with no host filesystem access) and flattens it into one statement per line for D1's own stricter `.exec()` (which, unlike `wrangler d1 execute --file=`, doesn't preprocess a multi-line, commented SQL file), then hands it in via a test-only `TEST_SCHEMA_SQL` Miniflare binding, mirroring the official example's own `TEST_MIGRATIONS` pattern; `worker/test/setup.js` applies it once per test file's worker startup. `worker/test/helpers/resetDb.js` mirrors `backend/test/helpers/resetDb.js` -- confirmed empirically (via a forced-failure assertion) that state persists across `it()` blocks within one file here too, so every file resets in a `beforeEach`, same discipline. One test file per `worker/src/db/*.js` module (`worker/src/db/__tests__/`), mirroring `backend/`'s own module-per-file split and porting its assertions directly -- 231 tests across all 16 query modules plus `normalize.js`, including the delicate `skeleton.js`/`trail.js` circular pair. Two backend-only describe blocks were deliberately not ported (`migrateWorkItemSupport`, `migrateTextBlockLines`): both upgrade rows written before their respective content shapes existed, and a D1 database starts fresh on the current shapes, so neither function exists on the worker side to test. One convention difference threaded through every ported assertion: D1's `.first()` returns `null` for a missing row where better-sqlite3 returns `undefined`. Run with `npm test` from `worker/`; `npx vitest run` from `worker/` (231 tests, 17 files) passes alongside the unaffected `backend/` (203) and `frontend/` (388) suites.

Second of the four, at the person's confirmed choice to also include Log's `block_added` entries (over scoping Log out and only wiring up the three surfaces that needed no schema change): deep-linking to a specific block within a Space (Open item 20). Landed as a single URL convention, `?highlight=<blockId>`, read by `SpacePage.jsx` -- once the block feed loads (the Think section is never collapsed, so the target is always in the DOM the moment it can be), the matching entry gets `document.getElementById('block-<id>').scrollIntoView({behavior: 'smooth', block: 'center'})` plus a brief `data-highlighted` flash (a CSS `background-color` transition already unconditional on `.block-row`, so toggling the attribute animates both in and out) that clears itself after ~2.5s -- the URL param itself stays in place, so the link is still shareable/reloadable after the flash fades. A real StrictMode bug surfaced live, not caught by the automated suite (which doesn't run in Strict Mode): the scroll effect's guard ref (`highlightScrolled.current`) was set `true` in the effect body but never reset in its cleanup, so React 18's dev-only setup→cleanup→setup double-invoke (which runs whenever an effect's own dependencies change, not just on initial mount) cleared the first scheduled timer and then left the guard permanently blocking a second one from ever being scheduled -- the flash never cleared. Fixed by having the cleanup undo exactly what setup did (`highlightScrolled.current = false` alongside `clearTimeout`), the standard fix for a non-idempotent effect under Strict Mode; caught only by a live Playwright check, since reproducing this specific double-invoke/timing interaction is exactly what a mocked-API Vitest render doesn't exercise. Four cross-Space surfaces now carry a `blockId`/`block_id` alongside their existing `spaceId`: `listOverdueReviews` (`backend/src/db/queries/dashboard.js`), `getWeekCalendar`'s Milestone/Session entries (already had their own block id from the earlier Week-view pass, just not yet linked to), `getThemeInsights`'s `openTensions` and `getTimeInsights`'s `overdueMilestones` (`backend/src/db/queries/insights.js`), and a new, nullable `activity_log.block_id` column (`ensureColumn`-migrated for existing databases) set only on a `block_added` event and left `null` on `block_removed` (nothing left to link to once the block is gone) and on every Trail-derived Log row (a Trail entry is Skeleton-wide, never about one block). All four ported to `worker/` in the same pass, including the new `worker/schema.sql` column -- like the Projects-table precedent before it, this needs the one-time `ALTER TABLE activity_log ADD COLUMN block_id TEXT;` run by hand against the real, already-deployed D1 database (documented in `worker/DEPLOY.md`'s existing "Making schema changes later" section, not yet run since this session has no live Cloudflare access). Verified live end-to-end against a temporary local backend, not just the test suites: created a Space with an overdue `reviewBy` item, an open Tension, and an overdue Milestone, confirmed `GET /dashboard/overdue-reviews` and `GET /insights` both carried the right `blockId` values and `GET /activity` recorded the new block's id on its `block_added` row (and `null` on the Space's own `space_created` row), then a live Playwright check against the real dev server confirmed the scroll-and-flash itself -- the target block scrolled into view and visibly flashed gold, fading out on schedule after the Strict Mode fix -- before the temporary Space was deleted and the real database's known baseline confirmed held. `npx vitest run` (401 frontend tests, 6 new covering highlight/scroll behavior on `SpacePage.test.jsx` and deep-link hrefs on `Dashboard.test.jsx`/`InsightsPage.test.jsx`/`LogPage.test.jsx`) and both backend suites (`backend/`'s 241, `worker/`'s 234, 5 new each) pass, `oxlint` shows the same pre-existing warnings plus one new, deliberately-accepted and commented one (the scroll effect's `setState`-in-effect warning -- same "documented, working design" treatment `GraphView.jsx`'s own ref-during-render warnings already got in the professionalism pass), `vite build` clean. A real, unrelated bug was also found live while testing (a `<form>` nested inside a `<p>` in `SpacePage.jsx`'s `TagEditor` -- invalid HTML nesting React logs a warning about on every Space page load) -- flagged as a separate suggested task rather than folded into this pass.

Third of the four: letting a Work item's support point link to a claim in a different Space, not just its own (Open item 6). Confirmed one fork via direct question before building, toward the bigger option: not just another Space's Work items (which Synthesis's existing cross-Space `GET /work-items` endpoint already made cheap), but also another Space's claim-bearing Skeleton lane items (Premises/Evidence/Open Questions), which needed a new cross-Space listing endpoint -- `listAllSkeletonClaims()` (`backend/src/db/queries/skeleton.js`, mirroring `listWorkItems()`'s own cross-Space `json_each`-based query pattern already established for `listOverdueReviews`), exposed as `GET /skeleton-claims`. A pointer's shape widened from `{blockId, itemId}` to `{spaceId, blockId, itemId}`, with `spaceId` omitted entirely for a same-Space link (including every pointer saved before this pass) so old data keeps resolving exactly as it always did -- only a genuinely cross-Space pick sets it. Resolving a cross-Space pointer needed a new way to fetch just the one target block, without pulling in every block from a Space this component isn't even viewing: a new `GET /blocks/:id` route (there was no way to fetch a single block standalone before this), backed by a new `getBlockByIdWithSpaceTitle()` query function -- a small, dedicated wrapper around the already-heavily-used `getBlockById()` rather than adding a Space-title join to that function itself, so nothing else that calls it needed to change. `WorkBlock.jsx`'s `SupportLinker` now fetches both cross-Space listings lazily (only once the picker actually opens) alongside the existing same-Space candidates already drawn from `spaceBlocks`, groups the merged list by Space (via a `<h4>` heading per Space, "This Space" first), and gained a search box -- the same grouped/searchable shape `CreateSynthesis.jsx`'s own cross-Space picker already established, reused rather than re-invented now that the candidate list can span every Space. `resolvePointer` gained a third state beyond "resolved" and "removed": `undefined`, meaning the data needed to resolve it (a cross-Space fetch, or the Space's own not-yet-loaded `spaceBlocks`) simply hasn't arrived yet -- `SupportItem` shows "Resolving..." for that case instead of prematurely flashing "(linked claim removed)," a small pre-existing rough edge on the same-Space path fixed in passing while already in this exact code. `WorkBlock` maintains a small `crossSpaceBlocks` cache (keyed by block id, populated via `GET /blocks/:id` as cross-Space pointers show up in `support`) so each distinct target is fetched once, not once per render or once per pointer referencing it. A resolved cross-Space pointer renders with a `(in <Space title>)` suffix, so it's never ambiguous which Space a linked claim actually lives in. Verified live end-to-end, not just the test suites: created two real Spaces via a temporary local backend, filed an Assessment and an Evidence item in one, linked a Question's support point in the other to both via the real API calls the frontend makes, confirmed `GET /work-items` and `GET /skeleton-claims` both returned the cross-Space candidates and `GET /blocks/:id` resolved the target with its `spaceTitle`, then a live Playwright check opened the real "Link a claim" picker, confirmed both cross-Space candidates appeared grouped under the correct Space heading, picked one, and confirmed it rendered resolved with the "(in Space B)" suffix -- then, to confirm live resolution specifically (not a value snapshotted at link time), edited the source Assessment's statement directly via the API and reloaded the linking Space, confirming the edited text appeared automatically with no re-linking needed. Ported to `worker/` in the same pass (`listAllSkeletonClaims`, `getBlockByIdWithSpaceTitle`, and the two new routes). `npx vitest run` (405 frontend tests, 8 new -- cross-Space linker/resolution behavior in `WorkBlock.test.jsx`) and both backend suites (`backend/`'s 246, `worker/`'s 239, 5 new each covering `listAllSkeletonClaims`/`getBlockByIdWithSpaceTitle`) pass, `oxlint` shows the same warnings as before this pass (no new ones), `vite build` clean.

Fourth and last of the four well-defined Open items: persisting item-level Synthesis lineage, so `getProvenanceInsights()` (Insights) can finally answer "which specific Work items fed which Synthesis," not just count how many of each exist -- stated as a real, uncomputable gap directly in that function's own code comment since the Provenance facet was first built. Confirmed via direct question to build both surfaces the persisted data would unlock, rather than just one: a Work item's own Block Report gaining a real "Used in Synthesis" line (a genuinely new capability -- there was no reverse pointer from a Work item back to any Synthesis that used it before this), and Insights' Provenance facet becoming precise rather than a rough ratio. Landed as a new `properties.sourceItemIds` field (an array of the source Work items' own block ids) on the Text block `CreateSynthesis.jsx` already creates for "Source Material" -- the same "embed the pointer directly in existing block properties" pattern support-point and Tension pointers already use, rather than a new table; `properties.categories` on that same block already named it "Source Material" for the person, this just makes it queryable too, distinct from the copied-in text sitting alongside it for drafting. `findSynthesesUsingWorkItem(blockId)` (`backend/src/db/queries/reports.js`) is the reverse lookup -- every Source Material block anywhere whose `sourceItemIds` contains this one Work item's id, returning every Synthesis it feeds (a Work item can feed more than one) -- gated in `getBlockReport` to only run for a Work-type block, since nothing else could ever appear in a `sourceItemIds` array. `getProvenanceInsights()` gained `distilledWorkItemCount` -- the count of *distinct* Work items actually used in at least one Synthesis, read via a `json_each` join over every Source Material block's `sourceItemIds` array (deduplicated, so an item used in two Syntheses still counts once) -- which turned out to also fix a real, pre-existing mislabeling in the Insights page itself: the "Work -> Synthesis -> Resource" funnel's middle number was `synthesisCount` (how many Synthesis *pieces* exist), visually presented as if it were a subset of the Work item count above it, when it never actually was one. The funnel now shows `distilledWorkItemCount` there instead (a real subset, fixing the funnel's own semantics), with `synthesisCount` kept alongside as "(across N Syntheses)" rather than dropped, since it's still real, separate information. The reading sentence changed to match: "N% of raw Work items have actually been distilled into a Synthesis so far," replacing the old, much weaker "roughly N% as many Syntheses exist as raw Work items." Verified live end-to-end against a temporary local backend and a live Playwright check of the real UI, not just the test suites: created a Synthesis through the actual `/synthesis/new` flow, confirmed the created block carried `properties.sourceItemIds`, opened the source Work item's own Report panel in a real browser and confirmed "Used in Synthesis: My Real Synthesis" rendered in the prose narrative, then loaded `/insights` and confirmed the funnel read "1 compiled into a Synthesis (across 2 Syntheses)" and the reading sentence matched exactly, before deleting the temporary Spaces and confirming the real database's known baseline held. Ported to `worker/` in the same pass. `npx vitest run` (405 frontend tests, one payload assertion updated in `CreateSynthesis.test.jsx` plus the Provenance funnel test in `InsightsPage.test.jsx`) and both backend suites (`backend/`'s 249, `worker/`'s 242, 5 new each covering `distilledWorkItemCount` and the new Block Report membership line) pass, `oxlint` shows the same pre-existing warnings only, `vite build` clean. This closes out all four of the well-defined Open items taken on in this pass -- the new content-ingestion feature (links/videos, M365 docs, PDFs, Markdown, .txt, images) is the next thread, per the person's own confirmed sequencing, as its own combined scoping conversation rather than started narrower.

Most recently: the content-ingestion feature itself ("pull in links/videos, M365 docs, PDFs, Markdown, .txt files, and images," raised much earlier and deliberately deferred to its own combined scoping conversation, per the sequencing above -- distinct from Open item 15's backup/search/trash-undo, a separate infrastructure thread that stays open). Confirmed three forks via direct question before building, all toward the recommended option: two separate mechanisms (a server-side link preview, no storage, versus an uploaded file, stored on disk) rather than one unified "paste anything" box; local filesystem storage for `backend/` now, with `worker/`'s Cloudflare-hosted side deliberately deferred (would need R2, Cloudflare's object storage, which this session can't provision without the person's help -- the same D1-API-token precedent as the original Hosting pass); and "store & view only" for extraction depth -- PDF and image files get a real inline preview, Markdown/.txt are fetched and rendered as plain text, and every other file type (Office docs) is download-only, with no text extraction or search indexing of file contents. Landed as two new Media block `mediaType`s, `'link'` and `'document'`, alongside the existing `'image'`/`'audio'`/`'sketch'` (`MEDIA_TYPES` in `frontend/src/registry/blocks.js`) -- a `'link'` block's content carries `linkTitle`/`linkDescription`/`linkImage`/`linkSiteName` fetched once at creation time from the pasted URL's own Open Graph/meta tags (never re-fetched or kept live, unlike a Reference's target-Space preview); a `'document'` block carries `fileName`/`fileType` alongside the uploaded file's own `url`. `backend/src/linkPreview.js` is pure logic with no database or Express dependency (`isSafeUrl` -- rejects non-http(s) schemes, localhost/loopback, every RFC1918 private IPv4 range, and `.local` hostnames, a best-effort SSRF guard appropriate for a single-user personal app fetching a link the person themselves chose to paste in, not a public-facing service; `extractLinkMeta` -- regex-based Open Graph/meta-tag/`<title>` extraction with HTML-entity decoding, falling back gracefully when a site has no Open Graph tags at all) -- kept separate from routing the same way `textLinks.jsx`/`listItems.js` already separate parsing from data, so it ports to `worker/` unchanged (`worker/src/linkPreview.js` is a verbatim copy, since it uses no Node-only APIs) and is directly unit-tested (11 tests, mirrored into both `backend/test/linkPreview.test.js` and `worker/src/linkPreview.test.js`). `backend/src/routes/linkPreview.js` (`POST /link-preview`) and its `worker/src/index.js` route-match equivalent both fetch server-side (avoiding the browser's own CORS restrictions, which block reading another site's HTML directly from the frontend) with an 8-second timeout and a real `User-Agent` header, returning the extracted fields with nothing persisted server-side. `backend/src/routes/uploads.js` (`POST /uploads`, `GET /uploads/:filename`) uses `multer` (a new `backend/` dependency) with disk storage under `backend/data/uploads/` (already covered by the repo's blanket `backend/data/` gitignore) -- filenames on disk are server-generated UUIDs, never the user's own original filename, so nothing about a chosen file's name can become part of a path this server reads from or writes to; a defensive second check re-verifies the resolved path still starts with the uploads directory before serving a file back, on top of `path.basename` stripping any directory components from the requested name. A file-type allowlist (PDF, Markdown/.txt, common image formats, and Office document MIME types) rejects anything else with a clear `"Unsupported file type."` error, checked by MIME type first and falling back to extension only for Markdown/.txt (whose mimetype varies unpredictably by browser/OS). `MediaBlock.jsx`/`MediaWorkshop.jsx` both gained `'link'`/`'document'` rendering branches -- a link renders as a card (thumbnail, title, description, site name, all optional and gracefully omitted when a fetch found none, falling back to the raw URL as the title); a document renders through a new shared `frontend/src/blocks/mediaDocument.jsx` (`DocumentPreview`, the same "shared implementation so the ordinary and Workshop versions can't drift apart" pattern `textLinks.jsx`/`listItems.js` already established) -- a PDF gets an inline `<iframe>`, Markdown/.txt is fetched client-side and shown in a `<pre>`, anything else (Office docs) shows a plain "no inline preview for this file type" note, and every case gets a download link. `frontend/src/api.js` gained `getLinkPreview(url)` (an ordinary JSON POST through the shared `request()` helper) and `uploadFile(file)` (bypasses `request()` entirely, since a multipart upload needs `FormData` as the body with no explicit `Content-Type` header -- letting `fetch` set its own multipart boundary is what makes the upload parseable server-side). `CreateResource.jsx` gained a three-way "Source" toggle (Guided questions / Paste a link / Upload a file) sitting between the Type-tags section and whichever source-specific UI is showing -- confirmed via direct question that Type tags and the universal Touches/Touched-By section stay identical across all three source kinds, since sub-typing and cross-Space relations are orthogonal to how a Resource's own content actually got in. Paste-a-link fetches a preview on the URL field's blur (not per keystroke, the same "one discrete action" reasoning save-on-blur fields elsewhere in the app already follow) but never blocks submission on that fetch succeeding -- a failed or slow preview still lets the link itself be saved, with `linkTitle` left `null` so `MediaBlock.jsx`'s own existing raw-URL fallback handles display. Upload-a-file requires a completed upload before Create Resource is enabled (there's nothing to create a Resource from otherwise), uploading immediately on file selection rather than waiting for submit, with the returned `mimeType` deciding whether the resulting block becomes `mediaType: 'image'` (reusing the already-fully-built image branch verbatim) or `'document'`. Verified live end-to-end against a temporary local backend and a live Playwright check of the real dev server, not just the test suites: confirmed the SSRF guard blocks `localhost`/loopback/private-range URLs through the real route (not just the unit tests), confirmed a real file upload is served back byte-for-byte with a working path-traversal guard (`../../package.json` correctly 404s) and an unsupported file type is correctly rejected, created a real "document" Resource through the actual `/resources/new` file-upload flow in a live browser (confirmed the uploaded text file's content rendered inline on the new Space's page) and a real "link" Resource through the same flow (confirmed the link block renders with a graceful raw-URL fallback, since this sandboxed session's own network policy blocks the actual outbound fetch to a real external site -- the SSRF-safe, well-formed-error code path this exercised is exactly what a genuinely unreachable site would also hit), then deleted every temporary Space and uploaded file and confirmed the real database's known 4-Space baseline held. `npx vitest run` (426 frontend tests, 20 new -- `linkPreview.test.js`-mirrored assertions are backend/worker-side; frontend additions cover the two new Media rendering branches, the new `mediaDocument.jsx` component, `api.js`'s two new functions, and `CreateResource.jsx`'s three-way Source toggle) and both backend suites (`backend/`'s 260, `worker/`'s 253, 11 new each covering `linkPreview.js`'s pure functions) pass, `oxlint` shows the same pre-existing warnings plus one new, deliberately-accepted and commented one (`mediaDocument.jsx`'s text-fetch effect triggers the same set-state-in-effect warning already accepted elsewhere for a real external-system synchronization, not derivable render state), `vite build` clean. Deliberately not built in this pass: the `worker/` upload route (needs R2, not yet provisioned -- see Open), any text extraction from uploaded file contents for search (explicitly out of scope per the confirmed "store & view only" depth), and video embedding (a link to a video site renders as an ordinary link-preview card, same as any other link, rather than a distinct embedded-player Media type -- not raised as a separate need).

Most recently: a change-legibility pass, from the person naming a specific, concrete gap directly -- "when something is added or updated or whatever, theres no real tell that suggests what was changed and what the implications of that are. no visual cues, nothing" -- alongside the broader complaint of getting lost among the app's own tools, especially mid-demo to someone else. Grounded in a real audit before proposing anything, not a guess: read `api.js`'s mutation-listener mechanism and confirmed the gap precisely -- every PATCH/DELETE toast said only generic "Saved"/"Deleted" regardless of what actually happened, POST was excluded from toasting entirely (on the reasoning that "a new item appears" is obvious enough, which doesn't survive contact with a long feed or a live demo), and a newly created row had no spatial anchor at all -- the exact `data-highlighted` flash mechanism built for deep-linking existed but only fired from a `?highlight=` URL param, never from an in-page create. Separately confirmed there was no legend anywhere explaining what a SpaceGlyph shape, a family-color stripe, or a Tool icon actually means. Presented four candidate fixes plus a scope question for "implications" specifically via one multi-select `AskUserQuestion`; all four fixes were chosen, plus "add a few targeted implication notes" over staying narrowly scoped to visibility alone.

Landed as: **content-aware toasts.** Wherever a backend function already computes a "what happened" sentence for `activity_log`/Trail (block add/remove, Space create/status-change/delete, Workspace/Project create, Template/Resource Template create+update, Skeleton filing/promotion/Tension-pairing, Review creation, a manual Trail note), that exact sentence is now also attached to the HTTP response as `changeSummary` -- reusing the string already computed, never a second, independently-maintained copy of it. Three new, genuinely computed implications were added on top, none of which existed as any sentence before: a new pure module, `backend/src/changeSummary.js`'s `describeBlockContentChange()`, detects a Milestone's `reached` flag flipping and returns "Milestone reached -- now counted in Insights and the Week digest" (or the reverse); `updateSpace` (`backend/src/db/queries/spaces.js`) detects a due-date change and returns "Due `<date>` -- already overdue" or "-- now shows on your Week digest" (or "Due date cleared"); `createTensionPair` (`skeleton.js`) returns "Tension paired: \"`<label>`\" -- now counted as an open Tension in Insights", distinct from (and richer than) the plain sentence still logged to Trail itself, since Trail's own record and a one-shot toast don't need to say the same thing. `frontend/src/api.js`'s `request()` was rewritten around this: PATCH always announces something (falling back to a plain "Saved" when the backend has nothing more specific to say -- still an honest, non-misleading default for any successful edit), POST only announces when the response actually carries a `changeSummary` (deliberately no generic fallback for POST, since not every POST is a "creation" worth announcing -- `moveBlockInSpace` reorders, `getLinkPreview` only previews -- and a wrong generic label would be worse than staying silent), and DELETE keeps its existing plain "Deleted" (naming exactly what was deleted would need context this generic helper doesn't have, and the click that triggered it already named the thing). `Toast.jsx`'s `MESSAGES` kind-lookup is gone -- the mutation listener now receives a ready-to-display string directly, and the toast's visible window widened from 2000ms to 3200ms to give a real sentence time to actually be read, not just a single word.

**Flash-highlight on create.** `SpacePage.jsx`'s deep-link flash was generalized from a URL-param-only, one-shot mechanism into a reusable one: `flashId`/`flashBlock()` replace the old `highlightBlockId`-only state, still seeded from `?highlight=` on load but now also settable directly the moment `handleAddBlock`/`handleStartSession` resolves with the newly created block's own id (guarded by a per-id ref, `lastFlashedId`, so an unrelated later edit elsewhere on the page can't re-trigger an old flash). The same small pattern was mirrored into `WorkspacePage.jsx` (on adding a new Tool, and on pulling an existing one in) and `ProjectPage.jsx` (on adding a Milestone, starting a Session, or pulling one in) -- both previously had no `id` attribute on their block-row `<div>`s at all, needed now for `document.getElementById` to find the target. All three effects share one now-documented, deliberately accepted oxlint `set-state-in-effect` warning (synchronizing with a real external system -- the DOM element found via `getElementById`/`scrollIntoView` -- not deriving render-time state), the same treatment `GraphView.jsx`'s own ref-during-render warnings already got in the professionalism pass.

**A compact legend.** `frontend/src/components/Legend.jsx`, reached via a new "? How to read this app" trigger in `Sidebar.jsx` (so it's on every page, not buried on one). Deliberately built from the real, live `SpaceGlyph` component rather than a drawn diagram or a written description -- each of its six explanatory rows renders an actual glyph with synthetic-but-real props (status progression, branch count, an open-Tensions crack, an overdue dash, Milestone rings, a manual accent), so the legend can never drift out of sync with what the glyph actually draws; `SPACE_STATUSES`/`SPACE_ACCENTS` (already exported from `SpaceGlyph.jsx` for `AccentPicker`'s own chip row) meant no new constant needed inventing. A second section explains the three family colors (`general`/`work`/`time`) using the exact same CSS selectors `.block-row[data-family]` already uses, then points at the Tools catalog (`/tools`) for the full per-Tool icon glossary rather than duplicating all 22 icons here -- "compact" was the confirmed brief, and that glossary already exists and is kept in sync by the registry.

Verified live end-to-end against a temporary local backend and a live Playwright check of the real dev server, not just the test suites: confirmed `changeSummary` round-trips correctly through real HTTP responses for a Space creation, a Milestone-reached toggle, and a due-date change (matching the exact strings above); in a real browser, clicking "+ Add Entry" showed a toast reading `Added a text entry to "..."` and flashed the new row in place, toggling a Milestone's reached checkbox showed the implication sentence, and opening/closing the Legend from the Sidebar rendered all six glyph rows and the three family swatches correctly with no native browser dialog ever firing. The temporary Space was deleted afterward and the real database's known 4-Space baseline confirmed held. `npx vitest run` (440 frontend tests, 21 new -- `Toast.test.jsx`'s rewritten contract, `api.test.js`'s changeSummary-aware assertions, flash-on-create tests in `SpacePage.test.jsx`/`WorkspacePage.test.jsx`/`ProjectPage.test.jsx`, and a new `Legend.test.jsx` plus a `Sidebar.test.jsx` addition) and both backend suites (`backend/`'s 272, `worker/`'s 265, 12 new each covering `changeSummary.js` and every function that now attaches it) pass, `oxlint` shows the same pre-existing warnings plus the three new, deliberately-accepted and commented ones described above, `vite build` clean. Deliberately not built in this pass, per the confirmed scope: implication notes beyond the three named cases (Milestone reached, a Space's due date, a Tension pairing) -- broader "what does this imply" coverage across every action was explicitly the option not chosen, in favor of shipping these four fixes well; and flash-highlighting a block whose *content* changes elsewhere (a Milestone reached, a Tension paired) rather than one freshly *created* -- the toast alone was judged sufficient there, since an update happens to a row you were already looking at, unlike a brand-new row that can land off-screen in a long feed.

Most recently: the personalization/theming pass, from the person naming three connected problems in one message -- the `accent` picker (`star`/`underline`/`triangle`/`dot`) was unreadable ("its not clear what these do or why they're here"); Creation Mode's "Personalize" step was a misunderstanding of what they actually wanted ("it doesnt feel useful in any way other than as an additional tagging method"); and, most concretely, looking at a real Space of their own with several different Tools on it, "i really have a hard time distinguishing things, even though im the one who entered all of it. thats not a good sign." What they asked for instead: the ability to make each Space/Tool/Resource/Synthesis "its own thing, not just in how its titled, but also how it looks/feels... a certain shape, if you will." Confirmed three forks via direct question before building, all toward the bigger option: **distinct computed defaults plus a hand override** (not override-only, which would have left an untouched Space looking identical to every other one -- the actual complaint); **all four dimensions at once** (Color, Shape/silhouette, Density/layout, Typography) rather than starting with color alone; and this pass scoped as **theming + the status vocabulary + killing the fake Personalize step**, with the other eight threads raised in the same message explicitly deferred to their own later passes rather than folded in.

Landed as: `frontend/src/theme/itemTheme.js`, a new single-file registry in the same spirit as `registry/blocks.js` -- open one file and see every default the app assigns. It defines the four dimensions' allowed values (`THEME_ACCENTS`, 10 colors; `THEME_SHAPES`, 8 silhouettes; `THEME_DENSITIES`; `THEME_TYPEFACES`), a `BLOCK_THEME_DEFAULTS` entry for all 18 Tool types, `SPACE_THEME_BY_TAG` for the four Space kinds, and the pure resolver functions (`resolveBlockTheme`/`resolveSpaceTheme`) that merge a stored override onto its computed default. The defaults are assigned on a deliberate rule rather than arbitrarily: **accent carries family** (General neutral-ish, Work in the maroon/rust/clay/plum range, Time in the gold/amber range) so the existing three-family reading survives intact, while **shape carries type**, which is what finally distinguishes the 18 types from each other rather than just the 3 families. A test asserts, against the live registry rather than a hardcoded list, that no two Tool types share an (accent, shape) pair -- so a future Tool can't silently be added as a visual duplicate of an existing one. Only the override is persisted (`spaces.theme`, a new nullable JSON column; `blocks.properties.theme`), never the resolved value, so an untouched item keeps tracking its kind's default if that default is ever changed later.

Rendering goes through data attributes, not inline styles: `themeAttributes(theme)` returns the four `data-theme-*` attributes, and `frontend/src/index.css` gained a documented "Item themes" section implementing every dimension as attribute rules. Those rules use a doubled attribute selector (`[data-theme-shape='slab'][data-theme-accent]`, specificity 0,2,0) specifically so they beat the single-class rules they sit alongside (`.block-row`, `.tool-card`) without depending on source order -- called out in the CSS's own comment, since specificity-by-accident is exactly the kind of thing that breaks silently later. The old `data-family` color rules were removed (themes now carry that meaning), though `data-family` itself stays for grouping. `frontend/src/components/ThemePicker.jsx` is the one shared picker for both levels -- a collapsed "◐ Theme" toggle expanding to four rows (color as swatches, the rest as chips), always showing the *resolved* value as active so an untouched item reads honestly as already having a look, with "Reset to default" appearing only once a real override exists. `ToolsPage.jsx`'s catalog cards now render each Tool's own default theme too, so the catalog previews the real look rather than 18 identical boxes.

Two other things in the same pass, both directly named by the person. **The status vocabulary** was replaced wholesale: the app's four invented values (`nascent`/`developing`/`stable`/`dormant`) became the person's own five (`dormant`/`inactive`/`active`/`interesting`/`mature`) -- "the status thing is not using categories im familiar with." `migrateSpaceStatuses()` maps the two retired values onto `active` once at startup, the same one-time-migration pattern `migrateTextBlockLines`/`migrateWorkItemSupport` already established; `SpaceGlyph.jsx`'s status styling was rewritten around the new five (a real progression in opacity/stroke-weight, with `dormant` alone drawn grey), and its stroke color now follows the Space's own resolved theme accent rather than being hardcoded maroon. **The fake "Personalize" step** in `CreateSpace.jsx` became "Goal & tags," which is all it ever actually did -- the real personalization capability now lives where the person asked for it, inside each thing, not as a one-shot step at creation.

Verified live end-to-end against the real dev database and a Playwright pass, not just the suites: confirmed the status migration ran correctly on the real 4-Space baseline (`active`/`active`/`mature`/`active`), then created a temporary Space carrying seven different Tool types and confirmed in a real browser that all seven resolved a genuinely distinct (accent, shape) pair, that each one's accent actually resolves on the edge its own shape uses (a `rule`'s top border, an `underline`'s bottom, a `tab`'s pseudo-element -- not just the left edge the first check happened to sample), that the status pill cycles the new five-word vocabulary, and that picking "moss" in the Space-level picker updated both the header's own `data-theme-accent` and the glyph's stroke live. The temporary Space was deleted afterward and the real database's known 4-Space baseline confirmed held. `npx vitest run` (459 frontend tests, 18 new -- `itemTheme.test.js` including the no-duplicate-pair guard, and `ThemePicker.test.jsx`) and both backend suites (`backend/`'s 281, `worker/`'s 270) pass, `oxlint` adds no new warnings and in fact drops one (16 to 15 -- removing `SPACE_ACCENTS` removed one `only-export-components` warning from `SpaceGlyph.jsx`), `vite build` clean. Ported to `worker/` in the same pass -- which caught one real bug that would only ever have shown up on the deployed site: `worker/src/index.js`'s `handleUpdateSpace` was still destructuring `accent` off the request body and never reading `theme`, so every theme edit would have silently succeeded and changed nothing. The new `spaces.theme` column needs the one-time `ALTER TABLE spaces ADD COLUMN theme TEXT;` run against the real, already-deployed D1 database (documented in `worker/DEPLOY.md`'s existing "Making schema changes later" section, not yet run since this session has no live Cloudflare access). The `accent` column is deliberately left in place in both schemas, unused and annotated as superseded, rather than dropped -- dropping it would be a destructive migration on real personal data for no functional gain.

Most recently: Workspaces as real specialized thinking environments -- thread (d) of the large personalization/structure message, and by far the biggest of the seven deferred there. From the person's own framing: "maybe we should make this a space where we can really get into the thinking work... produce documents, edit artifacts, workshop different deliverables or presentations or demos," naming eight concrete kinds to start with, plus "maybe workspaces should also get its own page like tools do."

Audited before proposing anything, which is what set the shape of the forks. `workspaces` was just `(id, space_id, name)` with no notion of a kind anywhere, and `WorkspacePage.jsx` was entirely generic -- nothing distinguished one Workspace from another except what you happened to put in it, which is exactly the complaint. Checking the eight named kinds against the 18 existing Tools found five or six genuinely assemblable today (Analyst, Critical Thinking, Conceptualize, Worldview Assessment, Problem Formulation) and two or three that needed Tools that simply did not exist (Etymology, Word-Concept Mapping, and partly Modeling). Four forks confirmed via direct question; the person picked **"Both in one pass"** on scope -- the Kind mechanism *and* the new Tools together, the largest of the three options offered and the opposite of the recommendation, which had been to ship kinds first and build the new Tools as later slices. Also picked: a **catalog plus directory** for the new top-level page, the **kind genuinely shapes the page** (not just seeds it and lets go), and **a Workspace still belongs to one Space** (no inversion, no data migration -- keeping this pass clear of the separately-queued Projects inversion). Built as five reviewed slices rather than one commit, per this project's own method, despite being one pass.

**Three new Tools, and a fourth Tool family.** `wordEvolution`, `conceptMap` and `model` (`WordEvolutionBlock.jsx`, `ConceptMapBlock.jsx`, `ModelBlock.jsx`) are registered under a new `family: 'mapping'` -- Tools about structure and relation rather than a single claim. That family is not cosmetic: the audit found `NewBlockForm.jsx`'s `handleSubmit` treated *any* unrecognized type as a Work item in a bare `else`, so filing these under `family: 'work'` (or adding any future non-Work Tool without noticing) would silently have created them with Work's `{statement, support, confidence}` shape. Each new type got its own explicit branch, and that fallback is now guarded -- an unhandled type throws a named error instead of quietly producing the wrong content shape. Shared row logic lives in `mappingRows.js` (`addRow`/`updateRow`/`removeRow`/`moveRow`, plus `ALIGNMENTS`) and the shared click-to-edit field in `mappingFields.jsx`, the same "one implementation so copies can't drift" split `textLinks.jsx`/`listItems.js` already established. Theme defaults follow the existing rule exactly -- one new accent (`indigo`) carries the new family, three shapes carry the three types -- so the no-duplicate-pair guard still passes untouched. Reports gained a headline and a detail case for each in both backends; Model's report resolves relation ids to component names the same way its component does at render time.

**The Kinds mechanism.** `frontend/src/registry/workspaceKinds.js` is the one file to open to see every environment, in the same spirit as `registry/blocks.js`. It lives in the frontend, not a database table, for two stated reasons: a kind carries page layout and framing copy a row can't hold, and unlike Templates and Resource Templates nothing here was asked to be editable from inside the app. The backend stores only `workspaces.kind` (a nullable column, `ensureColumn`-migrated) and never reads the registry. `createWorkspace` gained `kind` and `starterBlocks`, so a kind's starting Tools are created in the same request already carrying the new Workspace's id -- rather than the page creating the Workspace and then looping a second round of requests to fill it.

**The kind actually shapes the page.** `WorkspacePage.jsx` renders its kind's named sections, each with the question that section exists to answer, groups member blocks into them by Tool type, leads the add-a-Tool picker with that kind's own Tools *and* starts the picker on the first of them, and carries the kind's theme on the page itself. An empty section is kept and framed rather than hidden, since the prompt is the point. A Tool matching no section lands in a final "Also here" group rather than disappearing -- a kind leads and arranges, it never restricts. An unkinded Workspace keeps the plain flat feed exactly as before, and Focus Mode falls back to that flat feed too, since sections would be noise when exactly one block is showing. The per-block row was extracted into one `renderBlock` so the flat and sectioned feeds can't drift apart on what a block actually renders.

**The top-level page.** `WorkspacesPage.jsx` at `/workspaces`, with its own Sidebar entry: a catalog of all eight kinds (each card carrying its own theme, its sections, and what it starts you with) plus a directory of every Workspace across every Space with its kind, Space and Tool count -- backed by one new `listAllWorkspaces()` query counting members via the same `json_each` membership test `updateBlockWorkspaces` writes, and a `GET /workspaces` route declared before `/workspaces/:id` so the literal path isn't captured as an id. The directory answers something nothing in the app could answer before: a Workspace lives inside one Space, so finding one previously meant already remembering which Space it was in.

The registry's own consistency tests caught three real bugs in this pass, all in content written during it rather than in code: three lead Tools (`hypothesis`, `question` on Critical Thinking; `conceptMap` on Modeling) that the picker offered but no section would file, and -- found live in the browser, then codified as a second guard -- two kinds (Etymology, Modeling) seeding a Writing block none of their own sections claimed, which dropped it straight into "Also here" the moment the Workspace was created. Both guards now run against the live registry, so a future kind can't ship with either fault.

Verified live end-to-end against a real local backend and a real browser, not just the suites: created an Etymology Workspace through the actual API and confirmed the kind persisted, that its starter blocks were seeded with distinct positions already joined to it, that `GET /workspaces` returned it with the right Space title and member count, and that the Workspace Report carried its `Kind:` line; then in Playwright confirmed the catalog renders all eight kinds with genuinely distinct looks, the Sidebar marks Workspaces as current, the kinded page shows its three named sections with their prompts, the picker leads with (and starts on) the kind's own Tools, and the new Word Evolution Tool actually accepts a sense-shift and renders it on its timeline. Fixed two display nits found only by looking at it: the catalog listed "Formulation, Formulation, Formulation" for Worldview Assessment (now deduped to "Formulation x3"), and the picker led with a kind's Tools but still defaulted to Writing. The temporary Space was deleted afterward and the real database's known 4-Space baseline confirmed held, with no orphaned `workspaces` rows. `npx vitest run` (frontend 513 tests, 48 new across the Mapping Tools, `mappingRows`, the kinds registry, `WorkspacePage` and a new `WorkspacesPage.test.jsx`) and both backend suites (`backend/`'s 287, `worker/`'s 275, 6 and 5 new) pass, `oxlint` shows the same 15 pre-existing warnings and no new ones, `vite build` clean. Ported to `worker/` in the same pass; the new `workspaces.kind` column needs the one-time `ALTER TABLE workspaces ADD COLUMN kind TEXT;` run against the real, already-deployed D1 database (documented in `worker/DEPLOY.md`'s existing "Making schema changes later" section, not yet run since this session has no live Cloudflare access).

Most recently: the durability pass -- Open item 15, raised as a real gap two passes earlier and left untouched since. Three pieces, chosen together and built as sequenced slices: an export path, search across entry content, and a trash window. Four forks confirmed via direct question before any code, all toward the recommendation this time.

Grounded in an audit first, which changed the shape of all three. (1) `backend/export-to-d1.mjs` is a Node CLI script reading the *local* SQLite file -- useless as a backup, since the real accumulated thinking now lives in the deployed D1 database. A backup has to run inside the app, in both backends. (2) FTS5 was probed directly in both better-sqlite3 and D1 (both support it) and then deliberately *not* used: at 26 blocks locally and ~41 deployed, a virtual table plus sync triggers plus a rebuild path would add three things that can silently drift out of step with the content they index, to speed up a query that already returns instantly. (3) The obvious soft-delete -- a `deleted_at` column on every table -- would mean every read query in the app filtering it forever, with one missed filter quietly resurfacing deleted content; snapshotting the removed rows into one `trash` table gets the same undo behaviour while touching only the six delete paths and changing no existing read at all.

**Export.** `getFullExport()` (`backend/src/db/queries/exportData.js`) returns every table in foreign-key dependency order, so a future restore could replay it top to bottom. `renderExportMarkdown()` (`backend/src/exportFormat.js`, pure formatting with no database access, the same split `reportFormat.js` already uses -- which is what lets `worker/` reuse the file verbatim) turns that same payload into one readable document: a contents list, then each Space with its metadata, entries, Structure and Trail. One document rather than one file per Space, deliberately -- a browser can't hand over many files without a zip dependency, and a single document with a contents list is more durable to read in ten years anyway. Two routes (`GET /export/json`, `GET /export/markdown`) send it as an attachment. The Markdown renderer detects Work Types *structurally* (they're the ones carrying a `statement`) rather than by name, so a future Work Type renders correctly without this file hearing about it -- verified with a deliberately unknown type in the tests.

**Search.** `searchEverything(q)` (`backend/src/db/queries/search.js`) LIKEs over `blocks.content` plus Space titles and goals. The SQL matches raw JSON, so the rows come back wide and are narrowed in JS: `readableText()` walks the content generically, skipping a named set of plumbing keys (ids, tags, pointers, fixed enum values) and anything UUID-shaped, and a row whose only match was plumbing is dropped before it is ever shown. That was a real fix, not a theoretical one -- the first live run returned `466920ad-... The map is not the territory. quote` as an excerpt. A `/search` page groups results by Space and entry, and an entry result links straight through with the existing `?highlight=` convention, so a search lands you on the thing rather than near it. The search box itself lives in the Sidebar rather than on one page, since "hard to find things" is only half-solved by a search you have to navigate to first.

**Trash.** A new `trash` table (see Data model) plus `recordTrash`/`restoreFromTrash`/`purgeTrashEntry`/`emptyTrash`. Every delete path that loses authored content snapshots first -- `deleteSpace` captures its blocks, Workspaces, Projects and Trail entries alongside the Space row, so restoring puts the whole thing back. Rows are re-inserted with their original ids, so anything pointing at them (a Reference, a support-point pointer, a block's `workspaces` array) resolves again with no repair step; `INSERT OR IGNORE` rather than a plain insert, so restoring twice is "it's back" rather than a primary-key crash halfway through leaving a half-restored Space. The Node side wraps the restore in a transaction; the Worker uses `env.DB.batch()`, which D1 applies atomically, for the same guarantee. A `/trash` page lists what was deleted with Restore and a permanent purge; nothing expires on its own.

Two real fragilities caught and fixed while building, neither of which a passing test would have surfaced on its own: `listTrash` ordered only by `deleted_at`, which has second granularity -- two deletes in the same second tied, and SQLite's order on a tie is unspecified, so `rowid DESC` was added as a deterministic tiebreaker; and both `resetDb.js` helpers were missing the new `trash` table, the exact test-isolation gap that already bit this project once with `projects`.

Verified live end-to-end in a real browser, not just the suites: typed a phrase into the Sidebar search on the Dashboard, followed the result, and confirmed it scrolled to and flashed the actual entry; opened the export panel and confirmed both downloads; then created a Space through quick capture, added an entry, deleted it through the real UI (a Playwright `dialog` listener stayed silent throughout, confirming no native dialog fires), found it waiting in the trash, restored it, and confirmed both the Space and its entry came back. Both export routes were also exercised by curl against the real dev database and their output read. Every temporary Space was deleted and the trash emptied afterward, restoring the known 4-Space baseline. `npx vitest run` (frontend 533, +20) and both backend suites (`backend/`'s 320, `worker/`'s 304, +33 and +29) pass, `oxlint` shows the same 15 pre-existing warnings and no new ones (three new ones appeared mid-pass and were all fixed rather than accepted -- two throwaway destructures and one avoidable synchronous set-state), `vite build` clean. The new `trash` table needs the one-time `npx wrangler d1 execute thinking-spaces --remote --file=schema.sql` run against the deployed D1 database, alongside the schema changes already queued from the previous three passes.

Most recently: own top-level pages for Spaces, Resources and Syntheses -- thread (e) of the large personalization/structure message, raised with an explicit condition attached: each page must carry "all the relevant details needed per page... so that its clear why that even gets its own page."

That condition is what the audit was for, and it changed one of the three. Both Dashboard digests turned out to be a link plus one badge, so Resources and Syntheses had obvious room to be more. But the Dashboard *already* carried the complete Space list with search and a status filter with per-tab counts -- so a Spaces page repeating that would have failed the person's own test. Confirmed four forks via direct question, all toward the recommendation: build the Spaces page as the real index **and slim the Dashboard's list** so the two have different jobs; group Resources by type and show what actually references each; show Syntheses' lineage and promotion state; and slim both digests to a few items with a "See all" link rather than removing them.

The audit also found rich data that no index read: `listBacklinksForSpace` could answer which Spaces use a Resource (so a Resource nothing points at is one brought in and never used -- previously unanswerable without visiting every one), and `properties.sourceItemIds` had recorded what fed each Synthesis since the item-level-lineage pass, with only the reverse lookup ever built.

**Two new queries, both in `spaces.js`** rather than a new module, since they build directly on `listSpacesByTag` and a reader looking for "how does this app list Spaces" then finds all of it in one place. `listResourcesIndex()` batches every Resource's backlinks in one query (same reasoning `listAllWorkspaces` uses for member counts) and deduplicates them by Space -- two Reference blocks in one Space is still one Space using the Resource, and listing it twice reads as a bug. `listSynthesesIndex()` resolves `sourceItemIds` forward through `json_each` to the real Work items and their Spaces, reading each claim's *current* text rather than what was copied at compile time.

**The Spaces page** groups by status (from `SPACE_STATUSES`, so it can't drift from the vocabulary the status pill cycles), with cards carrying the glyph, Categories, connection count, open Tensions, Milestone progress and overdue state -- all already computed on every Space, so this needed no new backend read at all. Filter by title or by what a Space is working toward, plus four sorts.

**The Resources page** groups by type, with each Resource showing which Spaces draw on it, and a summary line counting how many are referenced nowhere. A Resource can carry several type tags and appears under each, since the groups are a way of finding it rather than an exclusive filing.

**The Syntheses page** shows each piece's kind, whether it's been promoted to Resource, and what it was distilled from -- each source claim linking straight to the entry it came from via the existing `?highlight=` convention.

**The Dashboard was slimmed to match.** Its Space list is now a glance surface: everything overdue, then the six most recently touched, with "See all N" linking to `/spaces`. Search, the status filter and per-Space delete all moved to the index -- delete specifically because managing the collection belongs there, and because a delete now goes to the trash rather than being permanent. Both digests keep four items plus their own "See all".

One real bug surfaced only by testing with richer data than the real database holds: a promoted Synthesis carries the `resource` tag too, so its *Synthesis kind* ("essay") was being read as a Resource type and inventing bogus "Essay" and "Synthesis" type headings on the Resources page. Fixed with an explicit `producedHere` flag -- such a Space groups under "Produced here" rather than under a kind that was never a kind of Resource.

Verified live in a real browser against deliberately richer temporary data, since the real 4-Space baseline has no Categories, Tensions, Milestones or typed Resources to exercise: confirmed the status grouping and rich card meta, type grouping with real backlinks and the unused count, and a Synthesis rendering its real lineage back to a claim in another Space. Fixed one visual nit found only by looking -- grid cards were stretching to the tallest in each row, leaving dead space. Every temporary Space was deleted and the trash emptied afterward, restoring the known 4-Space baseline. `npx vitest run` (frontend 553, +20 across three new page test files, with seven Dashboard tests for capabilities that *moved* replaced by four covering the new hand-off contract) and both backend suites (`backend/`'s 331, `worker/`'s 315, +10 each) pass, `oxlint` shows the same 15 pre-existing warnings and no new ones, `vite build` clean. Ported to `worker/` in the same pass; no schema change, so nothing new is queued against the deployed database.

Most recently: the UX standardization pass -- thread (f), prompted concretely by the "▶ Start a Session" button sitting "in such a random spot" but named as a general coherence problem rather than one button.

The audit turned up something sharper than "this needs a design system": **the convention already existed, written down, and was inverted in practice.** `index.css` documents `.btn-ghost-small` as "for per-row/per-item actions (move, remove) that shouldn't compete visually with primary actions" -- yet 35 of its 75 uses were page-level actions, while `.btn` was used only 10 times. Net effect: nothing looked more important than anything else, which is exactly why the Session button read as random -- it was styled identically to "Move up". One correction the audit forced on the first framing: `btn btn-primary` *was* being used consistently, all six uses being creation-form submits. The breakdown was specifically page-level actions outside forms, which made the fix far more surgical than "re-tier 45 call sites."

Four confirmed forks, all toward the recommendation: fix the hierarchy, give page actions a consistent home, make the fake buttons real, and consolidate the leftovers -- with three button tiers kept (the problem was misuse, not a missing tier) and an action bar under the page title as the home.

**`PageActions.jsx`** is that home: one strip directly under each page's `<h1>`. Deliberately a plain wrapper rather than something taking an array of action descriptors -- the actions stay written in the page as ordinary elements, and this only settles where they sit. The rule it enforces is now written out above the button styles in `index.css`, since the rule being unwritten is how it drifted in the first place. `ReportButton` gained a `tier` prop, because a Report on a whole page is one of that page's actions while a Report on one entry belongs inline with it. Applied across the Dashboard, Space, Workspace, Project, Spaces, Resources, Syntheses, Templates and Resource Templates pages; the Session button moved into the Space page's bar, which is the complaint that prompted all this. Four now-dead CSS classes (`.new-space-btn`, `.new-resource-btn`, `.dashboard-create-row`, `.report-row`) were removed, plus one stale comment referencing one of them. Destructive actions deliberately stayed in their bottom-of-page `danger-zone` rather than being hoisted into the bar -- "wherever it sits" is part of the written convention, and putting Delete next to Report would be worse, not more consistent.

**The 18 non-button interactive elements** turned out on inspection to be three different things, not one, so they got three different treatments rather than a blanket conversion. Ten were genuine buttons wearing a `<span>` (remove-chips, tag chips, click-to-edit fields) and became real `<button>`s, with a small CSS reset so they still read as the text they edit rather than sprouting browser chrome; `mappingFields.jsx`'s shared `EditableText` was one of them, so all three Mapping Tools got it at once. Six were modal overlay/stop-propagation handlers, which are not buttons at all -- but checking them surfaced a worse gap than the one being fixed: **none of the three overlays closed on Escape**, so a keyboard user could open the Legend, the Export panel or a confirm dialog and be stuck in it. A shared `useEscapeKey` hook fixed all three (shared precisely because three copies of a keyboard handler is what gets fixed in two places and left broken in the third; the handler is held in a ref, assigned in an effect, so no call site needs its own `useCallback` and no ref is touched during render). The last one, `TextBlock`'s click-to-edit `<div>`, was deliberately left alone and documented: it wraps prose rendered through `renderTextWithLinks`, so making it a button would nest interactive elements -- invalid HTML and a real bug. It needs a different fix (an explicit edit affordance), which is its own small pass.

**The leftovers**: `.insight-empty` and `.empty-note` were doing the same job in two declarations, now merged into one rule with `.insight-empty` kept as an alias rather than renamed across 11 Insights call sites -- one declaration, so they can no longer drift. And the single lowercase `"Could not load spaces:"` was brought in line with the app's other 20-odd error strings.

Worth recording what the audit found was *not* broken, since it shaped the scope: loading and error messages were already consistent (17 identical "Loading...", one error shape app-wide), and every creation form's submit was already correctly primary.

Verified live in a real browser: every page renders exactly one action bar with the right actions in it, the Space page's bar reads "Space Report | ▶ Start a Session" with zero loose buttons left after the Add-Entry form, both page actions resolve to the `btn` tier while per-entry controls stay quiet, the Legend and Export panel both close on Escape (previously impossible), and a converted click-to-edit control can be focused by keyboard and opened with Enter -- also previously impossible. `npx vitest run` (frontend 553, one existing assertion updated for the corrected error casing) and both backend suites (`backend/`'s 331, `worker/`'s 315, untouched -- this pass is frontend-only) pass, `oxlint` shows the same 15 pre-existing warnings and no new ones (one appeared mid-pass from the ref-during-render pattern and was fixed rather than accepted), `vite build` clean. No schema change, no `worker/` port needed, nothing new queued against the deployed database.

Most recently: Projects and Goals -- threads (a), (b) and (c) of the large personalization/structure message, taken together because (c) is only correct once (a) has landed. From the person's own framing: "projects are cool within a space, but im thinking these might make more sense as their own pages... and so the spaces would actually be pulling in the different project info and engaging with them within their respective space rather than making projects within the space for only that space," plus a separate Goals concept -- "projects are personally initiated, goals are revealed as relevant pursuits" -- prompted by wanting a Space to work toward more than one thing at a time, which the single free-text `goal` field could never hold.

Four forks confirmed via direct question before any code. Three went with the recommendation: build both pages plus the rename in one pass (they're one structural change, and doing (a) without (c) would leave a section named for something it no longer holds); derive a Project's Spaces from its own entries rather than storing a second membership list; and replace the `goal` field with real Goals rather than keeping both. The fourth went the *other* way from the recommendation: a Goal is **a pursuit that Projects also serve**, not a standalone label -- which is what gave `projects.goal_id` its reason to exist and made the Goals page worth having at all.

**The inversion.** `projects.space_id` is gone. Unlike `spaces.accent`, it could not be left in place and ignored: it carried NOT NULL plus a foreign key into `spaces`, so a standalone Project could not be inserted at all while it existed -- the failure surfaced immediately, as a real foreign-key error, when the first spaceless Project was created. `migrateProjectsSpaceless()` does the standard SQLite make-copy-swap rebuild (a column carrying a foreign key can't be dropped), and is idempotent. Everything that read a Project's Space was rewritten to derive it instead: `listProjectsForSpace` joins through `blocks.properties.projectId`, `getProjectReport` gathers members across every Space and names the Goal, `getGraphData` draws one `contains-project` edge per Space a Project has work in, and `deleteSpace` no longer deletes Projects at all -- one Space's deletion must never take a Project other Spaces also feed.

**Goals.** A new `goals` table plus `spaces.goal_ids` (a JSON array, the same many-to-many shape `tags` uses) and `projects.goal_id`. A Goal deliberately has no Milestones or Sessions of its own -- give it those and it becomes a Project with a different name -- so what `listGoalsIndex()` answers is reach: which Spaces work toward it, which Projects serve it, batched across every Goal in two queries rather than one per Goal.

**The pages.** `/projects` earns its place on what no Space page could show: every Project, the Goal it serves, its progress, and every Space its work actually happens in. `/projects/:id` (moved from `/spaces/:id/projects/:id`) groups its members under the Space each one lives in, and since it has no Space of its own to add into, one Space picker governs both "add new" and "pull in something already there." `/goals` deliberately has no per-Goal page beneath it: a Project has real content that needs somewhere to live, whereas everything a Goal has fits on its card. On the Space page, "Working toward" became a chip toggle over real Goals plus a read-only line naming the Projects that Space feeds, and an entry's own Project picker now offers *every* Project rather than the Space's own -- assigning one there is precisely how a Space comes to feed a Project at all, so filtering to "Projects already here" would have made the first assignment impossible.

**The rename (thread (c)).** With Projects moved out, the "Organize" panel holds exactly one thing and is named for it: Workspaces. `WorkspaceList`'s own inner `<h2>` went with it -- the `<summary>` now says "Workspaces", so an inner heading would only repeat it, the same duplication `TrailSpine`'s own heading was removed for in the coherence pass. Caught by looking at the rendered page, not by reading the diff.

Verified live end-to-end in a real browser against real data, not just the suites: a Project spanning two Spaces reported both under "Spaces:" in its own Report, showed both as grouped sections on its page, drew two `contains-project` Graph edges, and appeared on both Spaces' own "Work here feeds" line; its Goal showed both Spaces and the Project; toggling a Goal chip off and back on wrote correctly both ways; and the three panels on a Space page read "Details / Workspaces / Trail". Every temporary Space, Project and Goal was deleted afterward and the trash emptied, restoring the real database's known 4-Space baseline. `npx vitest run` (frontend 578, +25 across new `ProjectsPage.test.jsx`/`GoalsPage.test.jsx` and a rewritten `ProjectPage.test.jsx`) and both backend suites (`backend/`'s 346, `worker/`'s 330, +15 each) pass, `oxlint` shows the same 15 pre-existing warnings and no new ones (one appeared mid-pass from a synchronous setState in an effect and was fixed rather than accepted), `vite build` clean. Both `resetDb` helpers gained the `goals` table -- the same test-isolation gap that already bit this project twice, with `projects` and then `trash`.

Ported to `worker/` in full. `worker/DEPLOY.md` gained a "Queued, not yet applied to the deployed database" section listing every schema change accumulated since the last live deployment (the `goals` table, `spaces.goal_ids`, `spaces.theme`, `workspaces.kind`, `projects.goal_id`, `activity_log.block_id`, the Resource Templates seed, and the Projects rebuild), plus a separate note on `migrateSpaceStatuses()` -- the one Node-side boot migration that has a real counterpart on the deployed data (it was populated before the status vocabulary changed) and that a Worker has no boot hook to run itself.


Most recently: Trail auto-tracking -- thread (g), the last of the large personalization/structure message, from the person's own verdict: "trail is useless, in my opinion, unless it is auto-tracking everything happening within the space and sending review reports to the main log."

The audit settled the scope before anything was built, and turned up something blunter than "Trail logs a bit but not enough": **Trail had never recorded anything at all.** Zero `trail_entries` in the real database, across 51 Spaces created and 98 entries added. Auto entries were written from exactly one place -- Skeleton edits (`=`/`?`/`!` promotion, filing a line, pairing a Tension) -- so on a Space where those paths never get used, the section reads "No history yet." forever. "Useless" was a factual description, not an opinion. Three smaller findings came with it: the data mostly already existed in the wrong table (`activity_log` held 98 `block_added` rows and more, all carrying `space_id`, and `listGlobalActivity` merged Trail *into* the Log while nothing did the mirror); `updateBlockContent` wrote no log row at all, so editing an entry -- and reaching a Milestone or completing a Session, both content edits -- was invisible everywhere; and the "review reports to the main log" half already worked mechanically (`trail_review` is merged into the Log and labeled there), it had simply never fired, because no Review had ever been logged.

Three forks confirmed via direct question, with one real tension flagged first: CLAUDE.md deliberately said the Log "doesn't log every keystroke-level content edit -- that would bury the events actually worth seeing," so auto-tracking everything runs against a stated principle on purpose. The person picked **show what's already recorded and record what's missing**, **every edit too, but coalesced** (not the recommended "meaningful changes only" -- the fuller option), and **leave Reviews alone**, since that half starts working by itself once Trail has content.

Landed as three pieces. **Recording**, which goes into `activity_log` rather than Trail -- a deliberate split, since every `trail_entries` row takes a full Skeleton snapshot and that cost should stay tied to Skeleton changes, not to every keystroke-session. `updateBlockContent` now records each edit in one of two shapes: a change `describeBlockContentChange` can actually name gets its own `block_changed` row (that function gained a Session-completed case alongside its existing Milestone-reached one), and anything else goes through a new `logBlockEdit`, which folds into the previous row when it's the same entry edited again within ten minutes -- bumping a new `activity_log.event_count` column instead of inserting another. Ten minutes is roughly "one sitting": long enough that rewording a paragraph stays one line, short enough that returning after lunch reads as a separate occasion. A named change never coalesces, since it happened once. `updateBlockContent` also gained `{ logEdit: false }`, which all four of `skeleton.js`'s own calls pass -- each of those already writes a Trail entry for the same change, and recording it twice would report one action twice. A due-date change is logged too, alongside the status change that already was.

**Reading**: `listSpaceHistory` returns a Space's Trail entries and its activity in one chronological list, each row tagged `source`. Merged in JS rather than SQL because a Trail row needs its snapshot parsed and an activity row has no snapshot column -- a UNION would mean inventing null columns on both sides to line them up. `listTrailEntries` itself is untouched, since Reports, Review and Rewind all depend on it returning real Trail rows only.

**Rendering**: `TrailSpine.jsx` renders both. A Trail entry keeps everything it had (expand, the editable note, Rewind's Now-vs-As-of comparison); an activity row is one flat, quieter line with its kind, its summary, a `×N` when it stands for several occurrences, and a `?highlight=` link through to the entry it's about. It deliberately doesn't expand -- with no snapshot behind it, offering Rewind would promise a comparison it can't produce.

Two real problems surfaced only by looking at the rendered page, neither caught by a passing test. Every row repeated the Space's own name (`Edited a text entry in "Foo"` -- written for the Log, where a row *must* say which Space it belongs to, but pure noise on that Space's own page, on every single line). `listSpaceHistory` now strips it, using an exact match against the Space's own known title rather than a guess at the shape of arbitrary prose, and restores the leading capital that stripping a `"Title" ` prefix would otherwise eat. And the adaptive-density default from the coherence audit had quietly broken: `spaceHasHistory` tested `trail.length > 0`, which is now true of every Space that exists, so the Trail panel would have defaulted open everywhere -- it now looks past a Space's own creation row, so a Space whose entire history is "you made this" still counts as nothing to look at yet.

Verified live in a real browser against a temporary Space, not just the suites: five saves in a row collapsed into one row reading `×5`, a Milestone reached and a Session completing each got their own named line, and a manual note and a Review still expanded with the Now-vs-As-of comparison while no activity row offered one. The temporary Space was deleted afterward and the trash emptied, restoring the real database's known 4-Space baseline. `npx vitest run` (frontend 589, +11 across a new `TrailSpine.test.jsx` and the two adaptive-density cases in `SpacePage.test.jsx`) and both backend suites (`backend/`'s 358, `worker/`'s 338, +12 and +8) pass, `oxlint` shows the same 15 pre-existing warnings and no new ones, `vite build` clean. Ported to `worker/` in full; the new `activity_log.event_count` column is queued in `worker/DEPLOY.md` alongside the schema changes already waiting there.

One thing deliberately not built, and worth naming rather than leaving to be discovered: **Trail doesn't paginate.** The Log page already caps at 40 rows with a "Show N more"; a Space's own history returns everything. That's fine at today's volumes (a few rows per Space) but it will grow now that edits are recorded, and it's the obvious next thing here if a long-lived Space ever starts feeling heavy.


**Standing principle, not a one-time pass:** per the Dev Mode decision, anything that can be created should have an equally ordinary way to be removed or changed, on whatever page it's created or shown — check for this whenever new content or a new page is added, rather than treating editability as a separate pass that's ever fully "done."

**Open:** the Workspaces pass deliberately deferred three things, called out explicitly rather than silently dropped. All three are now done for every Block type; one narrower thread remains:
1. Bespoke, "engineering/artistic" per-Tool-type layouts inside a Workspace — all five Block types now have their own (`TextWorkshop.jsx`, `ListWorkshop.jsx`, `ReferenceWorkshop.jsx`, `MediaWorkshop.jsx`, `ComparisonWorkshop.jsx`). The five View types (Timeline, Progress, Streak, Ledger, Graph) still render as their ordinary `component` wherever they'd normally appear — none has a `workshopComponent` yet. Whether a View even needs one (they're already read-only, computed lenses rather than directly-edited content) is an open question, not an assumed next step.
2. ~~Creation Mode integration~~ — done for `CreateSpace.jsx`; `CreateResource.jsx` was deliberately left out (see the Creation Mode vocabulary entry above) and remains a possible, separate follow-up if wanted.
3. ~~Relational Map integration~~ — done; Workspaces are real nodes/edges in the Graph now.
4. Voice capture, raised in the same design corpus that prompted the Writing Surface/Skeleton pass — deliberately deferred as the most speculative and environment-dependent idea raised, not forgotten.
5. ~~More Work Types~~ — Analysis, Deduction, Definition, and Demonstration joined Assessment and Question in one pass, then Insight and Implication, then Hypothesis and Objection once the support-linking structure made Objection viable, each a smaller follow-up at the person's direct request. Walked back most of the rest of the original list (derive, infer, plan, outline, expound, explain, integrate, clarify, ...) as already covered by an existing Tool or a near-duplicate of one of the ten now built, confirmed via direct question rather than assumed. Considered closed for now, not permanently — a genuinely new thinking-verb can still be raised later.
6. ~~Whether a support point's link should be able to reach a claim in a *different* Space~~ -- done, see the Roadmap entry above.
7. ~~The front-end discoverability thread~~ — the person clarified the "full redesign" ask as actually being "hard to find things": formatting, organizing, cataloging, visual representation. A grounded audit (reading the actual pages, not guessing) found four concrete, independent gaps; all four are now built, across two passes:
   - `family` (`'general'`/`'work'`) added to every `blockRegistry` entry, grouping the Tools catalog (`ToolsPage.jsx`, General/Work sub-headings under "Blocks") and the "+ Add Block" dropdown (`NewBlockForm.jsx`, native `<optgroup>`s) -- fixed Creation Mode's Tools step for free too (`CreateSpace.jsx` reuses `NewBlockForm` directly), and `NewBlockForm`'s Work options are now read live from the registry, so a future Work Type needs no edit to that file.
   - The Dashboard's Space list gained a title search box and a status filter (reusing the same chip-toggle pattern as the Category filter strip), and all 5 digest sections became native `<details>` elements, collapsible individually rather than one unbroken stack.
   - A Space's block feed gained a type-based filter strip, independent of and combinable with the topic-based Category strip -- only shows once a Space actually has more than one distinct Block type on it.
   - Synthesis's cross-Space Work-item picker gained a statement-text search box and a "N selected" count, so narrowing the view never loses track of what's already checked.

   This addresses the specific pain points the person named, not a wholesale visual reskin -- if a further redesign is wanted beyond findability, that's a new, separately-scoped ask, not assumed to follow from this one.
8. ~~Item-level Synthesis lineage~~ -- done, see the Roadmap entry above.
9. ~~The Time arc~~ — done: due dates, Milestones, Sessions, Review, and a fifth Insights facet surfacing all of it together are all built, across four sequenced layers, closing out the felt-but-unspecific gap the person originally raised.
10. ~~The "professionalism pass"~~ — done. All four confirmed threads (Tool/section personality, dialogs/interaction polish, the three correctness fixes, and the organization pass) are built, see the Roadmap entry above for each. Re-verified live that the `queries.js` split (`backend/src/db/queries/`) changed no behavior: a full curl-driven regression pass against a temporary Space, confirmed against the known 4-Space baseline afterward.
11. ~~`deleteSpace`'s pre-existing `workspaces` foreign-key gap~~ — fixed, see the Testing section below and the Roadmap entry above. Flagged three times (Reports, the `queries.js` split, and finally caught by a real test) before actually being fixed.
12. ~~Frontend test coverage~~ — done, including page-level components: every pure logic module, both registries, shared infrastructure, all Block/View components, and every page (Dashboard, SpacePage, CreateSpace, CreateResource, CreateSynthesis, WorkspacePage, ToolsPage, LogPage, InsightsPage, GraphPage, TemplatesPage, TemplateEditor) now has its own test file (see the Testing section below and the Roadmap entry above).
13. ~~Space page metadata layout & fixed content width~~ — done, see the Visual direction entry above. Initially presented alongside the aesthetics pass's other three findings and declined, then built right after at the person's explicit request to include it too.
14. ~~Discoverability & affordances~~ — done, see the Roadmap entry above (filter counts, live Tool descriptions in "+ Add Block", a glyph tooltip, the Tools catalog's new Skeleton & Tensions section, and Synthesis's forward-mention of promotion).
15. ~~Data durability & search~~ -- **done**, see the Roadmap entry above: an in-app export (JSON backup plus a readable Markdown archive), full-text search across entry content reached from a persistent Sidebar box, and a trash table making every delete undoable. One piece was deliberately left for later and is the only part of this item still open: **a restore/import path** -- reading a previous export back in. Confirmed via direct question to ship export-only first, since an export you can keep is the whole risk fix and a restore needs real decisions about id collisions and merge-versus-replace. Worth building the day that question actually arises, not before.
16. ~~Interaction & workflow~~ — done, see the Roadmap entry above: a save/delete toast, a "needs attention" badge in TopNav (a louder in-app badge, confirmed via direct question over email/desktop notifications), quick capture (just a title, confirmed over a first-thought-becomes-content flow), and adaptive density for the Space page's details panel.
17. A three-part desktop-experience request. (a) ~~"feel spread out like a desktop app"~~ — done, see the persistent-sidebar Roadmap entry above. (b) ~~a desktop icon/launcher~~ — done, once the person supplied their OS (Windows 11) and local repo path: a new `launcher/` folder (`Start-ThinkingSpaces.bat`, `Setup-Desktop-Icon.ps1`, `thinking-spaces.ico` — a thought-bubble glyph hand-drawn in the app's own matte-black/oxblood/gold palette, not a stock icon) gives a one-time-setup desktop shortcut that `git pull`s, installs dependencies the first time only, starts both dev servers each in their own window, and opens the browser -- the exact three manual steps `README.md` used to ask for by hand every time. Not part of the app's own frontend/backend, so it's undocumented by the Testing section and untouched by `vitest`/`oxlint`/`vite build` -- verified instead by reading the script logic and by opening the generated `.ico` at 16px/32px/256px to confirm it stays legible small. (c) ~~the dashboard-titling ask~~ — done, see the Week-calendar Roadmap entry above: the real gap was the old "This week" digest not explaining how it decided what counted as "this week," not a mismatched label. A second, separate observation raised in the same message -- that the Tools catalog's demos read as static rather than something you can actually click into -- is tracked as its own item (19 below), not folded into the Dashboard one.
18. ~~A Dashboard "week" digest redesign~~ — done, see the Roadmap entry above: `getWeekCalendar()` replaced `listRecentTrailEntries()` with a real Sunday-Saturday calendar grid mixing past Trail activity with upcoming due dates/Milestone targets, confirmed via direct question over two smaller options (a separate week-ahead section, or just relabeling the existing list). Click-through stayed at "the Space it belongs to," confirmed over a deeper block-level deep link the Space page has no way to support yet (see item 20 below for that gap).
19. ~~Interactive Tools-catalog demos~~ — done, see the Roadmap entry above: every Block type's demo on `/tools` is now genuinely clickable via `ToolsPage.jsx`'s `DemoBlock` and the `onSave` override every Block component now supports.
20. ~~Deep-linking to a specific block/item within a Space~~ -- done, see the Roadmap entry above.
21. ~~Hosting, Step 2 of 2~~ -- done, see the Roadmap entry above: live at `https://thinking.thegardners.xyz`, real data migrated, Access-gated, `/api/*` routed through the Worker.
22. ~~Automated test coverage for `worker/`'s D1 port~~ -- done, see the Roadmap entry above.
23. ~~Actually running the Cloudflare deployment~~ -- done, with the person providing a scoped API token (D1:Edit, Workers Scripts:Edit, Workers Routes:Edit + DNS:Edit for `thegardners.xyz`) since a headless session can't complete `wrangler login`'s browser OAuth. Access-policy editing was deliberately left out of the token's scope and done by the person directly in the dashboard, per the original plan.
24. A large feedback batch, raised the moment the live deployment above was confirmed working, covering seven separate threads at once. All are now done (see the Roadmap entry above and below): a `Setup-Desktop-Icon-Web.ps1` shortcut opening the real hosted site directly (folded in explicitly at the person's request, alongside this batch, since the desktop-icon thread (item 17) had only ever covered the local-dev shortcut); a Link-a-claim fix -- confirmed via direct code read that support-linking already works end-to-end, so the actual gap was discoverability, not a broken feature, closed with a button tooltip and a fuller empty-state explanation; ~~(a) the vocabulary audit~~; ~~(b) Sessions/Milestones as a real structural concept~~ (Projects, see the Roadmap entry below -- named "Project" rather than "Goal" to avoid colliding with a Space's own pre-existing `goal` field, a real naming collision caught while building rather than assumed away); ~~(c) SpaceGlyph reactivity~~ (see the Roadmap entry below -- Review staleness deliberately left out, confirmed via direct question, since it's true of nearly every Space nearly all the time and would read as permanently alarmed, the same reasoning that already excluded it from the "needs attention" badge); ~~(d) Resource Templates~~ (see the Roadmap entry below -- landed as a new, separate mechanism replacing three generic facets entirely per type, all 17 named types built in one pass, all three confirmed the opposite way from what was recommended); ~~(e) Synthesis Kinds/Source Material~~ (see the Roadmap entry below -- the real gap was Source Material having no visible label on the actual Space page, not the term itself; fixed with real Categories on every starter block, plus a free-text Kind input closing an asymmetry with Resources' own type tags); and ~~(f) Insights' interpretive readings~~ (see the Roadmap entry below -- a computed `reading` sentence per facet, additive framing on top of the existing bars/counts, never replacing them). Four forks were confirmed via direct question before scoping the rest: Personalize/editability narrows to symbol reactivity first (glyphs/markers that visibly react to more of a Space's own state, not full Windows-style theming in one shot); Session/Milestone structure becomes a real dedicated concept (not just individual Block types loosely implying structure); the Week view gets a real brainstorm of new value before any rebuild, not just a justification of what exists; and the naming complaint ("Block" is brutalist, "Text" feels patronizing) becomes a full vocabulary audit, not a two-word fix. Still open, each to be scoped and built as its own slice: ~~(a) a vocabulary audit across the app's full naming surface, not just the two terms named~~ -- done, see the Roadmap entry above; ~~(b) Sessions and Milestones becoming a real structural concept within a Space~~ -- done as Projects, see the Roadmap entry below (Projects-as-Graph-nodes, an Insights facet, and a Project-level Report were all deliberately left out, not raised by the person); ~~(c) expanding SpaceGlyph's reactivity beyond its current 3 computed dimensions~~ -- done, see the Roadmap entry below (widened to overdue and Milestone-reached ratio; Review staleness deliberately left out); ~~(d) Resource templates -- real per-type starter content/facets~~ -- done, see the Roadmap entry below (all 17 named types built; a Resource-level Insights facet and Graph integration were not raised by the person, so neither was built); ~~(e) Synthesis needing more Kinds beyond the current small suggestion list, plus clearer copy or naming for what "Source Material" actually means~~ -- done, see the Roadmap entry below (10 named Kinds plus a free-text input; Source Material and the other two starter blocks now carry real Categories); ~~(f) Insights gaining interpretive/relevance framing per data point, not just raw numbers/bars~~ -- done, see the Roadmap entry below (a computed `reading` per facet, all five); ~~(g) a concrete brainstorm of the Week view's real value (currently mostly "hope, foresight" per the person's own words) before deciding whether/how to rebuild it~~ -- done, see the Roadmap entry below (a brainstorm of four candidate directions presented via direct question; the person picked two -- making Milestones/Sessions actionable right from the calendar cell, and a bulk "Review this week" ritual over every genuinely-active Space; the other two candidates, surfacing undated things worth scheduling and leaving the view as-is, were not picked). This closes out the entire item-24 feedback batch -- all seven threads are now done.
25. ~~A coherence audit's second thread~~ -- done, see the Roadmap entry above: SpacePage.jsx's eleven stacked sections are now grouped into a non-collapsing "Think" visual label (Skeleton-completeness-strip/filters/feed/Add-Entry) plus two new adaptively-collapsible panels, "Organize" (Workspaces+Projects, closing the real bug that both used to render at full size even when completely empty) and a now-foldable "Trail" (unchanged in name/contents, just gaining the same adaptive open/closed default). Thread one (framing copy on the Dashboard/Insights/Log/Trail) is done too -- the coherence audit is fully closed out.
26. Content ingestion (links, uploaded files) is done for `backend/` -- see the Roadmap entry above -- but `worker/`'s Cloudflare-hosted upload route was deliberately not built: it needs R2 (Cloudflare's object storage), which this session can't provision without the person's help, the same D1-API-token precedent the original Hosting pass followed. The link-preview route *is* ported to `worker/` already (it needs no storage, just an outbound fetch). Provisioning R2 and porting `backend/src/routes/uploads.js` to `worker/src/index.js` + an R2 binding is the remaining piece before file uploads work on the live, deployed site.
27. The change-legibility pass (see the Roadmap entry above) confirmed its scope as content-aware toasts, flash-highlight-on-create, and a compact legend -- two related ideas were named and deliberately left out rather than assumed in: implication notes beyond the three built (Milestone reached, a Space's due date, a Tension pairing) -- e.g. a Reference's target Space, a Category assignment, a Review being logged -- would each need their own real, nameable "and this means..." sentence rather than a guessed generic one, so this stays a name-one-at-a-time addition, the same discipline Work Types and Resource Templates already follow; and flash-highlighting a row whose *content* changes (not just a fresh create) -- judged lower value since an update happens to a row already on screen, but would need every Block component's `onBlocksChanged` callback to start passing back which block id changed (currently just `refetchAll` with no argument), a real refactor touching every Tool, not a small addition.

28. A large personalization/structure message, raised in one go alongside the theming complaint above. The theming half is done (see the Roadmap entry above); **seven threads from that same message were explicitly deferred**, confirmed as their own later passes rather than folded in, and are listed here in the person's own framing rather than restated as tickets. Three, (d), (e) and (f), are now done; the other four are still open:
    - ~~**(a) Projects as their own top-level page**~~ -- **done**, see the Roadmap entry above. The inversion is real: `projects.space_id` is gone (rebuilt, not just ignored -- it carried NOT NULL plus a foreign key, which blocked inserting a standalone Project at all), and a Project's Spaces are now derived from wherever its own entries live, so one Project can span several. A Space "pulls in" the Projects its own work feeds, read-only, exactly as the person described.
    - ~~**(b) A Goals page**~~ -- **done**, see the Roadmap entry above. A Goal has no Milestones or Sessions of its own on purpose; what it has is reach. Confirmed via direct question, *against* the recommendation, that a Goal is a pursuit Projects also serve -- which is what gave `projects.goal_id` its reason to exist.
    - ~~**(c) Rename the Space page's "Organize" section to "Workspace"**~~ -- **done** in the same pass, since (a) is what made it correct. Now reads "Workspaces".
    - ~~**(d) Beef up Workspaces into real specialized thinking environments**~~ -- **done**, see the Roadmap entry above. All eight named kinds are built (Analyst, Critical Thinking, Problem Formulation, Conceptualize, Worldview Assessment, Modeling, Etymology, Word-Concept Mapping), along with the three genuinely new Tools two of them needed (Word Evolution, Concept Map, Model, under a new `mapping` Tool family) and the top-level `/workspaces` page. Confirmed via direct question to do all of it in one pass rather than shipping kinds first and the new Tools later. Deliberately *not* built, since neither was raised: a `workshopComponent` for any of the three new Tools (they render the same way everywhere for now, like most Tools), and Workspace Kinds as an editable-from-inside-the-app mechanism the way Templates and Resource Templates are -- the registry is source code, which is what lets a kind carry page layout at all.
    - ~~**(e) Own top-level pages for Spaces, Resources, and Syntheses**~~ -- **done**, see the Roadmap entry above. Each earns its place on something no index showed before: the Spaces page on per-Space state the Dashboard's one-line rows never carried, Resources on which ones are actually referenced anywhere, Syntheses on what each was distilled from. The Dashboard was slimmed in the same pass so it and the new pages have different jobs rather than duplicating each other.
    - ~~**(f) A UX standardization pass**~~ -- **done**, see the Roadmap entry above. The root cause turned out to be that the button convention already existed in `index.css` and had been inverted in practice, so page actions looked exactly like per-row ones. Fixed with a `PageActions` bar under every page's title, the convention written down where it governs, real buttons in place of interactive spans, and Escape-to-close on all three overlays. One item deliberately left: `TextBlock`'s click-to-edit `<div>` can't become a button without nesting interactive elements, so it needs its own small pass for an explicit edit affordance.
    - ~~**(g) Trail auto-tracking**~~ -- **done**, see the Roadmap entry above. Checking first was the right call, but not for the reason expected: the audit found Trail had recorded *nothing at all* on any real Space, ever, since auto entries only ever fired on a Skeleton edit. A Space's own recorded activity now shows in its Trail, ordinary edits are recorded (coalesced, so a writing session is one line), and the "review reports to the main log" half needed no new code -- it already worked, it had just never had a Review to carry. One thing left open here: Trail doesn't paginate the way the Log does, which will matter on a long-lived Space eventually.

    All seven threads of that batch are now done.

Confirm which is the actual next priority before starting it, same as any other pass.

## Testing
Every prior pass was verified by hand -- live curl/Playwright checks against a temporary "TEST" Space, thrown away the moment the pass shipped. That leaves nothing to catch a regression when a later change touches shared code a dozen features now depend on. **Backend**: Vitest (`backend/vitest.config.js`), one test file per `backend/src/db/queries/*.js` module under `backend/src/db/queries/__tests__/`, mirroring the module split itself exactly. Two pure modules sit outside that split and are tested alongside them: `backend/test/linkPreview.test.js` and `backend/test/exportFormat.test.js`, both mirrored verbatim into `worker/` since the modules they cover are verbatim copies too. Every test file gets its own fresh, isolated `:memory:` database -- never the real personal data file -- via `THINKING_SPACES_DB_PATH` (read by `backend/src/db/index.js`, unset in normal dev/production use) set once in `vitest.config.js`; `backend/test/helpers/resetDb.js`'s `resetDb()` clears every table between tests within one file. Run with `npm test` from `backend/`. **Frontend**: Vitest (`frontend/vitest.config.js`, jsdom environment) plus React Testing Library and `@testing-library/user-event`; `frontend/test/setup.js` extends `expect()` with jest-dom's matchers and cleans up the DOM between tests. `frontend/src/api.js` is mocked per test file (`vi.mock('../api.js')`) rather than faking real HTTP -- every one of its exports already funnels through one `request()` helper, so mocking the module's own functions is simpler than standing up a fake server. Test files sit next to the source they cover (`Foo.jsx` + `Foo.test.jsx`), not in a separate mirrored tree, since the frontend has no equivalent to the backend's `queries/` module-per-file split to mirror. Covers every pure logic module (`listItems.js`, `textLinks.jsx`, `api.js`'s request handling), both registries' own internal consistency (`blocks.js`/`views.js` -- every entry's shape, every `worksWith` reference resolves, every View's `appliesTo` actually accepts its own demo data), shared infrastructure (`usePageTitle`, `ConfirmDialog`, `Sidebar`), all eight Block components, and all five View components (`GraphView`'s own coverage is deliberately a shallow smoke test, not deep interaction testing -- it's a continuous hand-rolled physics simulation whose correctness is best judged visually, not worth faking real layout geometry jsdom doesn't provide). Page-level components are covered too, one test file per page under `frontend/src/pages/` (`Dashboard`, `SpacePage`, `CreateSpace`, `CreateResource`, `CreateSynthesis`, `WorkspacePage`, `ToolsPage`, `LogPage`, `InsightsPage`, `GraphPage`, `TemplatesPage`, `TemplateEditor`) -- real interaction tests (typing, clicking, confirming a themed dialog) against a mocked `api.js`, not shallow snapshots, following the same patterns the component-level pass already established (`react-router-dom`'s `useNavigate` mocked per file that navigates; any page using `useConfirmDialog()` wrapped in `ConfirmDialogProvider`, with dialog buttons queried via `within()` to disambiguate from same-labeled buttons elsewhere on the page). `GraphPage` stubs `requestAnimationFrame` to a real macrotask, same reasoning as `GraphView`'s own test. Run with `npm test` from `frontend/`. **Worker**: Vitest via `@cloudflare/vitest-plugin` (`worker/vitest.config.js`) -- tests run bundled inside a real, sandboxed Workers runtime (Miniflare) against an actual D1 instance, not a mock, so they exercise the real `env.DB.prepare()/.bind()/.all()` calls `worker/src/db/*.js` makes. `worker/test/setup.js` applies `schema.sql` (read on the Node side in `vitest.config.js`, since setup files run with no host filesystem access, and flattened to one statement per line for D1's own stricter `.exec()`) once per test file's worker startup; `worker/test/helpers/resetDb.js` mirrors `backend/test/helpers/resetDb.js`'s per-test `beforeEach` reset, since state persists across `it()` blocks within one file here too. One test file per `worker/src/db/*.js` module under `worker/src/db/__tests__/`, porting `backend/`'s own assertions directly (231 tests across 17 files) -- `migrateWorkItemSupport`/`migrateTextBlockLines` are the two backend-only cases with nothing to port, since neither function exists on the worker side (a D1 database starts fresh on the current content shapes). Run with `npm test` from `worker/`.

## Transparency / indexability requirement
Every Tool — every Block type and every View type — must be registered in one single, readable file each (`frontend/src/registry/blocks.js`, `frontend/src/registry/views.js`) — never scattered across the codebase. `frontend/src/registry/workspaceKinds.js` follows the same rule for the eight Workspace Kinds (see the vocabulary entry above): one file, every environment, and the database stores only which kind a Workspace is. The point is that the person can open one file and see the complete list of what exists; the planned Tools catalog page makes this visible *in the app itself*, not just in the source. Keep all cross-Space queries (counts, status distribution, backlink lookups, tag membership) going through a small, consistent set of query functions imported from `backend/src/db/queries.js` rather than ad hoc SQL sprinkled around — this is what makes Dashboard metrics and things like the Resources digest buildable without new plumbing. As of the organization pass (see Roadmap), `queries.js` itself is a thin barrel re-exporting from `backend/src/db/queries/*.js` — one small file per concern (Spaces, Blocks, Workspaces, Skeleton, Trail, Review, Work, the Log, Insights, Reports, Dashboard aggregations) — rather than one ~1,800-line file; every route still imports from `queries.js` and never reaches into a `queries/*.js` file directly, so the "one entry point" property this section asks for is unchanged, it's just backed by readable per-concern files instead of one large one.
