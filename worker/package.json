{
  "name": "thinking-spaces-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "setup": "wrangler d1 execute thinking-spaces --local --file=schema.sql && wrangler d1 execute thinking-spaces --local --file=templates-seed.sql",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.3",
    "vitest": "^4.1.11",
    "wrangler": "^3.78.0"
  }
}
